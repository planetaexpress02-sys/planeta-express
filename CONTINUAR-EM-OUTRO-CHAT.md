# CONTEXTO DO PROJETO — Sistema Planeta Express (para continuar em outro chat)

> Cole este arquivo inteiro no início de uma nova conversa do Claude Code (ou diga: "leia CONTINUAR-EM-OUTRO-CHAT.md na pasta Planeta Express Transportes"). Ele resume tudo o que já foi construído para você dar continuidade sem perder nada.

---

## ⚡ ONDE O PROJETO ESTÁ (30/08/2026 — v8.0)

**v8.0 publicada e sincronizada** (commit `919fbc4`). Árvore do git limpa, `main` = `origin/main`, GitHub Pages no ar, pasta offline e celular (`Planeta Express - CELULAR.html`, ~3,90 MB) na mesma versão. Assets em `?v=203`, cache SW `planeta-express-v8-0`, rodapé `v8.0`.

**🚧 PENDÊNCIA ABERTA DA v8.0 — A LOGO NOVA.** O cliente pediu para trocar a marca por uma arte **preta e dourada** que ele anexou no chat, reprovou a reconstrução que eu fiz em vetor, e **ficou de salvar o PNG dele em `assets\logo-original.png`**. Enquanto não chegar, a marca do sistema segue a da v7.8. Ver a seção da v8.0 no histórico.

**O que MUDOU muito nas últimas versões — leia antes de mexer:**

| Saiu | Quando | Para onde foi |
|---|---|---|
| Chatbot (IA Planeta) | v7.1 | removido; `_iaMotorista`/`_iaVeiculo` viraram `motoristaPorNome()`/`veiculoPorTexto()` no app.js |
| Monitoramento (mapa) | v7.1 | removido |
| Aba **Início** | v7.2 | o **Painel de Controle** virou a tela de entrada |
| Clima de Londrina + sino de alertas | v7.3 | removidos do topo |
| Aba **Exames** | v7.3 | aba "Exames e Certificados" **dentro da ficha do motorista** |
| Aba **Direção Defensiva** | v7.5 | seção "Certificados" na mesma aba da ficha |
| Aba **Tacógrafos** | v7.5 | card na ficha do **cavalo** (só `tipo==='Cavalo'`) |

**🔑 A regra que explica todas essas remoções:** essas telas **não tinham dado próprio** — eram *views* sobre `DB.vencimentos`. Por isso nada se perdeu. Antes de remover qualquer tela, **cheque se ela guarda dado ou só mostra**.

**Visual (v7.6→7.9):** base **quase preta** (`--void #050609` … `--surf3 #161C27`) com acento **ciano/azul** (`--au #00e5ff`, `--au3 #0077ff`). O ouro foi testado na v7.6 e **o cliente não gostou** — não repetir sem ele pedir. **As ~1.000 cores fixas viraram tokens no `:root` do `estilo2.css`: mudar o tema é mexer LÁ.** O relatório A4 (`.rel-*`) fica de fora de propósito (papel branco).

**Marca (v7.8):** três arquivos, todos transparentes e recortados — `logo-claro.png` (interface escura), `logo-escuro.png` (relatório A4) e `logo-marca.png` (monograma, selos pequenos). O `logo.png` original é quadrado branco opaco e só serve de ícone. **Trocou a arte? Gere as três.**

---

## 1. O QUE É
Sistema de Gestão Operacional feito sob medida para a **Planeta Express Transportes** (transportadora de cargas frigorificadas, CNPJ 26.126.673/0001-86, base em Londrina/PR). Cliente é **não-técnico**, fala **pt-BR**, quer tudo **pronto para usar**, design **moderno e profissional**. Trate sempre com linguagem simples e passo a passo.

## 2. ONDE FICA
- Pasta da empresa (working dir): `C:\Users\uilia\OneDrive\Área de Trabalho\Planeta Express Transportes\`
- **O sistema em si:** subpasta `Sistema Planeta Express\` — abre com **duplo clique no `index.html`** (100% offline) OU pelo site online (**GitHub Pages**: https://planetaexpress02-sys.github.io/planeta-express/ — Netlify aposentado, ver item 9).
- Documentos reais da empresa estão nas pastas irmãs: `Documentos Motoristas`, `Documentos Veículos`, `Documentos Jurídicos`, `Certificados Tacógrafos`, `Relatórios de Manutenção`, `Documentos Internos` (planilhas xlsx, docx, PDFs, fotos).

## 3. ARQUITETURA (importante)
App **SPA em JavaScript puro, sem framework, sem build**. Funciona offline e, quando configurado, online com sincronização.
Arquivos em `Sistema Planeta Express\`:
- `index.html` — casca: splash, sidebar, topbar, carrega os scripts. Tem `?v=NN` nos assets (cache-bust) e registro do service worker.
- `assets/estilo.css` + `assets/estilo2.css` — estilos (estilo2 tem os componentes v2+ e overrides).
- `assets/dados.js` — SEED (base semente com dados reais). Coleções: empresa, motoristas, veiculos, vencimentos, baterias, manutencoes, documentos, notas, checklists, pneus, viagens(via VIAGENS_SEED), descargas, abastecimentos, faturamento, vales, ctes, servicos, anexos, processos (ficha criminal), checklistModelo, config.
- `assets/viagens.js` — `const VIAGENS_SEED` (90 viagens jan–jun/2026 importadas das planilhas). Carrega ANTES de dados.js.
- `assets/alarmes.js` — `const ALARMES_TK` (137 alarmes Thermo King, cada um com {c, d, ex, so} = código, descrição, o que significa, o que fazer).
- `assets/arquivos.js` — `const ARQUIVOS_EMPRESA` (68 arquivos reais da pasta, com caminhos relativos `../...` — só abrem no modo LOCAL, não no site online).
- `assets/app.js` — **o motor** (~2500 linhas). Toda a lógica, views, modais. Inclui máscaras `maskCPF/maskRG/maskFone/maskCEP/maskCNPJ`, `fldMask` + listener global de pontuação automática, `registrarLeitura` (histórico de KM/horas com data) e `modalHistLeitura`.
- ~~`assets/assistente.js`~~ e ~~`assets/monitoramento.js`~~ — **APAGADOS na v7.1** (o cliente disse que não usava). Duas funções deles NÃO eram do chatbot e sobreviveram no `app.js` como **`motoristaPorNome()`** e **`veiculoPorTexto()`**: quem usa é a leitura de documentos (`_impRef`) e a Central (`cpidRelacionar`).
- `assets/central.js` — Central de Processamento de Documentos: um pipeline só para todo documento (PDF/XML/Excel/imagem/DOCX/e-mail/ZIP).
- `assets/contabilidade.js` — aba Contabilidade. **Deriva dos módulos, não duplica** (`CONTAB_FONTES`).
- `assets/relatorios.js` — engine única dos relatórios A4 (`PEXRel`). ⚠️ `PEXRelExecutar` copia uma **lista fixa** de campos do `gerar()`; campo novo tem que ser acrescentado lá também.
- `assets/importar.js` — leitor de planilhas .xlsx/.csv no navegador (module `PEXImport`). **NÃO está incluído no index.html** (recurso preparado, ainda não plugado numa tela de importação).
- `assets/nuvem.js` — camada Supabase (login, carregar/salvar DB, realtime, storage de arquivos).
- `assets/config-online.js` — lê a config online do localStorage OU usa a URL+key do Supabase embutidas (já preenchidas).
- **Marca (v7.8):** `assets/logo-claro.png` (arte branca, transparente → interface escura), `assets/logo-escuro.png` (arte escura → **relatório A4**), `assets/logo-marca.png` (só o "P" → selos de 36–44px). O `logo.png`/`logo-sm.png` são **quadrados brancos opacos** e servem só de **ícone do app**. `logo-gold.png` não é usada. `assets/fotos/m1..m7` (fotos dos motoristas).
- `manifest.json` + `service-worker.js` — PWA instalável e **atualização automática** (SW é "network-first"; nome do cache muda a cada versão: `planeta-express-v4-4`).

## 4. DADOS (persistência)
- Offline: tudo num objeto `DB` salvo em `localStorage['pex_db_v4']`. Uploads locais em **IndexedDB** (`pex_files`).
- **Preferências** (`DB.config`) também salvas em `localStorage['pex_config']` — sobrevivem a trocas de versão (não resetam).
- `ensureCollections()` faz backfill de coleções novas ao carregar (não perde dados) e roda `corrigirValoresAntigos()`. Roda também `importarCadastroSeed()`, que aplica **só a lista da versão** (`SEED_ENTREGAS`) e **uma vez por base** (`DB.seedAplicado`) — NUNCA varre a semente inteira, senão ressuscita o que o cliente apagou (foi o defeito da v6.91, corrigido na v6.92). Segunda trava: `DB.*Removidos` + `marcarRemovido`.
- Funções-chave de dados: `loadDB`, `saveDB` (salva local + nuvem debounced), `saveLocal`, `ensureCollections`, `aplicarRemoto` (recebe realtime), `flushNuvem` (salva ao fechar/trocar aba).

## 5. MÓDULOS (abas do menu, na ordem) — atualizado na v7.9
**Principal:** Painel de Controle (é a tela de ENTRADA desde a v7.2), Vencimentos.
**Cadastros:** Frota (o filtro "Todos" sai separado em **Cavalos** e **Carretas**), Motoristas, ANTT-RNTRC, Licenças e Alvarás.
**Manutenção:** KM/Horas, Trocas de Óleo, Relatório de Manutenção (`servicos`), Pneus, Baterias, Abastecimentos.
**Operação:** Viagens, Descargas, CT-e, Check-list (mapa SVG do veículo), Notas de Despesa, Pedágios, Alarmes Thermo King, Documentos.
**Financeiro** (sem senha desde a v6.21) · **Contabilidade** · **Empresa:** Quadro Societário, Código de Ética · **Sistema:** Configurações.

⚠️ **A ficha do MOTORISTA tem 5 abas:** Resumo · **Exames e Certificados** (ASO, Toxicológico, Opentech + Direção Defensiva) · Contrato de Trabalho · Ficha Criminal · Documentos.
⚠️ **A ficha do VEÍCULO** tem o card **Tacógrafo** quando é cavalo.
⚠️ **Não existem mais** as abas Início, Exames, Direção Defensiva, Tacógrafos e Monitoramento — ver a tabela no topo.

## 6. PADRÕES/CONVENÇÕES IMPORTANTES

### 🔴 AS 6 REGRAS QUE MAIS CUSTARAM CARO (todas vieram de erro real)
1. **Teste pelo CAMINHO REAL, não montando o objeto.** Entreguei um bloco no relatório com 51 asserções passando e **o cliente abriu e não estava lá**: eu montava o spec à mão e pulava justo a função que descartava o campo. Relatório se testa com `PEXRelAbrirId(id)` + `PEXRelExecutar()` conferindo o HTML de `#pexRelOv`. Formulário: abrir o modal, preencher por `getElementById().value`, chamar o `salvar…`.
2. **Backfill NUNCA varre a semente.** Só a lista da versão em `SEED_ENTREGAS`, uma vez por base (`DB.seedAplicado`). Varrer ressuscita o que o cliente apagou — já trouxe um motorista demitido de volta.
3. **Número derivado tem fonte única, e o KPI tem que bater com a tela que abre.** Se um cartão diz "Tudo em dia" e a tela que ele abre vem vazia, o cartão está errado (foi preciso escolher a *faixa* certa ao clicar).
4. **Espelho entre módulos precisa de trava contábil.** Ao fazer um lançamento aparecer em dois lugares, marque a origem (`origemPagamento`) e faça a fonte do espelho devolver `null` em `CONTAB_FONTES` — senão o dinheiro conta duas vezes.
5. **Remover recurso: `grep` das funções + `diff` das classes de CSS.** Funções guardadas por `typeof x==='function'` somem **em silêncio**; e havia regra viva (`.antt-acoes`) no meio do CSS "morto".
6. **`requestAnimationFrame` anima; quem GARANTE o número é o `setTimeout`.** Sem a rede (`_pexRedeContadores`), os 8 KPIs ficavam em "R$ 0,00".

- **Não dá para testar visualmente** no ambiente (o preview do Code fica congelado). Validar por: contagem balanceada de `{}`/`()`/backticks via grep + presença de funções + revisão. O cliente testa e manda print.
- **Dinheiro:** usar `fldR$(label,id,valor)` no formulário e `parseBRL(val('id'))` ao salvar (aceita padrão BR "50.490,84"). Exibir com `money(v)`. NÃO usar type=number para dinheiro (bug: ponto de milhar vira decimal).
- **Ícones:** `svg('nome')`; há `tipoIcone(tipo)` p/ ícone por tipo de documento. Ícones novos: idcard(CNH), flask(tox), clinic(exames), wheel(direção), taco(tacógrafo), tire(pneus).
- **Anexos com selo:** `badgeAnexo(entidade, refId, regex, categoria)` mostra "📎 Anexado" verde clicável (abre com `verArquivo`) ou botão "Anexar" (`uploadPara(ent,ref,categoria)`). Já aplicado em Tacógrafos, Direção Defensiva, Exames e CRLV (ficha do veículo).
- **Arquivos:** `todosArquivos()` mescla nuvem (`DB.anexos`, Supabase Storage) + local (IndexedDB). `subirUm(file,ent,ref,cat)` grava local + sobe à nuvem. `verArquivo/baixarArquivo` são async (blob local OU signed URL da nuvem).
- **Ao lançar nova versão:** subir o número no rodapé (`index.html`), trocar `?v=NN` de TODOS os assets, trocar o nome do cache no `service-worker.js`, reconstruir os arquivos (ver item 8), fazer `git commit`.

## 7. ONLINE (Supabase + Netlify) — ESTADO ATUAL
- **Supabase:** projeto do cliente já criado. Ref do projeto: `kxwcwpxaovwgwviqhelh`. URL: `https://kxwcwpxaovwgwviqhelh.supabase.co`. A **anon key** (pública, segura) já está embutida em `config-online.js`.
  - Tabela `dados` (id text, conteudo jsonb) guarda o DB inteiro como 1 registro (id='empresa'), com RLS p/ usuários logados e realtime ligado.
  - **Login:** Supabase Auth (email/senha). Usuários sugeridos: `uilian@planetaexpress.com` e `marcelo@planetaexpress.com` (mesma senha). A saudação "Bem-vindo Uilian/Marcelo" vem do início do email (`NOMES_USUARIO`).
  - **Storage:** bucket privado `arquivos` (o cliente PRECISA criar — PASSO A2 do `SETUP-ONLINE.txt`) p/ sincronizar os PDFs/fotos anexados.
- **Netlify:** o cliente hospedou por drag-drop (site tipo `voluble-torrone-8cedc9.netlify.app`). Está **migrando para deploy automático via GitHub** (ver item 9).
- Guias na pasta: `SETUP-ONLINE.txt` (passos A, A2, B, C, D), `COMO ACESSAR.txt`, `COMO USAR NO CELULAR.txt`, `PUBLICAR-AUTOMATICO.txt`.

## 8. BUILDS (arquivo de celular + link do artifact)
**Script recriado e salvo permanentemente:** `build_celular.sh` na pasta da empresa (`...\Planeta Express Transportes\build_celular.sh`). Gera **os dois** de uma vez:
- `Planeta Express - CELULAR.html` (**na pasta da empresa, um nível ACIMA do repo**) — arquivo único, 100% offline, tudo em base64. **~3,9 MB** na v7.9.
- `planeta_artifact.html` (no scratchpad) — sem `<head>`/`<body>`, para publicar como Artifact.
- **Como rodar:** `bash build_celular.sh` (o script já faz `export T` sozinho desde a v6.96). Extrai o corpo do index.html com `awk`, concatena os CSS e os JS na ordem (viagens, dados, alarmes, arquivos, importar, app, central, contabilidade, aniversarios, relatorios) e troca caminhos de imagem por data URIs. **NÃO inclui** supabase/nuvem/config-online.
- ⚠️ **DUAS ARMADILHAS JÁ VIVIDAS:** (1) rodar sem `T` **truncava o celular para 0 byte** — hoje o script confere antes de escrever; (2) o arquivo do celular **NÃO entra no repositório** (já entrou duas vezes, e velho) — está no `.gitignore`.
- ⚠️ O build embute **três marcas** (`uri_logo` clara, `uri_logo_dark` escura, `uri_marca` monograma) e o **ícone** separado (`uri_icon`, o quadrado opaco).
- **Link do sistema publicado (Artifact):** `https://claude.ai/code/artifact/b9498522-eeee-48ea-ad9d-c5fc06f505cf` (privado, do cliente; versão OFFLINE por-aparelho). Para atualizar no MESMO link: gere o `planeta_artifact.html` e chame a ferramenta Artifact passando `url=` esse link. **Já atualizado para v4.5.**

## 9. DEPLOY — GitHub Pages (Netlify aposentado)
**MUDOU (28/07/2026):** o Netlify ficou **sem créditos** (bloqueou novos deploys). Migramos para **GitHub Pages (grátis, sem limite)**.
- Repo publicado: **https://github.com/planetaexpress02-sys/planeta-express** (público, branch `main`).
- Remote `origin` já configurado no repo local; **push já autenticado** (Git Credential Manager guardou a credencial no PC do cliente) → **eu consigo dar `git push` direto** das próximas versões, sem ação do cliente.
- Site (depois que o cliente ativar Pages em Settings→Pages→main /root): **https://planetaexpress02-sys.github.io/planeta-express/**
- **Fluxo agora:** a cada melhoria faça `git add -A` + `git commit` **+ `git push origin main`** → o GitHub Pages atualiza sozinho. Guia em `ATUALIZAR-SEM-NETLIFY.txt`.
- Cliente NÃO tem GitHub Desktop (só git CLI 2.55). O celular (`Planeta Express - CELULAR.html`) e a pasta offline seguem atualizados localmente a cada versão.

## 10. PENDÊNCIAS / PRÓXIMOS PASSOS
- Cliente concluir: criar bucket `arquivos` (Storage), criar os 2 usuários, desligar signups públicos.
- Validar login/sync/storage reais com o cliente (nunca foi testado de verdade aqui).
- **Direção Defensiva não tem NENHUMA validade cadastrada** — os certificados existem só como arquivo na pasta. Ofereci lançar as validades a partir dos PDFs; o cliente ainda não respondeu.
- **Perguntas em aberto que ele ainda não respondeu:** (a) o KPI arredonda (R$ 2.726,00 no cartão × R$ 2.725,84 na lista) — mudar para centavos seria decisão do sistema inteiro; (b) o cadastro de veículo ainda oferece o tipo **"Reboque Frigorífico"** enquanto a tela diz "Carreta" — trocar exigiria migrar dado; (c) o dourado do splash/login é da **logo** e continua lá.
- **`_pedParseSemParar` precisa aprender o que a v6.94 descobriu:** ler o **par crédito/débito** do vale-pedágio (hoje ele pega só o débito e o vale entra como se fosse 100% reembolsado), o **nº da fatura** e os itens da capa (plano/estacionamento/créditos) para criar a entrada em `DB.pedFaturas`. Sem isso, fatura importada pela Central esconde a sobra do vale.
- Faltam dados que o cliente ainda não passou: alguns campos de cadastro (CTPS/PIS, etc.).

## 11. HISTÓRICO RESUMIDO DE VERSÕES
v1→v2: base + módulos (frota, motoristas c/ foto, exames, vencimentos, KM/Horas, manutenção, baterias, documentos c/ upload). v2.2: alarmes TK, notas, pneus, check-list. v3.0: Início, Ética, Viagens, Descargas, Abastecimentos+médias, registro de 68 arquivos, alarmes clicáveis (causa+solução). v3.3: **online Supabase** (login+sync). v3.4: atalho desktop, CNHs lidas, cadastro completo, TSP verde, status "Pendente". v3.6: logo dourada (depois revertida p/ preta em v3.8), sync instantâneo, seta voltar mobile, trocar senha, gráficos clicáveis. v3.8: pneus c/ quantidade, manutenção dividida (Trocas de Óleo + Relatório), tacógrafos só cavalos. v3.9: **sync de arquivos** (Supabase Storage). v4.0: fix "certificado anexado" só quando real. v4.2: **valor em Reais padrão BR** + Notas com pizza. v4.3: correção automática de valores antigos (bug ÷1000). v4.4: selo "Anexado" verde/clicável em Direção Defensiva, Exames e CRLV.

v4.5: **Assistente Inteligente** (chat por comandos, offline, com Desfazer); **pontuação automática** (CPF/RG/telefone/CEP via `data-mask` + listener global); check-list mostra **APROVADO (verde) / REPROVADO (vermelho)**; KM/Horas guarda **data de cada alteração** (histórico por veículo, `registrarLeitura`); trocas de óleo mostram **quanto está vencida** (km/h); pneus com **coluna Quantidade** e **% de borracha** para usados/recapados.

v4.6: assistente renomeado p/ **"Inteligência Artificial da Planeta Express"** (saudação por horário ao abrir). Ganhou **CONSULTAS** a todos os dados (pneus, KM/horas, próxima troca de óleo, vencimentos, baterias, gastos, resumo de veículo, frota, motoristas), **entendimento de texto incompleto** + placas parciais (`_iaVeiculo` fuzzy, `_iaMotorista`), e **PERGUNTA quando falta dado** (estado `IA.pending` + follow-up conversacional; `_iaFalta`/`iaFinaliza`/`iaExec`). Pneus: tirado o "×" da quantidade; **total soma as quantidades** (`pneuTotal`/`pneuQtd`, KPI "Pneus no total"). Celular + Artifact regerados; commit feito.

v4.7: **IA só aparece após login** no modo online (`iaPodeUsar`/`iaAtualizarAcesso`, chamado no init e no `aposLogin`); offline sempre disponível. **Conhecimento muito ampliado** — `iaConsulta` responde qualquer dado: ficha completa de veículo (`_iaFichaVeiculo`: chassi/renavam/ano/cor/km/óleo/pneus/bateria/documentos) e de motorista (`_iaFichaMotorista`: CPF/RG/CNH/idade/contato/endereço/exames); campos específicos (CPF, telefone, chassi, etc.); vencimentos com filtro "este mês"/"vencidos"; **alarmes Thermo King** por código (usa `ALARMES_TK`), médias (`mediaVeiculo`), viagens, descargas, gastos, contagens e busca. **Financeiro NÃO é exposto** pela IA (aponta o PIN). Desambiguação de nomes repetidos (`_iaMotoristas`).

v4.8: IA **entende frases curtas** sem pergunta formal ("cpf uilian", "chassi IRU", "telefone odecio", "km IRU") — roteamento por campo/entidade (`_iaConsultaCurta`) além das perguntas; "km IRU 1520000" atualiza, "km IRU" consulta. **Saudação minimalista** ("Boa tarde ✨ Sou a IA da Planeta Express. Como posso ajudar?"). **Importadas 111 manutenções** das planilhas de Relatório de Manutenção (cavalos + carretas) para cada veículo em `DB.servicos` — `const MANUT_SEED` no fim do `dados.js` + `importarManutencaoPlanilhas()` no `ensureCollections` (id fixo `mi_*`, importação única sem duplicar; motor do EJZ consolidado). Parser xlsx próprio em Perl (scripts `xlsxdump.pl`/`genmanut.pl` no scratchpad) — xlsx é zip; cuidado: células vazias auto-fechadas `<c .../>` quebram regex ingênua (remover antes de parsear).

v4.9: **Relatório de Manutenção reformulado** (`viewManutencao`): serviços separados por **Cavalos/Carretas**, um card por placa (`manutCardVeiculo`) com subtotal e contagem corretiva/preventiva; novo campo **Tipo (Corretiva/Preventiva)** no `modalServico`/`salvarServico` (e no `MANUT_SEED`, lido da coluna Tipo das planilhas; backfill via `importarManutencaoPlanilhas`); **gráfico de gasto por veículo** (`barChart` c/ `vtxt`/`moneyK`, cavalo azul/carreta verde) + **donut Corretiva×Preventiva**; KPIs (total, corretiva, preventiva, mês); filtro `manutFiltro` Todas/Corretiva/Preventiva. IA responde gastos por veículo/tipo.

v5.0: **Manutenção, Pneus e Baterias organizados por veículo** (estilo Frota): cada aba mostra cards de veículo (placa+especificações), Cavalos e Carretas separados; a lista só abre ao **clicar na placa** (`vcardMini`/`vcardsSecoes`/`detalheVeiculoHead`; sub-rotas `#manutencao/<id>`, `#pneus/<id>`, `#baterias/<id>` no router → `viewManutencaoVeiculo`/`viewPneusVeiculo`/`viewBateriasVeiculo`). Serviços do **mais antigo ao mais novo**, colunas **DATA / KM(cavalo) ou HORAS(carreta) / SERVIÇO / TIPO / OFICINA / VALOR**. Overview de Manutenção mantém KPIs + gráfico. Baterias fora da frota atual → seção "Outras placas" (`batGrupoBloco`/`batItem`/`_plk`). Horas das carretas migradas para o campo `km` do serviço (exibe "h"); `modalBateria(id,placa)` aceita placa e inclui placas fora da frota.

v5.1: **Botão "Relatório" (imprimir/PDF) na barra superior** (todas as telas): `imprimirRelatorio()` monta `#printHead` (empresa+CNPJ, título da tela via ROTAS+placa/nome, data) e chama `window.print()`; `@media print` esconde menu/FAB/botões e mostra o cabeçalho. **Baterias em tabela** com colunas Data / Marca / Local da compra / Valor / Trocado na garantia / Garantia (meses+até) / Telefone / Obs (`batTabela`; `batGrupoBloco` usa tabela); novos campos na bateria: `trocaGarantia` (data) e `obs`.

v5.2: **Importação de CT-e dos XML**. 36 CT-e (jun/2026) extraídos dos XML (chave, nº, série, CFOP, data, remetente/tomador, destinatário, rota origem→destino, vTPrest, produto, vCarga, xObs) → `const CTES_SEED` no fim do `dados.js` + `importarCtesSeed()` no `ensureCollections` (id=`cte_<chave>`, sem duplicar; `cteDerivaPlaca` acha a placa por "PLACA:" ou Renavam citado na obs). Aba CT-e: coluna **Rota** + destinatário; modal com origem/destino/destinatário e chave/produto/CFOP. **Botão "Importar XML"** (`importarCteArquivos`/`parseCteXml` com DOMParser) p/ importar novos CT-e no navegador. Extrator Perl `genctes.pl` no scratchpad (chave vem do NOME do arquivo — em complementares o 1º `<chCTe>` é o do CT-e original).

v5.3: **Estoque/reserva + upload de NF em PDF**. Baterias: seção "Baterias reservas (estoque)" (coleção `estoqueBaterias`: data/marca/local/qtd/valor/obs; `estoqueBateriasSecao`/`modalEstoqueBateria`). Pneus: seção "Pneus em estoque" (`estoquePneus`: +medida/DOT; `estoquePneusSecao`/`modalEstoquePneu`). Abastecimentos: botão **"Enviar NF (PDF)"** → `abastecNfUpload` lê o PDF (`pexLerPdfTexto` via DecompressionStream + `extrairAbastecimento`), abre `modalAbastec(null,pre)` preenchido (data/litros/valor/km|horas/placa/posto) p/ conferência e **anexa a NF** ao salvar (`_nfPendente`+`subirUm`). Notas: botão **"Enviar PDF"** (`notaNfUpload`) anexa ao período + sugere valor; anexo por período (`badgeAnexo('nota',...)`). **PDF é melhor-esforço** (fontes CID podem sair ilegíveis — o usuário confere). Coleções novas add em `ensureCollections` e SEED.

v5.4: Pneus com **coluna Obs** na lista por veículo; aba **KM/Horas compacta e uniforme** (`.kmgrid` minmax 250 + `grid-auto-rows:1fr`; `.kmcard` flex-column menor); **assistente sem os chips de sugestão** ao abrir (removidos `iaQuickChips`/`iaChip`/`.ia-quick` div). Tela inicial ganhou a linha "Transporte rodoviário de cargas frigorificadas". OBS: cliente pediu uma ilustração de caminhão (Volvo FH + carreta frigorífica) na tela inicial, foi feita em SVG (`svgCaminhao`) mas **ele achou péssima e mandou remover** — não recriar sem pedido explícito.

v5.5: **Leitor de PDF muito mais forte** — `pexLerPdfTexto` agora acha objetos/streams, descompacta FlateDecode, lê o mapa **ToUnicode** (`_pexParseCMap` bfchar/bfrange) e strings **hex `<..>`** (fontes CID), separa palavras por operadores de posição (Td/TD/T*/Tm); `extrairAbastecimento(txt,nome)` com regex adaptados a NF/DANFE de combustível (litros perto de DIESEL/ARLA/S10, VALOR TOTAL DA NOTA, KM/hodômetro, PLACA, data de emissão) + decodifica a chave de 44 díg do nome (`_pexChaveInfo`). Se o PDF for imagem (sem texto), avisa e mantém anexado. Pneus: KPI "Registros de pneus"→**"Pneus em estoque"** (conta `estoquePneus`); estoque de pneus ganhou campo **`localEstoque` "Local (onde está)"**. KM/Horas: placas menores (`.kmcard-h .plate`). ⚠️ **GOTCHA:** ao editar via Edit/perl, um **espaço literal dentro de regex** (ex.: `/ /` ou `/[\s ]/`) virou byte **NULL** no arquivo (grep passa a ver "binário"); use `\s` e `String.fromCharCode(32)` em vez de espaço literal.

v5.6: **CORREÇÃO CRÍTICA** — um comentário do leitor de PDF tinha `T*/Tm`; a sequência `*/` **fechou o bloco de comentário no meio**, quebrando TODO o app.js (SyntaxError → tela travava no splash, menu/conteúdo vazios). Texto do comentário trocado (sem `*/`). ⚠️ **LIÇÃO:** nunca escreva `*/` dentro de comentário `/* */` (ex.: "T*/Tm"). ⭐ **NOVA CAPACIDADE DE TESTE:** com o site no **GitHub Pages** (URL http real), dá para VALIDAR de verdade no navegador (mcp Browser): `preview_start` na URL, `javascript_tool` com `fetch('assets/app.js').then(t=>new Function(t))` pega erro de sintaxe com linha, e `typeof init/router/DB` + `#nav childElementCount` + `#splash` confirmam se o app inicializou. (curl do bash NÃO tem internet aqui — status 000; usar o Browser.)

v5.7: Aba **Pneus** com banner azul; KPI "Pneus em estoque" clicável (`pneuIrEstoque` → scroll até `#sec-estoque-pneus`); no `modalPneu` (novo) select **"Tirar do estoque"** que dá **baixa automática** na qtd do `estoquePneus` ao salvar (em `salvarPneu`); campo Posição vira input com **datalist** (Dianteira/Tração/Traseira/Estepe 1/Estepe 2 → dá p/ cadastrar 2 estepes na carreta com qtd 2). **Descargas por mês** (`descMes` + `<select>` de meses + banner + subtotal). KM/Horas: cards menores (`.kmgrid` minmax 208). **Validado no navegador via GitHub Pages** (parse OK, init OK, views sem erro).

v5.8: **Importação de planilhas ligada** (o motor `importar.js` finalmente plugado numa tela). `importar.js` agora entra no `index.html` (antes de `app.js`) e no `build_celular.sh`. Novo botão **"Importar planilha"** na barra de Vencimentos → `modalImportar()`: escolhe .xlsx/.csv, chama `PEXImport.lerArquivo` + `PEXImport.detectarVencimentos` em cada aba, e monta uma **prévia conferível** (`importarLerArquivo`/`importarRender`). Cada linha resolve o vínculo automaticamente (`_impRef` usa `_iaVeiculo`/`_iaMotorista`), mostra situação (Novo/Atualiza/Já existe/Sem vínculo/Sem data) com `<select>` para corrigir o registro (`importarSetRef`), checkbox por linha (`importarToggle`). `importarConfirmar` insere os novos e **atualiza a validade** dos já existentes (mesmo tipo+vínculo+registro), ignorando idênticos. ⚠️ **GOTCHA importante:** `parseD` só aceita ISO `AAAA-MM-DD`; datas em texto BR ("dd/mm/aaaa") da planilha **quebram** se salvas cruas — normalizar com `_impISO` (converte BR/2-díg → ISO, rejeita inválidas) ANTES de gravar. Validado no navegador (file://): app inicializa, detector acha os blocos, datas convertem, placa/motorista casam, modal renderiza e conta certo, zero erro de console.

v5.9: **Início do Enterprise 2.0 — "Painel de Comando" dark premium.** Depois de muitas iterações de direção visual (protótipos como Artifacts: relatório de reengenharia, painel organizado, centro de comando HUD, home 3D com caminhão holográfico WebGL nativo), o cliente **decidiu**: quer o **painel organizado dark premium** (estilo da imagem de referência que ele mandou — cards + gráficos + mapa), **NÃO** o caminhão 3D; e **aceitou sistema online/remoto** (o que já temos via Supabase + GitHub Pages). **Stack mantida vanilla/offline** (ele não tem Node; R3F/Three exigiria build+hospedagem paga e quebraria o "abrir offline"). Implementado: `viewPainelPro()` no app.js — nova **tela de abertura** (rota `dashboard` virou landing: `location.hash||'#dashboard'`), overlay dark full-screen com **classes prefixadas `pc-`** (zero colisão com o CSS light atual, tudo scoped em `.pc-app`, CSS no fim do estilo2.css). Lê **dados reais**: KPIs (frota 10, motoristas, vencimentos), gráfico de performance (viagens/mês real, linha que se desenha), donut de atividade (% concluídas), próximos vencimentos, visão da frota, mapa (rastreio "preparado"), financeiro **honesto** (Faturamento CT-e R$166k + ticket + manutenção R$350k reais; "Contas a pagar/receber: A configurar"). Demais módulos **intactos** (só a abertura mudou). Validado ao vivo no GitHub Pages: render OK, dados reais corretos, zero erro de console. **Automação real (e-mail/OCR entrando sozinho) é a próxima etapa — precisa de backend (Supabase Edge Functions).** ⚠️ Ainda NÃO consigo tirar screenshot aqui (o painel do navegador não compõe quadros) — validação é estrutural + dados; o visual quem confirma é o cliente.

v6.0: **Tema DARK futurista no sistema inteiro.** O cliente mandou uma imagem-brief pedindo "atualizar TODO o sistema para a nova identidade visual" (todas as telas, tabelas, formulários, modais, relatórios) mantendo 100% dos dados/funcionalidades/regras. Feito **sem tocar em markup nem lógica** — só CSS: no fim do `estilo2.css`, um bloco "TEMA DARK FUTURISTA" que **redefine os tokens `:root`** para a paleta escura (--content #070d15, --card #0d1524, --text #eaf1f9, --accent #4c8dff neon, semânticas translúcidas) + **override de cada superfície clara "chumbada"** que não usava token (topbar, botões, tabelas th/hover, modais + footer, inputs/select/textarea/selectlite/kmcard/alarme, seg, filecards, check-list chk-opt, alarme-block, truckmap, grouprow/bat-group, código de ética, atalhos do início, borracha-badge, e todo o painel do assistente IA). KPI k-val ganhou gradiente claro (o gradiente escuro sumiria). O Painel de Comando (pc-, v5.9) já era dark e continua separado/scoped. Validado ao vivo no GitHub Pages: body #070d15, cards/topbar/botões/modais/inputs escuros com texto platina, dashboard OK, zero erro de console. ⚠️ Login e `#printHead` (impressão) seguem como estavam (login já era dark; print continua branco de propósito). Próximo provável: refinos visuais pontuais que o cliente apontar por print + a trilha de automação real (backend Supabase).

v6.1: **Refinos dark** — glow neon no botão primário/nav ativa/foco de campos/ícones, profundidade nos cards, e **splash (abertura) + login migrados de dourado para azul neon** (coerência da identidade). Só CSS (bloco "REFINOS v6.1" no fim do estilo2.css).

v6.2: **Centro de Automação — caixa de entrada inteligente (offline/grátis).** Cliente pediu "a melhor automação que puder, para continuar grátis". Como e-mail/OCR de verdade exigiria backend pago, entreguei a automação que roda 100% no navegador reaproveitando os motores existentes: nova rota `#automacao` (`viewAutomacao`, item no menu Operação com **badge de pendências**). Solta documentos (drag-drop/clique) → `automacaoProcessar`→`_autoUmArquivo` trata cada um: **XML CT-e** → importa direto (dedup, `parseCteXml`); **planilha** → aplica validades com vínculo (`PEXImport.detectarVencimentos`+`_impISO`/`_impRef`), sem vínculo→pendência; **PDF** → `pexLerPdfTexto`+`extrairAbastecimento`, classifica abastecimento/pedágio/doc, arquiva (`subirUm` 'empresa') + pendência; **imagem** → arquiva+pendência. Coleções novas em `ensureCollections`: `automacaoLog` (histórico, máx 300), `automacaoPend`, `automacaoRegras` (remetentes: posto/pedágio/cliente — `modalRegra`). Pendências: **Revisar** (abre form pré-preenchido, ex. `modalAbastec(null,pre)`) e **✓ Concluir**. CSS `.autodrop`. Validado ao vivo: render OK, CT-e simulado importado e revertido sem sujar dados, menu OK, zero erro. ⚠️ **E-mail automático e OCR de imagem ainda precisam de backend** — por ora "salve o anexo e solte na caixa".

v6.3: **Abertura MAP-FIRST + menu plano + dourado.** Cliente pediu reconstrução da tela inicial com o **mapa como elemento principal**, menu **sem categorias** (lista exata), **dourado** para financeiro, **tudo clicável**, menos texto. Feito `viewComando()` (novo, substitui `viewPainelPro` na rota dashboard — pc- scoped, fixed overlay): mapa SVG das **8 cidades da região de Londrina** (Paiçandu→Ibiporã) com **rotas e veículos animados** (`animateMotion`), **clique no veículo abre painel** (`pcAbrirVeic`/`PC_FLEET`/`#pcVeicPanel`); faixa inferior de **KPIs clicáveis** (Frota/Viagens/Faturamento[dourado]/Alertas → rotas); copiloto **PLANETA AI**; header minimalista (sem subtítulos). Menu plano no `.pc-side` e o **menu principal (`renderSidebar`) achatado** na ordem pedida (Monitoramento, Viagens, Motoristas, Frota, Clientes, Financeiro, Vencimentos, Relatórios, Documentos, Automação, Abastecimentos, Pedágios, Analytics, Config) + grupo "Mais" para o resto (senão ficariam inacessíveis). Telas novas com rota: **Clientes** (real, consolida CT-e por cliente), **Pedágios** (liga à Automação + log), **Analytics** (barChart viagens/mês + donut manutenção), **Relatórios** (hub de impressão). Dourado via tokens `--gold`/`--gold-2` no `.pc-app`. Validado ao vivo: mapa (8 cidades/5 veículos/3 rotas), tile dourado, painel de veículo abre/fecha, 4 telas novas OK, zero erro. ⚠️ Mapa é **estilizado** (não geográfico exato); GPS ao vivo/ETA seguem "aguardando rastreador" (camada preparada). `viewPainelPro` (v5.9/6.0) virou código morto (mantido, inofensivo).

v6.4: **Mapa ajustado + OCR real.** Cliente pediu: mapa só com **Londrina (rótulo "BASE"), Maringá, Paiçandu, Cambé** (removidas as demais) e **voltar o monitoramento para janela menor** (não tela cheia). Feito: rota `dashboard` voltou para `viewPainelPro` (painel organizado com card "Monitoramento" pequeno), agora com **menu plano** (`navItem` ganhou param `cls`; `.pc-nav.pc-flat`, Financeiro dourado, IA via `iaToggle`, ícones novos no `_pcIco`: map/clients/wallet/toll/chart/fuel). Mapa do card refeito com as 4 cidades rotuladas + Londrina "BASE" destacada + 3 veículos animados. `viewComando` (tela cheia) virou código morto. **OCR** conforme o fluxo do cliente: `pexRenderPdfImg` (pdf.js renderiza 1ª página em canvas) + `pexOCR` (Tesseract 'por'); ligados na Automação (`_autoUmArquivo`: PDF sem texto→OCR; imagem→OCR direto→`extrairAbastecimento`) e em `abastecNfUpload` (PDF digitalizado→OCR antes de preencher). Libs via CDN com `defer` no index.html (pdfjs-dist@3.11.174 + tesseract.js@5.1.0) — **só funcionam online**; offline degrada com segurança (typeof guard → pula OCR). Validado ao vivo: dashboard=painel, mapa [MARINGÁ,PAIÇANDU,CAMBÉ,BASE], menu plano 14 itens + gold, pexOCR presente, zero erro. ⚠️ **Não deu para testar a qualidade do OCR aqui** (precisa online + doc escaneado real + download das libs ~15MB sob demanda) — cliente valida com uma nota escaneada real.

**REVERSÃO (importante):** depois da v6.4, o cliente pediu para **voltar à v5.8** — antes de toda a reconstrução visual que veio dos prompts grandes (estilo ChatGPT). Fiz a volta restaurando os arquivos do commit `92c9935` (v5.8) num novo commit `52e7407` — **sistema voltou ao tema CLARO + importação de planilhas**, exatamente como na v5.8. **Nada foi apagado:** toda a versão escura (v5.9→v6.4: Painel de Comando, dark theme, mapa, Automação, OCR, menu plano) está **preservada no histórico do git no commit `38d3479`** e pode ser restaurada quando o cliente quiser (`git checkout 38d3479 -- .` + commit). Validado ao vivo: tema claro (body #eef2f7), rodapé v5.8, menu original, zero erro. As entradas v5.9→v6.4 abaixo/acima ficam como **registro do que existe no histórico**, NÃO do que está no ar.

v6.5: **Evolução visual premium (tema CLARO) sobre a v5.8.** Depois de reverter para a v5.8, o cliente pediu para **EVOLUIR** (não reconstruir) a interface clara: manter tudo (estrutura, DB, rotas, componentes, lógica), só elevar aparência/UX. Feito 99% por **CSS** (bloco "EVOLUÇÃO VISUAL PREMIUM v6.5" no fim do estilo2.css): tokens refinados (--content off-white acinzentado #e9edf3, --card #fcfdff, azul corporativo #1f5fdc, **--gold** #b0812c só p/ financeiro, grafite), mais respiro (content 32px, gaps 20px, cards 22px), cards/KPIs com sombra em camadas + brilho no topo (::before) + elevação no hover (KPI translateY; card só sombra p/ não quebrar sticky th), botões premium (gradiente/hover/active/focus/`.btn.loading`), inputs com foco, tabelas com hover refinado, gráficos animados ao carregar (donut pop, barras crescendo), microinterações fade-up nos cards ao renderizar, `.skeleton` utilitário. **DOURADO** só no Financeiro via classe `fin-gold` (único retoque de markup: 1 classe no grid de KPIs de `viewFinConteudo`). Respeita `prefers-reduced-motion`. Validado ao vivo: tema claro intacto (body #e9edf3, texto grafite), tokens/padding/sombras aplicados, rodapé v6.5, zero erro. ⚠️ NÃO implementado (próximos passos, precisam de JS/markup): mapa no dashboard (v5.8 não tem), tabelas com ordenação/busca/paginação, tooltips/zoom nos gráficos, skeletons de fato ligados. Continua sem screenshot aqui — validação estrutural + computed styles.

v6.6: **Aprimoramentos de UX (tema claro)** — todos pós-render, sem mexer nas telas/lógica (`pexAfterRender` chamado no fim do `router`): **tabelas premium** (`pexEnhanceTables`: busca instantânea + ordenação por coluna `_pexKey` + paginação 12/pág, injetadas em qualquer `table.tbl` com ≥6 linhas); **tooltip** global elegante (`pexTipInit`, atributo `data-tip` em barras/donut/veículos); **barra de loading** no topo em uploads (`pexBar` em `processUpload`); `.btn.loading`. CSS no bloco "APRIMORAMENTOS UX v6.6" do estilo2. Impressão preserva todas as linhas.

v6.7: **Fix ordenação** — `_pexKey` só trata como número quando NÃO há letras (placas tipo IPD-9036 ordenam como texto; datas dd/mm/aaaa e dinheiro R$ continuam certos).

v6.8: **Mapa de monitoramento na tela de INÍCIO** (topo, antes de tudo), a pedido do cliente. Só **4 cidades**: Londrina (com selo **"BASE"** abaixo), Maringá, Paiçandu, Cambé. Mais moderno: anéis pulsantes na base (`.pex-hubring`), rota em gradiente, marcadores com glow, 3 veículos animados com tooltip, skeleton "conectando". **Removido do Dashboard** (estava lá na v6.6). Card `.pex-mapcard`/`.pex-map-hero` no início do `viewInicio`. Validado ao vivo: mapa é o 1º elemento da Início, cidades [PAIÇANDU,MARINGÁ,CAMBÉ,LONDRINA+BASE], fora do dashboard, zero erro. ⚠️ Mapa é estilizado (posições aproximadas); rastreio real = camada futura.

v6.9: **Tela de Início reorganizada** (a pedido): logo/nome (`ini-hero`) no **topo**; abaixo, `div.grid.ini-mon` (grid 1fr/1fr) com **Monitoramento** (card `.pex-mapcard`, título só "Monitoramento") na **metade esquerda** e à **direita** os KPIs **Veículos ativos** + **Motoristas ativos** (`.ini-kpis` flex-column). **Removidos da Início:** card "Acesso rápido" e card "Código de Ética" (a página/rota Ética e o item de menu continuam existindo — só saíram da Início). Mapa reajustado p/ viewBox 900×400 (cabe em meia tela): 4 cidades (Londrina+BASE, Maringá, Paiçandu, Cambé). Validado ao vivo: hero no topo, título "Monitoramento", mapa à esquerda, KPIs à direita, Acesso rápido/Ética fora, zero erro. `atalho()` em `viewInicio` ficou sem uso (inofensivo).

v6.10: **Monitoramento PREMIUM** (a pedido: "tá pálido, quero premium"). O card Monitoramento virou um **mapa ESCURO control-room** dentro da Início clara (contraste rico): fundo navy `linear-gradient(#102a46,#0a1626)` + glows azul/ciano, traçados de rota em gradiente `#4c8dff→#37e3d0` com glow animado, cidades brilhantes, hub Londrina com anéis, **selo BASE dourado** (`#pexGold`). **5 caminhões animados** (ícone `<g id=pxtruck>` via `<use>`, `.pex-truck`) em vez de 3 pontos, cada um com tooltip (placa·motorista→cidade) e clique→viagens. **Botão "Viagens" de volta** no cabeçalho. Removida a tagline "Transporte rodoviário de cargas frigorificadas" do `ini-hero`. CSS do mapa escuro no fim do estilo2 (scoped `.pex-map-hero`, sobrepõe o claro anterior). Validado ao vivo: 5 caminhões, botão Viagens, sem tagline, mapa escuro, selo gold, tooltips, zero erro.

v6.11: **HOME = Centro de Comando Executivo** (só a Início; resto do sistema segue claro). Inspiração SpaceX/Tesla/Apple/aviação. Tudo **scoped em `.ini-cmd`** (tema escuro grafite/petróleo/vidro via `margin:-32px` p/ full-bleed dentro do `.content`). Componentes novos (só visual, funcionalidade intacta): **header executivo** (logo + "Centro de Comando" + relógio `#iniClock` ao vivo, menos texto); **Monitoramento protagonista** (mapa `viewBox 1200x440`, largura total); **veículos = marcadores discretos `.ini-veh` SEM placa/nome** (só ícone), **clique abre painel lateral** `#iniVeicPanel` (`iniAbrirVeic`/`INI_FLEET`/`iniFecharVeic`) com placa, motorista, cliente, origem→destino, status, velocidade, última atualização, e links Documentos/Abastecimentos/Pedágios; **KPIs em vidro** (`.ini-kpi`) com **count-up animado** (`iniCountUp` via requestAnimationFrame, chamado no `pexAfterRender` p/ rota inicio), **sparklines**, indicador lateral colorido, dourado no Faturamento, todos clicáveis. Removidos Acesso rápido/Ética/tagline. CSS no fim do estilo2.

v6.12: **Fix faturamento da Home** — `c.valor` do CT-e já é number; o parser inline removia o ponto decimal (×100, dava R$5.782k). Agora usa o número direto quando `typeof==='number'` → **R$166k** correto (comprovado: soma=166205). ⚠️ count-up usa rAF (pausa com aba oculta) — anima normal p/ o usuário.

v6.13: **Início — removidos os KPIs Viagens e Faturamento** (cliente não pediu). Ficaram só **Frota ativa** e **Motoristas** no `.ini-kstrip` (grid 2 colunas). `viagens`/`cteSum`/`cteK` no `viewInicio` ficaram sem uso (inofensivos).

v6.14: **Painel de Controle no estilo Comando** (mesmo visual da Início). `viewDashboard` agora: wrap `.ini-cmd .ini-dash` (dark/vidro), header executivo (`ini-top` com relógio `#iniClock`), **8 KPIs em vidro com count-up** (helper GLOBAL `iniKpiTile`, `iniCountUp` roda no `pexAfterRender` p/ rota inicio E dashboard), sparklines, dourado/crit conforme o caso; **gráficos legíveis no escuro** (overrides `.ini-cmd .donut-c/.donut-s/.bar-val/.bar-lbl/.legend/.alert-row`) e **barras que crescem ao carregar** (`iniBarGrow` via transform-box fill-box). **Removido "Próximos vencimentos"**; "Situação dos vencimentos" virou largura total (`grid` em vez de `two-col`). Dados/lógica intactos. CSS no fim do estilo2 ("PAINEL DE CONTROLE no estilo Comando v6.14").

v6.15: **Relatório de Manutenção — DESIGN PREMIUM (evolução visual, sem tocar em lógica/dados/rotas).** Resposta ao briefing "Evolução do Design System" (software corporativo premium, tema claro, paleta #F4F6F8/#FFFFFF/#E5E7EB/grafite, dourado só p/ financeiro). Tudo **escopado em `#view[data-route="manutencao"]`** — zero impacto nas outras telas. Como foi feito: novo gancho em `pexAfterRender` que faz `#view.setAttribute('data-route',rota)` (permite CSS por tela sem mexer no markup das views) + bloco CSS "RELATÓRIO DE MANUTENÇÃO — DESIGN PREMIUM (v6.15)" no fim do `estilo2.css`. Mudanças: **banner escuro → header claro premium** (gradiente branco, borda #E5E7EB, texto grafite, chip azul do ícone); **KPIs Gasto total/Corretiva/Preventiva viram FILTROS clicáveis** (helper local `kpiF`, seta `k-go`, destaque `.ativo`) — o do mês (`i-slate`) é leitura; **donut** mostra **% de corretiva** no centro + legenda com valores (`.mnt-legend`); **barras "gasto por veículo" clicáveis** (`d.js` → abre a planilha do veículo); **rodapé dos cartões de veículo** (`foot`/`.mnt-cardfoot`) com custo + **barra corretiva×preventiva** (`.mnt-mix`); **tabela de detalhe** (`viewManutencaoVeiculo`) com valores à direita (`.ta-r`) + **linha de TOTAL GERAL** (`<tfoot>.mnt-total`, soma real independente da paginação) + KPIs com **% do total**; **responsividade dos KPIs corrigida** (o grid tinha `style` inline `repeat(4,1fr)` que ignorava o CSS → overrides `!important` por media query no escopo). Também **corrigido ícone ausente `cal`** (o KPI "Gasto no mês" aparecia sem ícone em 4 lugares) + adicionados ícones `trend` e `coins` ao mapa `IC`. **Validado AO VIVO** abrindo o `index.html` local no mcp Browser (`file://`): app inicializa, `data-route=manutencao`, fundo `rgb(244,246,248)`, banner grafite/borda #E5E7EB, 3 KPIs-filtro funcionando, donut 71% (dado real), total R$45.439,65 = soma exata, **zero erro de console**.

v6.16: **TEMA CYBER na aba Início + Painel de Controle (neon azul, evolução do dark, sem tocar em lógica/dados/organização).** Cliente mandou uma imagem-briefing "PROMPT PROFISSIONAL — TEMA CYBER" (cyber/futurista/imersivo, neon azul; cards animados com glow, gráficos que "carregam sozinhos", ícones/sombras/detalhes; **regras:** manter funcionalidades, não alterar dados/estrutura/lógica, só aplicar o design, responsividade + performance). Como as duas telas já eram escuras (`.ini-cmd`), foi uma **evolução** rumo ao neon do briefing. Feito 99% em CSS: bloco **"TEMA CYBER — ABA INÍCIO + PAINEL DE CONTROLE (v6.16)"** no fim do `estilo2.css`, escopado em `.ini-cmd` (cobre `viewInicio` e `viewDashboard`) — resto do sistema (claro) intacto. Paleta: neon `#00E5FF`/elétrico `#0077FF`, fundo radial `#0A1A2F→#001117`, sucesso `#00FF88`, atenção `#FFB020`, crítico `#FF3B30`, texto `#E6EAF2`, fonte `Inter`/`Sora` (fallback Segoe). Efeitos: cards com borda iluminada `rgba(0,229,255,.1)` + glow `0 0 20/40px` + **hover que intensifica**; KPIs em vidro neon com **contagem de números** (já existia, `iniCountUp`) + **entrada em cascata** (`cyIn` + stagger nth-child) + barra de acento e ícones com glow; **gráficos carregam sozinhos** — sparklines **se desenham** (add `pathLength="1"` no markup de `iniKpiTile`/`viewInicio` + `@keyframes cySpark` via `.cy-spark-line`), donuts aparecem (`pexPop`), barras crescem (`iniBarGrow`), tudo com brilho (`drop-shadow`); badges de status neon (`.st.ok/.crit/.warn/.neutro`), ícones do cabeçalho ciano (precisou bater a especificidade `.ini-cmd .card .card-h>svg`), relógio/live com glow, mapa com rotas/veículos neon (gradiente `pexRg` → `#00e5ff→#0077ff`), painel lateral do veículo neon. Respeita `prefers-reduced-motion`. **Validado ao vivo** (file://, ABA NOVA — ver pegadinha abaixo): Início e Painel com fundo `rgb(0,17,23)`, borda `rgba(0,229,255,.1)`, `cyIn`/`cySpark` ativos, fonte Inter, ícone do card ciano `rgb(0,229,255)`, 5 veículos neon; **Manutenção (#F4F6F8) e Frota seguem claras**; **zero erro de console**. Só CSS + 2 markup mínimos (pathLength, gradiente). ⚠️ Fonte Inter/Sora só aparece se a máquina tiver instalada (senão cai p/ Segoe) — embutir a fonte em base64 é um próximo passo possível.

v6.17: **TEMA CYBER também no Relatório de Manutenção (unifica o neon).** Cliente pediu "faça o mesmo estilo em relatório de manutenção" logo depois do cyber da Início/Painel. Converti o bloco CSS de Manutenção (que era claro/premium na v6.15) para **cyber**, escopado em `#view[data-route="manutencao"]` (bloco "RELATÓRIO DE MANUTENÇÃO — TEMA CYBER (v6.17)" no estilo2.css). **Manteve 100% da estrutura/funcionalidade da v6.15** (KPIs-filtro clicáveis, donut com % de corretiva, barra corretiva×preventiva nos cartões, linha de TOTAL GERAL, busca/ordenação/paginação) — só trocou a "roupa" clara pela neon. Como a tela usa componentes DIFERENTES das telas `.ini-cmd` (`.banner`, `.kpi`/`.k-ico`, `table.tbl`, `.vcard`, `.seg`, `.st`, `.detail-head`, `.pex-tbar`), tive de cyber-ficar cada um: fundo radial `#0A1A2F→#001117`, cards/banner/KPIs/vcards em vidro neon (borda `rgba(0,229,255,.1)` + glow + hover), ícones ciano com glow, tabela com **cabeçalho escuro** (override do `table.tbl th` claro do v6.6) + hover neon + total neon, busca/paginação repaginadas p/ o escuro, badges de status neon, **botão primário neon**, fonte Inter/Sora. No `app.js` mudei só as CORES do donut/barras do overview p/ ficarem vibrantes no escuro (corretiva `#f5a623`, preventiva `#16c98d`, barras `#2f8fff`/`#1fd4c4`). **Validado ao vivo** (aba nova): overview e detalhe cyber (fundo gradiente, banner `rgba(0,26,47,.6)`, ícone `rgb(0,229,255)`, th `rgba(0,18,32,.7)`, total `R$45.439,65` preservado, filtros funcionando); Início/Painel seguem cyber, **Frota/Vencimentos seguem CLARAS**; **zero erro de console**.

v6.18: **Menu Lateral Premium.** Briefing "Evolução do Menu Lateral" (SpaceX/Tesla/Stripe/Linear/Palantir, mantendo identidade Planeta). Só design/UX, sem tocar em rotas/permissões/módulos. Bloco CSS "MENU LATERAL PREMIUM (v6.18)" no estilo2.css: ícones refinados (21px, `.nav a .ico`), hover elegante + micro-animação no ícone (scale+cor neon), **item ativo com gradiente azul suave + LINHA NEON animada** (`.nav a.active::before` com `@keyframes navBar` height 0→62%) + glow discreto + ícone ciano; categorias com separadores (`.group::before`); **badge de alerta vermelho pulsante** (`@keyframes navPulse`); **RECOLHER para trilho só-ícones** — `toggleRail()`/`applyRail()` (estado em `localStorage['pex_rail']`), botão `.rail-toggle` (chevron na borda do index.html), largura 264↔76px com transição, textos somem, **tooltip** `::after` com `data-label` (add em cada item do `renderSidebar`); só desktop (`@media min-width:961`), escondido no mobile. Validado: 25 itens/7 grupos, ativo neon, badge pulsando, recolher 264↔76 confirmado (sem transição=76), tooltip "Início", persiste; zero erro.

v6.19: **Cockpit Superior (barra superior operacional).** Briefing "Cockpit Superior". Novo módulo no `app.js` (seção "COCKPIT SUPERIOR"), sem mexer em rotas/dados. **Busca global** `pexSearch(q)` (placa/motorista/CT-e/nota/cliente/fornecedor(oficina)/viagem/documento/manutenção/pneus). **Paleta de comandos `pexCmdOpen(mode)`** (Ctrl+K; overlay `#pexCmd` em vidro escuro cyber): Ações rápidas `PEX_CMDS` (Novo veículo/motorista/viagem/abastecimento/serviço/CT-e/checklist/nota/descarga/relatório → chamam os `modal*` reais) + resultados de busca; setas+Enter, `>` = modo comando. **Atalhos** Ctrl+K/Ctrl+N/Ctrl+/ (`pexCockInit`). **Central de notificações** (`pexNotifData`/`pexNotifToggle`, sino `#cockBell` + badge `#cockBadge`): Críticos/Avisos dos vencimentos reais (situacao ord 0/1) + Informativos; dropdown `#cockNotif` cyber. **Clima Londrina** `pexWeather()` (Open-Meteo, sem chave; cache 30min em `localStorage['pex_weather']`; degrada offline escondendo o chip). **Relógio** `cockTick()` (1s, `#cockTime`/`#cockDate`). **Status** `pexCockStatus()` (online/local honesto). Topbar reescrita no `index.html` (busca `#gsearch` virou botão `.cock-search` que abre a paleta + cluster `.cock-cluster`); `pexCockInit()` no init; `pexNotifBadge()` no `pexAfterRender`; listener antigo do `#gsearch` protegido com `if(s)`. CSS "COCKPIT SUPERIOR (v6.19)": barra clara com acentos neon + paleta/notif em vidro escuro; responsivo. Validado ao vivo: Ctrl+K abre, comando abre "Novo veículo", busca IPD=5/jonathan/difal, notif 2 crít+2 avisos (badge 4), relógio tickando, clima oculto offline, status "Local"; zero erro. ⚠️ Clima só online (fetch externo); na versão Artifact o CSP bloqueia o fetch → chip some (ok).

v6.20: **Tema CYBER no RESTANTE do sistema (exceto Viagens e Descargas).** Cliente: "mude todo o restante do sistema para estilo atual, EXCETO viagens e descargas". Feito com uma **classe `.cyber` no `#view`** ligada no `pexAfterRender` para TODAS as rotas MENOS `{viagens, descargas, inicio, dashboard, manutencao}` (viagens/descargas ficam claras = a exceção; início/painel já têm `.ini-cmd` e manutenção já tem bloco `data-route` — excluídos p/ não interferir). Novo bloco CSS **"TEMA CYBER GLOBAL (v6.20)"** escopado em `#view.cyber`, cobrindo TODAS as superfícies (usei o inventário do tema dark v6.0 do commit `38d3479` como checklist): tokens neon + fundo radial; banner/cards/KPIs/vcards vidro neon; tabelas (th escuro `rgba(0,18,32,.72)` + hover neon); badges/tags/plate; **formulários inline** (input/select/textarea/selectlite/alarme-input/kmcard); seg; gráficos (donut/barras/legend/alert-row); alarmcard/alarme-block, filecards, check-list (chk-list/row/opt/truckmap), baterias (grouprow/bat-group), Código de Ética, pex-tbar; botão primário neon; **dourado do Financeiro preservado**; impressão força papel branco. Só CSS + 1 linha no `pexAfterRender`. Validado ao vivo (?v=80): Frota/Vencimentos/Alarmes/Baterias/Financeiro cyber; **Viagens/Descargas claras** (th `rgb(243,246,251)`); Início/Painel/Manutenção intactos; zero erro. ⚠️ **Modais seguem CLAROS** (em `#overlay`, fora do `#view.cyber`) — igual já era na Manutenção; se o cliente quiser modais cyber, é um bloco `#overlay .modal` à parte (a lista de overrides de modal está no v6.0 do commit `38d3479`).

⚠️ **ONDE PARAMOS (atual — v6.42 implementada, validada ao vivo e PUBLICADA no GitHub Pages; celular regenerado; git limpo).** v6.42 = **MONITORAMENTO = COMMAND CENTER (só visual; dados/lógica intactos).** Redesenho do mapa da Início (`viewInicio`) inspirado na imagem-briefing do cliente (SpaceX/Palantir/NASA): grade **hexagonal** (`<pattern>`) + grid digital, base **LONDRINA** com anéis de **radar** pulsando (`.cc-radar` scale+opacity) + feixe girando + núcleo energético (`.cc-core`), **rotas neon** (`.cc-route` glow) com **partículas correndo** (`.cc-flow` = `<use>` das rotas com `stroke-dashoffset` animado), **veículos tecnológicos** (`#iniTruck` com corpo/cabine/farol/lanterna + glow + bob), **cidades** com marcador+glow+nome+selo ONLINE (Maringá/Paiçandu/Cambé/Ibiporã), **rótulos de rodovia** (PR-323/445/369/090 — decorativos), e **controles** (+/−/centralizar/camadas) em vidro com ripple no canto inf. esq. **Mantidas 100%:** rotas `pxr1..pxr4` + `animateMotion`, os **cavalos reais** (`iniCavalos`), o clique→painel lateral `iniAbrirVeic` com dados reais, o skeleton. Novos helpers `iniMapZoom` (zoom real ao redor de Londrina) e `iniBaseWx` (clima REAL de Londrina no chip da base, do cache `pex_weather` — mesma fonte do cockpit; NADA inventado). Bloco CSS "COMMAND CENTER — MONITORAMENTO PREMIUM (v6.42)" no fim do estilo2.css. **Validado ao vivo (1360px e 375px):** 3 radares, 4 rotas + 4 fluxos, 4 cidades, 4 rodovias, 5 caminhões reais, 4 controles, base LONDRINA, clima 22° injetado, zoom ok, **clicar no veículo abre o painel** (lógica preservada), sem overflow no mobile; zero erro. v6.41 = **TOPBAR SEM BARRA (só DESKTOP ≥861px) — cápsulas flutuantes.** O cliente não queria mais a "barra"/retângulo no topo. Feito só com CSS (bloco "TOPBAR SEM BARRA — CÁPSULAS FLUTUANTES (v6.41)" no fim do estilo2.css): (1) `.main` ganhou o **fundo cyber** (radial navy `#0a1a2f→#020a14` + glows) — antes o `body` era claro e apareceria atrás de um topbar transparente; (2) `@media(min-width:861px)` o `.topbar` fica **transparente** (sem background/borda/sombra/blur — `!important` p/ vencer o v6.32) → some a faixa, fundo contínuo com o conteúdo; (3) cada componente (`cock-search`, `cock-ico`, `cock-weather`, `cock-clock`, `cock-status`, `relbtn`, `userbadge`) vira **cápsula de vidro** exatamente como pedido: `bg rgba(12,20,34,.45)` + `blur(18px)` + `border rgba(0,170,255,.12)` + `radius 18px` + `shadow 0 0 18px rgba(0,170,255,.08)`; hover com elevação+glow (180ms), active com pressão; **pesquisa** maior (52px, foco iluminado), demais 48px, textos claros (clock/status/relatório/perfil legíveis no vidro), badge da notificação com anel escuro. **Mobile (≤860) NÃO foi tocado** (topbar v6.34 com barra + menu ☰ + marca). **Validado ao vivo (1360px):** topbar transparente sem borda/sombra, `.main` com gradiente cyber, cápsulas `rgba(12,20,34,.45)` radius 18 (search 52px), textos claros; em 375px o topbar segue v6.34 (bg `rgba(6,15,27,.82)`, menu/marca visíveis); zero erro. v6.40 = **NOVA EXPERIÊNCIA DA IA PLANETA (só visual; cérebro 100% intacto).** A IA virou uma **esfera tecnológica viva** no canto inf. direito (não é botão): CSS glassmorphism azul-neon com brilho interno, glow, reflexos, anel de luz e **respiração** (`orbBreath` 4.2s, ±2%), hover que aumenta+glow+legenda "Sistema operacional online". Ao clicar, o chat **nasce da esfera** (transform-origin bottom-right, scale .42→1 + blur→0 + fade, ~420ms). Painel **460×700**, vidro escuro (`backdrop-filter blur`), cabeçalho "IA PLANETA / Assistente Operacional Inteligente" + status **● Online (pulsa) · Sistema sincronizado**; mensagens do bot viram **painéis de vidro com glow** (só a última anima via `#iaBody .ia-msg:last-child`), digitação `• •• •••` em ciano, campo "O que deseja fazer?" com glow no foco, botões com ripple. **Só reescrevi `iaMontarFab` (HTML da esfera+painel) e o CSS** — `iaToggle/iaEnviar/iaRender/iaResponder/iaCmd*/iaConsulta` e toda a lógica (contexto/execução/consultas) permanecem. Bloco CSS "IA PLANETA — EXPERIÊNCIA PREMIUM (v6.40)" no fim do estilo2.css **sobrescreve** o bloco antigo do assistente. ⚠️ **A IA continua só aparecendo após login no modo online** (`iaPodeUsar`); offline sempre. **Validado ao vivo** (simulando offline p/ testar a UI): esfera 64×64 redonda respirando, painel 460×700 holográfico, bolha de vidro `rgba(12,32,60,.62)`, digitação visível, e o cérebro respondeu de verdade ("cpf do uilian"→022.997.900-98, "ajuda"→lista); zero erro. ⚠️⚠️ **LIÇÃO REforçada (v5.6):** eu tinha escrito `iaCmd*/contexto` no COMENTÁRIO do CSS — o `*/` fechou o comentário no meio e o parser DESCARTOU a regra `.ia-fab` (esfera saía retangular). NUNCA escrever `*/` dentro de `/* */`. v6.39 = (i) **ESC = Voltar** — o handler global de ESC no `init` agora: se há modal aberto (`#overlay.show`) fecha o modal; senão, se a paleta Ctrl+K está aberta ela cuida do próprio ESC (adicionado `e.stopPropagation()` no `pexCmdKey`); senão, se o dropdown de notificações está aberto fecha; senão chama `navVoltar()` (volta uma tela, com fallback Início). (ii) **Motorista "Problemas de saúde"** — nova seção "Saúde" no `modalMotorista` (textarea `f_saude` → campo `problemasSaude`), salvo em `salvarMotorista`, e exibido em destaque (card com borda vermelha + ícone stetho) na ficha `viewMotorista`. (iii) **Busca acha Termo Pallet e Transporte + vai DIRETO** — `pexSearch` das viagens agora casa `transporte` E `termoPallet` (via `inc`+`inck`), com sub "Transp. X · Termo Y · data", e o resultado aponta para `#viagens/<id>`; o router, ao receber `#viagens/<id>`, abre `modalViagem(id)` direto (deep-link da viagem). v6.38 = (a) **Viagens sem a coluna "Status"** (removido `<th>Status</th>` + `<td>` do `linhaViagem`, grouprow colspan 10→9; o campo `status` e o filtro "Pendentes" continuam existindo); (b) **Frota com mais dados por tipo** — `modalVeiculo` agora tem **Cor** (era só no seed, não editável) + seção **Cavalo** (Potência cv, Modelo do motor, Câmbio Manual/Automatizado/Automático) e seção **Carreta** (Modelo do equipamento de refrigeração, Dimensões internas, Divisória interna com ventilador Sim/Não, Nº e Modelo de trava-pallets), alternadas pelo tipo via `_vehToggleTipo()` (onchange do select de Tipo); `salvarVeiculo` grava todos; a ficha `viewVeiculo` exibe Cor + os campos do tipo (helper `extra`). Campos novos no veículo: `cor,potencia,modeloMotor,cambio,modeloEquip,dimInternas,divisoria,travaPalletQtd,travaPalletModelo`. v6.37 = (1) **trocas de óleo com VALOR** (campos Valor/Oficina no `modalManutencao`) **somadas na Manutenção** via helper `_manutTodos()` (serviços + óleo-com-valor como Preventiva) usado em `viewManutencao` e `viewManutencaoVeiculo` — óleo sem valor não muda nada, não duplica; (2) **Documentos ver/baixar/excluir** — `badgeAnexo` ganhou os 3 botões (era só ver), `verArquivo/baixarArquivo/_urlArquivo` endurecidos (try/catch, guard `typeof nuvemUrlArquivo`, msg clara `_msgArquivoIndisp`), `abrirReal/baixarReal` com `encodeURI` (espaços/acentos), e os arquivos-da-pasta no online agora mostram aviso 💻 informativo (não "erro" vermelho); ⚠️ os 68 arquivos da PASTA (`DB.arquivos`, caminhos `../...`) SÓ abrem no PC local — online/celular precisa reenviar via "Enviar arquivo" (limitação real, não bug); (3) **Viagens sem paginação** — tabela com classe `pex-noenh` (o `pexEnhanceTables` a ignora), lista completa do mês selecionado, linhas compactas (CSS `.viag-tbl`). ⚠️ **DUAS tentativas de mexer no cabeçalho MOBILE foram REVERTIDAS a pedido do cliente:** a v6.35 (cabeçalho "integrado") pelo revert `a683f7d`, e a v6.36 (mobile "sem cabeçalho" — topbar escondido + menu ☰ injetado na página) pelo revert `03cc105` ("ficou péssimo"). O código está **EXATAMENTE na v6.34** (topbar/cockpit normal em todas as telas). Ambas seguem no histórico do git (v6.35=`bae59a4`, v6.36=`e76f561`) se um dia quiser retomar. **⚠️ NÃO mexer na versão mobile sem novo pedido explícito — o cliente quer deixar o mobile para outro dia.** Sequência recente: v6.29 (**Descargas: cyber + acordeão por mês + importação de Excel**; commit `25c1336`), v6.30 (**Painel: navegação inteligente**; commit `1bc1eb8`), v6.31 (**Viagens CYBER**; commit `c49be68`), v6.32 (**topbar + modais CYBER**; commit `228e0b0`), v6.33 (**importação de Excel na Viagens** + cabeçalho sem descrição repetitiva + **Alarmes modernizados**; commit `b36d2d4`), v6.34 (**NAVEGAÇÃO MOBILE** — menu ☰ ao lado da marca + botão Voltar nas telas secundárias; commit `bfc58e1`). **Coexistência de temas:** o sistema é **CYBER neon do topo ao rodapé** — **NÃO há mais superfície clara** (topbar, menu, conteúdo e modais escuros; só a impressão força papel branco). **Viagens e Descargas também CYBER.** Menu lateral escuro premium; topbar cockpit escura cyber (paleta Ctrl+K e notificações já eram vidro escuro). **Financeiro sem senha.** **Gráficos "vivos"** em todo o sistema. **Pneus** com diagrama inteligente por eixo. **Vencimentos** só mostra a faixa (≤30 dias) setorizada. **KM/Horas** com filtro `#km/avencer`. **Próximos passos possíveis (o cliente decide):** (1) embutir fonte Inter/Sora em base64 (hoje só aparece se instalada na máquina); (2) automação real (e-mail/OCR entrando sozinho) precisa de backend (Supabase Edge Functions); (3) rastreador GPS real no mapa da Início (hoje "aguardando rastreador"). [OBS: tema escuro já está 100% — topbar/menu/conteúdo/modais todos cyber desde a v6.32.] ⚠️ **A base semente NÃO tem pneus e a maioria dos veículos não tem KM/óleo** — os dados reais estão só na NUVEM do cliente (logado); para validar pneus/KM aqui, injetar dados de teste (isolados no file://, remover depois, e conferir `nuvemUser()` = deslogado p/ não sujar a nuvem).

⭐ **VALIDAÇÃO NO mcp Browser (método correto — LER, atualizado):** o Browser **EXECUTA o JS** (`location.hash='#x';router();` + `getComputedStyle`; `read_console_messages onlyErrors`). **PEGADINHAS:** (a) arquivo fora da raiz → **snapshot do disco na hora que a aba abre**; recarregar a MESMA aba reusa o snapshot velho. (b) O snapshot **cacheia os assets por `?v=`** — se você editar `app.js`/`estilo2.css` mas NÃO subir o `?v=NN` no index.html, uma aba nova ainda serve o JS/CSS antigo. **Portanto: SEMPRE suba o `?v=` (parte do bump de versão) ANTES de validar, e abra ABA NOVA** (`tabs_create`+`navigate` p/ `file:///…/index.html`). (c) Abas novas às vezes fecham sozinhas (instável) — insista/limpe abas com `tabs_close`; costuma sobrar uma que pegou o snapshot novo. (d) Animações/transições/`requestAnimationFrame` **pausam no painel oculto** → count-up, largura em transição e classes via rAF podem ler o valor INICIAL no teste (ex.: largura do trilho leu 264 com transição, mas 76 sem); confirme o alvo por variável/estado, não pelo valor animado. Screenshot não funciona. Mobile (`Planeta Express - CELULAR.html`) regerado p/ v6.20.

v6.21: **Financeiro SEM SENHA** (`viewFinanceiro()` abre direto; trava/PIN removidos) + **Motor de Gráficos v2**: `barChart`/`donut` evoluídos (barras crescem + glow, grades, comparação de período `opts.compare`, %); NOVOS `lineChart` (linha+área que se desenha), `sparkline`, `heatmap`, `timeline`, `radar`; **zoom** (botão ampliar → modal, `pexEnhanceCharts` no `pexAfterRender`); Painel de Controle: Viagens/Despesas viraram linha/área com comparação + % vs. anterior + cards "Mapa de atividade" (heatmap) e "Linha do tempo — vencimentos". Tempo real já vinha do re-render (`router`/`aplicarRemoto`). CSS "MOTOR DE GRÁFICOS v2".

v6.22: **Módulo de Pneus Inteligente.** Sobre o módulo existente (reaproveita `DB.pneus`), `viewPneusVeiculo` reescrito: **diagrama SVG** do cavalo (3 eixos, 12 posições) / carreta (3 eixos + estepe); cada pneu clicável, **cor por condição** (`pneuCondicao`: sulco vs `sulcoMinimo`, ou % borracha, ou status → verde/amarelo/laranja/vermelho/cinza/vazio); **painel lateral** `pneuAbrir` (drawer fixo `.pn-panel`) com 16+ campos (marca/modelo/DOT/nº fogo/NF/fornecedor/data/valor/sulco/km rodados/custo-km/rodízios/reformas/vida útil/status/fotos-anexo/obs) + Editar/Mover/Tirar; `pneuMover` (troca slot + conta rodízio); análises desgaste/custo por eixo + próximos da troca. `modalPneu`/`salvarPneu` ganharam os campos novos (slot, modelo, sulco, valor, fornecedor, dataCompra, fogo, nf, rodizios, reformas). CSS "PNEUS — GESTÃO INTELIGENTE (v6.22)". ⚠️ **A base semente NÃO tem pneus** (`DB.pneus` vazio no teste local — dados reais só na nuvem do cliente); validei injetando pneus de teste (isolados no file://, removidos depois, cliente deslogado = nuvem intacta): 10 posicionados + estepes, 4 cores, painel 16 campos, mover OK; zero erro.

⚙️ **DEPLOY (02/08): a fila do GitHub Pages travou** nas v6.20/6.21 (build ficava "queued"). Resolvido com `git commit --allow-empty && push` (re-dispara) + **`.nojekyll`** adicionado (commit `1a8f6e9`) → deploys voltaram a ser rápidos (v6.22 publicou em ~20s). Ver [[deploy-github-pages]].

v6.29: **Módulo de DESCARGAS reformulado** (a pedido: "seguir o mesmo padrão visual e tecnológico dos demais módulos" + importação inteligente de Excel + organização por mês). Três frentes, tudo validado ao vivo (file://) sem erro de console. **(1) Tema/UX:** Descargas saiu da lista de exceções claras — agora recebe o **tema CYBER global** (removido `descargas` do `_noCy` em `pexAfterRender`). Nova `viewDescargas` com **4 KPIs** (registradas, valor no mês atual, valor acumulado, ticket médio), **pesquisa rápida** (`#descBusca`→`descSetBusca`, atualiza só a lista p/ **não perder o foco**, insensível a acento via `_descNorm`), **filtro por placa**, e botões **Expandir/Recolher tudo**. Ícones novos no `IC`: `search`, `chevron`, `filter`. **(2) Acordeão por competência:** as descargas são agrupadas por **mês/ano** (`_descComp`/`_descGrupos`), do **mais recente ao mais antigo**; cada mês é um card **expansível** (`.dsc-month`/`descToggle`/`descInit` anima a altura via `maxHeight=scrollHeight`; estado em `descAbertos`; 1ª visita abre o mês mais recente) com **contagem + subtotal** no cabeçalho; **dentro do mês, ordem cronológica** (data crescente). Na pesquisa, todos os meses com resultado abrem. `pexEnhanceTables` foi ajustado p/ **pular** as tabelas do acordeão (`.dsc-months`) e não injetar busca/paginação própria. **(3) Importação inteligente de Excel** (botão **"Importar Planilha Excel"** → `modalImportarDescarga`): reaproveita `PEXImport.lerArquivo` (.xlsx/.csv), e o novo **`_descDetectar`** acha sozinho a linha de cabeçalho e **mapeia as colunas** por fuzzy-match (Data/Placa/Transporte/Senha/Valor/Local/Pago — cada campo com regex de sinônimos pt-BR), **extrai tudo**, normaliza **data BR→ISO** (`_impISO`) e **valor** cru/BR (`_descNum`), aponta **inconsistências** (sem data, sem valor, placa fora da frota) e **duplicados** (mesma data+transporte, ou data+placa+valor — no banco e dentro do próprio arquivo), mostra **prévia conferível** com checkbox e badges (`descImpRender`) + a linha "Colunas identificadas"; **confirmação final** (`descImpConfirmar`) só insere as marcadas (`pago` vazio vira "Bradesco") e reabre o mês mais recente. `modalDescarga` agora preserva **placa fora da frota** (importada) no select. Só `app.js` (novas funções na seção DESCARGAS) + bloco CSS **"DESCARGAS (v6.29)"** no fim do `estilo2.css` (toolbar/acordeão, usa tokens do cyber, `@media print` abre todos os meses). **Validado ao vivo:** `#view.cyber` on, 3 meses [Julho/Junho/Maio 2026] mais-recente-primeiro, mês atual aberto c/ altura animada, `_descDetectar` mapeou 7 colunas de cabeçalhos variados + parseou datas BR/valor `1.234,56`→1234.56 + pegou o duplicado do seed, toggle/expandir/recolher OK, busca sem acento (atacadao→5, muffato→3), fluxo confirmar inseriu 2 e criou "Agosto 2026" no topo; zero erro. ⚠️ **`.xls` binário antigo NÃO é lido** (PEXImport só faz .xlsx-zip + .csv) — o modal avisa p/ salvar como .xlsx/.csv. **Publicada no GitHub Pages** (commit `25c1336`); mobile/celular ainda não regenerado.

v6.30: **Painel de Controle — NAVEGAÇÃO INTELIGENTE** (a pedido: "todo card deve abrir exatamente o item pendente, com o filtro/status certo; nunca a lista completa"). **Problema-raiz corrigido:** os cards de Vencimentos do Painel apontavam para filtros ANTIGOS (`vencimentos/critico|vencido|atencao|emdia`) que **não existem mais** desde a v6.27 (o módulo passou a usar faixas `venc|d10|d20|d30`) — então abriam vazio ou errado. Refeito em `viewDashboard`: as contagens dos cards agora são calculadas **por dias** (`fVenc`=vencidos, `fD10`≤10, `fD20`=11–20, `fD30`=21–30) para **bater exatamente** com o que abre. Correções card a card: **KPI "Documentos vencidos"** → `#vencimentos/venc` (só vencidos); **KPI "Vencem em ≤10 dias"** (era "Vencimentos críticos") → `#vencimentos/d10`; **donut "Situação dos vencimentos"** reconstruído com as 4 faixas, cada legenda → `#vencimentos/venc|d10|d20|d30`; **KPI "Trocas a vencer"** → `#km/avencer` (era `#km` lista cheia); **KPI "Pneus no limite"** → `#pneus/limite` (NOVO: `pneusFiltro`+`viewPneusLimite` lista só os pneus em condição crítica/próxima da troca — cada linha abre o veículo no diagrama; router trata `pneus/limite` como filtro, não como id de veículo; helper `_pneuNoLimite` via `pneuCondicao` key crit/troca, usado também no KPI p/ casar a contagem); **legenda "Cavalos/Reboques"** (Composição da frota) → Frota já filtrada por tipo (`frotaFiltro='cavalo'|'reboque'`); **linhas "Últimos check-lists"** → abrem o **registro exato** (`modalChecklist(id)`) em vez da lista. KPIs informativos (Veículos/Motoristas ativos, Despesas, Check-lists no mês) seguem abrindo o módulo correspondente. Só `app.js`. **Validado ao vivo (file://):** cada href/onclick conferido + navegação exercitada — vencimentos/venc mostra só "Vencidos" (2), /d20 só "11–20 dias" (2), km/avencer 1 pendente, pneus/limite lista só o pneu crítico (injetei 1 crít + 1 bom → aparece 1), Frota cavalo=5/reboque=5, linha de check-list abre o modal do registro; **zero erro de console**. ⚠️ Base semente não tem pneus/checklists — validados com dados de teste injetados e removidos (deslogado, nuvem intacta).

v6.31: **Aba Viagens igual ao resto do sistema (CYBER)** (a pedido). Como `viewViagens` usa só componentes padrão (`banner`/`grid kpis`/`kpi`/`toolbar`/`seg`/`selectlite`/`table.tbl`/`st`/`tag`/`grouprow`/`plate`) — todos já cobertos pelo bloco "TEMA CYBER GLOBAL (v6.20)" (`#view.cyber`) — bastou **remover `viagens` do `_noCy`** no `pexAfterRender`. Agora `_noCy={inicio:1,dashboard:1,manutencao:1}` (esses 3 têm tema próprio scoped). **Única exceção clara restante: os modais** (`#overlay`). Também **regenerei o arquivo do celular** (`Planeta Express - CELULAR.html`, ~2,7 MB, offline por aparelho) já com v6.29+v6.30+v6.31 via `build_celular.sh` (setar `export T="<scratchpad>"` antes; ⚠️ o `set -e` faz o script sair com código 1 no fim porque o `grep -o "assets/"` de conferência não acha nada = 0 ocorrências = OK — o arquivo já foi escrito antes; conferências: v6.31, data:image=15, assets/=0, viewViagens/viewPneusLimite/modalImportarDescarga presentes). **Validado ao vivo:** `#viagens` com `.cyber` (banner `rgba(0,26,47,.6)`, th `rgba(0,18,32,.72)`, 4 KPIs, 96 viagens), Descargas segue cyber, Início/Painel/Manutenção seguem com tema próprio (sem `.cyber`); zero erro. Só `app.js` (1 linha).

v6.32: **Cabeçalho superior (topbar/cockpit) + modais no tema CYBER** (a pedido: "aquele cabeçalho que ainda está claro em todas as abas, coloque igual no cyber; e faça os modais também"). O topbar é um elemento **GLOBAL** (fora do `#view.cyber`), então os tokens `:root` claros não valiam nele — usei **cores cyber explícitas**. Dois blocos novos no fim do `estilo2.css`: **"TOPBAR / COCKPIT — TEMA CYBER (v6.32)"** (fundo `rgba(6,15,27,.82)` + blur, `page-title` platina, `cock-search`/`cock-kbd`/`cock-ico`/`cock-weather`/`cock-clock`/`cock-status`/`relbtn`/`userbadge`/`menu-toggle`/`back-btn` escuros neon; `cock-badge` com anel escuro `#0a1524` + keyframe `cockBadge` redefinido; `.topbar` com `!important` p/ vencer os 3 `.topbar{}` claros anteriores) e **"MODAIS — TEMA CYBER (v6.32)"** (`#overlay .modal` com tokens cyber redefinidos + overrides das superfícies brancas chumbadas: container em gradiente escuro + borda neon, `m-h`/`m-f`/botão X, campos input/select/textarea/selectlite + foco, seg, botões primary/danger/ghost, banner interno dos modais de importação, tabelas de prévia `tbl th/td`, badges `st`/`tag`, `imp-off`; `@media print` volta a branco). A paleta Ctrl+K e o dropdown de notificações **já eram escuros** (v6.19). Só CSS. **Validado ao vivo:** topbar `rgba(6,15,27,.82)` com título platina em Início/Frota/Descargas/Viagens; modal com gradiente escuro `rgba(10,22,38,.98)` + borda `rgba(0,229,255,.18)`, input `rgba(0,18,32,.62)`, footer/botão X/primary neon; modal de importação com banner escuro; zero erro. **Agora o tema escuro está 100% — não sobra superfície clara** (só impressão).

v6.33: **Três melhorias (a pedido).** **(1) Importação de Excel na VIAGENS** — botão "Importar Planilha Excel" no banner (`modalImportarViagem`) + `_viagemDetectar` (mesma engenharia da descarga: acha a linha de cabeçalho e mapeia por fuzzy-match Data/Placa/Motorista/Transporte/Destino/Baixado/Termo Pallet/Termo baixado/Status; normaliza Baixado→SIM/TSP/NÃO, Termo→SIM/NÃO e Status→Pendente/Concluída/Cancelada; se não houver Status, deduz Concluída quando Baixado=SIM/TSP); prévia com badges/inconsistências (sem data/placa/motorista, placa fora da frota) e duplicados (data+transporte, ou data+placa+motorista — no banco e no próprio arquivo); `viagemImpRender/Toggle/Confirmar`. `modalViagem` preserva placa fora da frota. **(2) Cabeçalho (topbar) sem a descrição da aba** — `.topbar .page-title{display:none}` (cada tela já se identifica no próprio banner; era redundante). **(3) Alarmes Thermo King MODERNOS** — `viewAlarmes` reescrito: classificador `_alarmeCat` por categoria (Controlador roxo / Energia âmbar / Sensor ciano / Temperatura laranja / Motor-Refrigeração vermelho / Geral) com ícone+cor; **chips de filtro por categoria** (com contagem) + busca; **cards** (`.alm-card`) com faixa de acento neon (`--ac`), chip de ícone tintado, código com glow, tag de categoria, hover elevado e entrada em cascata; bloco CSS "ALARMES — VISUAL MODERNO (v6.33)". `app.js` + `estilo2.css`. **Validado ao vivo:** viagens detecta 8 colunas + pega duplicado do seed (vg1) + flag "placa fora da frota" correto (IPD-9036 não é da frota); page-title `display:none`; 137 cards de alarme, 6 chips, filtro Energia=19 / busca "sensor"=23, cores por categoria (alarme 01=Controlador roxo); zero erro. ⚠️ **color-mix** usado no CSS dos alarmes (chips/ícone/glow) — ok no Chrome/Edge do cliente; degrada com fallback.

v6.34: **Navegação mobile premium (só ≤860px; desktop/tema INTACTOS).** (1) **Menu ☰ grande e tocável** (56×56) com ripple/glow/press, ao lado da **marca** nova no topbar `.tb-brand` (logo em selo branco + "PLANETA EXPRESS"; `display:none` no desktop, então o desktop não muda). (2) **Botão Voltar** (`.back-btn` com ícone ArrowLeft inline) **em todas as telas secundárias** — escondido só em Início/Painel (via `body.pex-home`, setado no `pexAfterRender`); `onclick="navVoltar()"`. (3) **`navVoltar()` + pilha `PEX_NAV`** (rastreada no topo do `router` via `_pexTrackNav`, com flag `_pexNavBack` p/ não re-empilhar): volta à tela anterior; **sem histórico → Início** (nunca prende). Filtros/pesquisas preservados (são globais de módulo). (4) **Menu desliza ~250ms** — corrigido um bug: a transição global `.sidebar{transition:width…}` (v6.18, trilho desktop) matava o slide no mobile; agora `@media(max-width:860px){ .sidebar{transition:transform .26s} }`. (5) **Scrim com desfoque + fade** (`backdrop-filter:blur(4px)` + opacity/visibility; fecha ao tocar fora — já tinha `onclick=closeSidebar`). (6) **Sem rolagem horizontal** (`.content{overflow-x:clip}`; docScroll==375 em todas as telas — tabelas largas rolam dentro do `.tbl-wrap`, correto). (7) Espaço no header estreito: esconde o "+" `#cockNew` (redundante com a busca no mobile) e o selo do logo <480px; toques do cluster ≥46px. `index.html` (topbar) + `app.js` (navVoltar/tracking/pex-home) + bloco CSS "NAVEGAÇÃO MOBILE PREMIUM (v6.34)". **Validado ao vivo (375px e 1280px):** menu 56×56, Voltar aparece em Frota e some no Início, pilha Início→Frota→Motoristas volta certo e cai no Início sem histórico, sidebar com transição `transform`, scrim com blur, wordmark "PLANETA EXPRESS" visível, **zero overflow de página**; **desktop:** menu/voltar/marca ocultos e "+" de volta, sidebar fixa, nada mudou; zero erro de console.

⚠️ v6.35 (cabeçalho mobile integrado) foi feita e depois **REVERTIDA a pedido do cliente** (revert `a683f7d`). O que ela fazia (caso queira retomar): só CSS `@media(max-width:860px)` — topbar sem `border-bottom`/`box-shadow`, compacto e na cor do tema; na Início/Painel (`body.pex-home`) o topbar recebia a cor do `.ini-cmd` (`#0a1a2f`, fundido) e o brand duplicado do `.ini-top` sumia (`.ini-brand .mk` e `.tx b` = `display:none`), virando linha 2 (subtítulo + Operação ativa + `#iniClock`). Está no histórico no commit `bae59a4`.

⚠️ **v6.35 e v6.36 (duas tentativas no cabeçalho MOBILE) foram REVERTIDAS** — o cliente não gostou de nenhuma e pediu para deixar o mobile para outro dia. O que cada uma fazia, caso um dia se retome (ambas no histórico do git): **v6.35** (`bae59a4`) = topbar mobile "integrado" (sem borda, cor do tema, fundido com a Início, brand duplicado escondido virando linha 2). **v6.36** (`e76f561`) = mobile "sem cabeçalho": `@media(max-width:860px)` escondia o `.topbar` e a função `pexMobileNav(rota)` (no `pexAfterRender`) injetava ☰+Voltar DENTRO da página (ao lado da logo na Início; no banner das demais; nav "solo" com título nas sem-banner) + `.ini-cmd{margin:0}` + safe-area. **Lição:** o cliente quer o mobile parecendo app, mas as duas abordagens (integrar OU remover o cabeçalho) ele achou ruins — na próxima, alinhar o desenho ANTES (talvez um mockup/print) em vez de implementar direto.

v6.37: **Três correções (a pedido; mobile NÃO tocado).** **(1) Trocas de óleo com valor, somadas na manutenção:** `modalManutencao`/`salvarManutencao` ganharam **Valor da troca (R$)** + **Oficina**; novo helper **`_manutTodos()`** = `DB.servicos` + cada troca de óleo (`DB.manutencoes`) que tenha `valor>0`, mapeada como **Preventiva** (`{_oleoId, veiculoId, data, descricao:item, oficina, km, valor, tipo:'Preventiva'}`). `viewManutencao` (KPIs/gráfico/total) e `viewManutencaoVeiculo` (tabela + total + tag "óleo") agora usam `_manutTodos()`; as linhas de óleo editam via `modalManutencao`. Não duplica dados (óleo sem valor = 0, não altera nada). **(2) Documentos ver/baixar/excluir:** `badgeAnexo` (selo "Anexado") agora tem **3 botões: ver + baixar + excluir** (era só ver); `verArquivo`/`baixarArquivo`/`_urlArquivo` com try/catch, guard `typeof nuvemUrlArquivo==='function'` e mensagem clara (`_msgArquivoIndisp` distingue "enviado em outro aparelho" de "não encontrado"); `abrirReal`/`baixarReal` usam `encodeURI` (`_encPath`) p/ caminhos com espaços/acentos; `_arquivoLocalIndisponivel` virou aviso 💻 informativo (sem 'err' vermelho). ⚠️ **Os 68 arquivos da PASTA (`DB.arquivos`, `../...`) só abrem no PC local** — não é bug, é limitação (não estão no repo); para acessar online/celular, reenviar via "Enviar arquivo" (viram upload em IndexedDB/nuvem). **(3) Viagens sem paginação:** a tabela recebeu a classe **`pex-noenh`** e o `pexEnhanceTables` passou a **pular** tabelas com essa classe (além do `.dsc-months`) → some a paginação "1/2/3"; mostra a **lista completa do mês selecionado**; CSS **`.viag-tbl`** deixou as linhas compactas/profissionais + grouprow do mês sticky. **Validado ao vivo:** viagens 90 linhas sem paginação (mês 06/2026 → 10 completas), óleo R$500 num registro subiu o total da manutenção em exatamente 500 e aparece a linha "óleo" na planilha do veículo, `badgeAnexo` com ver/baixar/excluir, modal de óleo com Valor/Oficina, todas as telas renderizam; **zero erro**. Só `app.js` + `estilo2.css`.

v6.38: **Viagens sem coluna "Status" + Frota com mais campos por tipo (a pedido).** (1) Viagens: some a coluna "Status" da tabela (o campo `status` e o filtro "Pendentes" permanecem). (2) Frota: `modalVeiculo` reescrito com **Cor** + seções que alternam pelo tipo (`_vehToggleTipo`): **Cavalo** = Potência (cv), Modelo do motor, Câmbio; **Carreta** = Modelo do equipamento (refrigeração), Dimensões internas, Divisória interna com ventilador, Nº e Modelo de trava-pallets. `salvarVeiculo` grava tudo; `viewVeiculo` (ficha) mostra Cor + os campos do tipo. **Validado ao vivo:** Viagens com 8 colunas (sem Status), modal alterna cavalo/carreta ao trocar o Tipo, ficha do cavalo mostra 440 cv/Volvo D13/Automatizado/Cor e da carreta mostra Thermo King SLXe/dimensões/divisória/trava-pallets; zero erro. Só `app.js`.

v6.39: **ESC volta uma tela + Motorista "Problemas de saúde" + Busca por Termo Pallet/Transporte indo direto (a pedido).** (1) ESC: fecha o modal se aberto, senão `navVoltar()`. (2) Motorista: seção "Saúde" no cadastro (`problemasSaude`) + destaque na ficha. (3) Busca global (Ctrl+K/cockpit) passou a achar Termo Pallet e Nº de Transporte das viagens e o clique abre a viagem direto (`#viagens/<id>` → `modalViagem`). **Validado ao vivo:** busca "130949638"(transp.) e "4723077"(termo) achou a viagem vg1 e clicar abriu o modal "Editar viagem"; modal de motorista com campo Saúde e ficha mostrando o texto; ESC em #frota voltou p/ #inicio e ESC com modal aberto só fechou o modal; zero erro. Só `app.js`.

v6.40: **Nova experiência da IA Planeta** — esfera tecnológica viva (canto inf. dir.) + chat holográfico em vidro escuro (460×700), só visual; o cérebro (parser/contexto/execução/consultas) é o mesmo. Reescrevi só `iaMontarFab` + CSS "IA PLANETA — EXPERIÊNCIA PREMIUM (v6.40)". ⚠️ Cuidado com `*/` em comentário CSS (quebrou a regra `.ia-fab` na 1ª tentativa).

v6.41: **Topbar sem barra (desktop) — cápsulas de vidro flutuantes.** `.main` recebe o fundo cyber; `@media(min-width:861px)` deixa o `.topbar` transparente (sem faixa) e cada componente vira cápsula (`rgba(12,20,34,.45)` + blur18 + border neon + radius18 + glow), pesquisa 52px. Mobile intacto (v6.34). Só CSS.

v6.42: **Monitoramento virou Command Center** — mapa da Início redesenhado (grade hexagonal, base Londrina com radar, rotas neon com partículas, veículos tech, cidades com glow, controles). Só visual; rotas/cavalos reais/painel `iniAbrirVeic`/clima real mantidos. Só `app.js` (map SVG + `iniMapZoom`/`iniBaseWx`) + CSS "COMMAND CENTER (v6.42)".

v6.43: **NOVA ABA SEGUROS (apólices, vigências, prêmios e avisos de renovação).** O cliente mandou 9 PDFs de apólices (Allianz Frota, Tokio Marine RCTR-C 540 00029910 e RC-DC 550 00015676, Mitsui Auto BBJ-2D77, Tokio/Porto Vida, HDI e HDI/Icatu Vida em Grupo, + planilha-resumo "Seguros Ativos" que consolida tudo, e 1 auto CANCELADO Tokio QHS-0C02). Extraí os dados (a planilha-resumo já traz seguradora/apólice/vigência/prêmio/pagamento de 7; a Declaração Tokio traz as 2 de carga que NÃO estão na planilha). **Nova coleção `DB.seguros`** no `SEED` (dados.js, 9 apólices, campos: ramo auto|frota|carga|vida, grupo empresa|socio|func, seguradora, apolice, endosso, segurado, objeto, placa, inicio, **fim** (=vencimento), premio (null=averbação), pagamento, cobertura, status Ativo|Cancelado, obs) + backfill em `ensureCollections`. **Módulo `viewSeguros`** (app.js, seção "SEGUROS / APÓLICES"): banner, 4 KPIs (Apólices ativas, Prêmio anual `moneyK`, A vencer 90 dias, Vencidas — clicáveis viram filtro via `segFiltro`), card **"Próximas renovações"** (`.alert-row`, ativas que vencem em ≤180 dias, ordenadas), filtro por ramo (`selectlite`), e **cards agrupados por titular** (Empresa → Funcionários → Sócios) com tabela `table.tbl pex-noenh` (Ramo `.seg-tag` colorido / Seguradora / Apólice / Objeto / Vigência / Situação `situacao()` / Prêmio). Seção "Canceladas" à parte. CRUD completo: `modalSeguro`/`salvarSeguro`/`excluirSeguro` (17 campos, `parseBRL` no prêmio→null se vazio, anexo via `badgeAnexo('seguro',id,/./,'Apólice')`). **Integração de vencimentos:** `todosVencimentos()` agora MESCLA as apólices ativas como entradas sintéticas (`id='seg_'+s.id`, `entidade:'seguro'`, `validade:s.fim`) → aparecem automaticamente no módulo **Vencimentos** (itemRow trata entidade 'seguro'→rota `seguros`; `modalVencimento` com id `seg_*` redireciona p/ `modalSeguro`), nas **notificações** (`_vencNome`/`nomeEntidade` tratam 'seguro') e no **Painel de Controle** (novo KPI "Seguros a vencer" → `#seguros/avencer`, crit se ≤30 dias). Menu lateral: item `seguros` (grupo Financeiro) com **badge** (`contadores().seg` = ativas ≤60 dias). Ícone novo `umbrella` no `IC`; `tipoIcone` casa `/^Seguro/`. CSS "SEGUROS (v6.43)" (`.seg-tag`) no fim do estilo2.css. **Validado AO VIVO** (file://, aba nova, ?v=104): app.js parseia OK, 9 apólices carregadas, 8 entram em todosVencimentos (cancelada fora), viewSeguros com 4 KPIs [8 · R$37k · 3 · 0], 5 grupos + Canceladas, 7 renovações, tags coloridas, `#seguros/avencer`=3 linhas, Painel KPI data-count=3 (crit), badge menu=1, modal edita (apólice 22497649/fim 2026-08-17/anexo), CRUD create+delete OK (parseBRL "1.234,56"→1234.56), **zero erro de console**. Publicada no GitHub Pages (commit `41c2219`); celular regenerado (2,9 MB). ⚠️ As apólices de CARGA (RCTR-C/RC-DC) são **averbação** (prêmio variável, mín. R$500/mês) → `premio:null`, exibe "Averbação". Só faltam anexar os PDFs reais das apólices (o cliente faz pela própria aba, botão Anexar).

v6.44: **Enviar apólice com detecção automática do seguro.** O cliente achou trabalhoso anexar "uma por uma" (abrir cada seguro → Anexar). Novo botão **"Enviar apólice"** no banner da aba Seguros → `modalEnviarApolice()`: zona de arrastar-e-soltar (`.apo-drop`) + input múltiplo (aceita PDF/imagem). Cada arquivo é lido (`pexLerPdfTexto` quando é PDF) e `_apoliceMatch(nome,texto)` descobre a qual apólice pertence — pontua **nº da apólice** (dígitos, +100) e **endosso** (+45) casados no nome/texto, **placa** (+60), **seguradora** (+15) e palavras do **ramo** (+8). Prévia conferível (`APOLICE_FILA` + `_apoliceFilaHTML`/`_apoliceRefresh`): cada arquivo vira uma linha com `<select>` de TODOS os seguros (pré-selecionado no melhor palpite) + selo **Detectado** (score≥60, verde) / **Confira** (amber) / **Escolha** (sem match); dá p/ remover (`_apoliceRemove`). "Anexar N apólice(s)" → `subirUm(file,'seguro',id,'Apólice')` p/ cada, `reloadFiles`+`saveDB` (grava IndexedDB local + nuvem se logado; o selo "Anexado" aparece na ficha via `badgeAnexo('seguro',id,/./,'Apólice')`). Só `app.js` (seção ENVIAR APÓLICE) + CSS ".apo-*" no estilo2. **Validado ao vivo (file://, ?v=105):** parse OK, os nomes reais dos PDFs do cliente casam sozinhos ("RCTRC 540 00029910"→Tokio RCTR-C [108], "RCDC 550 00015676"→RC-DC [108]); por texto simulado com nº da apólice Porto→[123]; nomes genéricos ("Apólice 1.pdf") caem no dropdown; modal/drop/fila/botão OK, zero erro de console. Publicada no GitHub Pages (commit `c2c966a`); celular regenerado (2,9 MB). ⚠️ Em PDF **escaneado/imagem** o texto sai fraco → o auto-match pode não achar (o cliente escolhe no dropdown; a intenção era justamente evitar abrir seguro por seguro). **[RESOLVIDO na v6.45 — ver abaixo.]**

v6.45: **Leitura PROFISSIONAL das apólices (pdf.js + OCR).** O cliente reclamou que só 2 apólices foram detectadas, outras pediam conferência e algumas "nem conseguiu ler". Causa: o leitor interno `pexLerPdfTexto` (regex `N G obj`) NÃO abre PDFs com **object streams / xref streams** (é o caso das apólices Tokio Marine e Allianz) → texto vinha vazio → sem nº da apólice, sem match. Solução (mesmas libs da v6.4 revertida, agora plugadas no fluxo de Seguros): **pdf.js** `pdfjs-dist@3.11.174` (extrai o texto REAL, inclusive object streams) + **Tesseract.js** `@5.1.0` (OCR 'por' p/ páginas escaneadas/imagens), ambos por CDN com `defer` no index.html (após nuvem.js — ficam FORA do build do celular, que degrada p/ o leitor interno). Novo `pexLerApoliceTexto(file,onProgress)` orquestra: para cada uma das 5 primeiras páginas pega `getTextContent()`; se a página tem <40 chars (escaneada) e Tesseract existe, **renderiza em canvas (scale 2) e passa OCR**; se no fim ainda veio quase vazio, tenta o `pexLerPdfTexto` antigo como último recurso. Imagem solta → OCR direto. Helpers `_pdfjs`/`_tess`/`_pdfDoc`/`_ocrPagina`/`_ocrImagem`/`_ocrCanvas`. **Matcher reforçado** (`_apoliceMatch` agora retorna `reason`): nº da apólice (+100), endosso (+45), placa (+70), TODOS os tokens da seguradora (1º +18, demais +10), titular sócio (+14), palavras do ramo (+8) e vida **em grupo** × **individual** (+14); mostra o motivo da detecção na linha. **UI com progresso**: cada arquivo entra como "Lendo…" (spinner `.apo-spin` + status "OCR %"), botão "Anexar" trava enquanto lê; `_apoliceLer` reescrito p/ processar em sequência atualizando cada linha. **Validado AO VIVO no GitHub Pages** (v6.45, aba real online): `window.pdfjsLib` e `window.Tesseract` carregam (object), montei um PDF sintético com "Apolice 312205367 ... Frota" → `pexLerApoliceTexto` **extraiu o texto** e `_apoliceMatch` com nome genérico ("Apolice 1.pdf") acertou **Frota/Allianz/312205367 [126] via nº da apólice, seguradora**; zero erro de console. Publicada (commit `16cb10a`); celular regenerado. ⚠️ **OCR/pdf.js só com internet** (CDN) — no computador offline sem internet ou no arquivo único do celular, cai no leitor interno (a maioria das apólices tem texto e o pdf.js resolve; só rodar o envio de apólices com internet). Tesseract baixa o pacote de idioma (~15 MB) na 1ª vez que precisa de OCR de verdade.

v6.57: **Aba Conformidade ANTT/RNTRC + "Conjuntos ativos" + leitor de relatório robusto.** (1) **Nova aba ANTT** (`viewAntt`, rota `antt`, menu grupo Empresa): dados reais do extrato RNTRC do cliente — `DB.antt` no SEED (RNTRC **050428055**, categoria ETC, situação **Ativo/apto**, cadastro 04/10/2017, extrato 18/03/2026, endereço Carambeí, base legal Lei 10.233/2001 · 11.442/2007 · Res. 5.982/2022, + **12 veículos** 7 automotores/5 implementos com placa/uf/renavam/propriedade/situação) + backfill (`if(!DB.antt)`). Tela: card de status (badge ATIVO verde), KPIs (ativos/automotores/implementos/**conjuntos**), dados do registro, base legal + anexo do extrato (`badgeAnexo('antt','extrato',…)`), tabela de veículos. **CRUD completo**: `modalAnttDados`/`salvarAnttDados` (edita RNTRC/categoria/situação/datas/endereço/base) + `modalAnttVeiculo`/`salvarAnttVeiculo`/`excluirAnttVeiculo` (incluir/editar/remover veículo). CSS `.antt-badge`/`.antt-f`. (2) **"Veículos ativos" → "Conjuntos ativos"** na Início e no Painel (valor = nº de cavalos = 5, pois cada carreta faz conjunto com um cavalo) — trocado `cavalos+reb`→`cavalos`. (3) **`_faturParseRelatorio` robusto**: formato completo (Mês Ano Saídas Serv Outros Total) + **fallback flexível** (Mês Ano + 1..6 valores, ÚLTIMO=Total) p/ funcionar quando o pdf.js extrai o PDF "grudado"/desalinhado. **Validado ao vivo (lógica injetada):** leitor nos 2 formatos (completo e desalinhado) OK. Commit `ef2415f`. ⚠️⚠️ **DEPLOY TRAVADO:** a fila do GitHub Pages ficou presa em v6.55 — v6.56 e v6.57 NÃO subiram (era ESSE o "não funcionou" do faturamento: a versão nova nunca chegou ao cliente). Vários `git commit --allow-empty && push` feitos p/ re-disparar; a pasta OFFLINE local e o celular JÁ estão em v6.57 (o cliente pode usar offline enquanto o online destrava). Conferir depois se o Pages subiu (v118).

v6.56: **Faturamento — PIPELINE INTELIGENTE de documentos (OCR + reconhecimento).** Cliente quis comportamento de "software OCR+IA fiscal" (SAP/TOTVS). Feito o possível 100% no navegador (app estático, sem backend): reescrevi o import (`modalImportarFatur`/`_faturLer`/`_faturImpFilaHTML`/`_faturImpRefresh`/`_faturImportConfirmar`). Agora `FAT_FILA` = 1 item por ARQUIVO (fila de vários), cada um roda `_docProcessar(item,onEtapa)` com **etapas animadas** (`DOC_ETAPAS`: Lendo→Extraindo→Interpretando→Relacionando→Conferindo→Concluído). Motor: **pdf.js** (texto nativo) + **Tesseract OCR** (escaneado/imagem) via `pexLerApoliceTexto`. **`_docClassificar(txt,nome)`** reconhece o tipo por padrões (Relatório de Faturamento 97%, CT-e 93/96%, NF-e 91%, NFS-e 90%, Boleto 86%, Fatura/Duplicata, Planilha 80%, XML) com **% de confiança**. Extração por tipo (reusa `_faturParseRelatorio`/`_faturParseXml`/`_faturDetectExcel` + novo `_faturParseNotaTexto` p/ valor/data/chave de nota em texto). **Relacionamento** placa→veículo (`veiculoByPlaca`). **Conferência**: pendências (sem valor/data) + dedup (competência/chave). **Painel de resumo** por documento: tipo, confiança, nº de lançamentos, pendências, tempo (s). **Logs auditáveis** `DB.faturLogs` (`_faturLog`: quando/usuário/arquivo/tipo/conf/campos/salvos/tempo). CSS "Pipeline de documentos (v6.56)" (`.doc-card/.doc-step/.doc-spin/.doc-conf/.doc-reg`). **Validado ao vivo (classificador injetado):** os 8 tipos corretos (Relatório 97%, CT-e 93, NF-e 91, NFS-e 90, Boleto 86, XML CT-e 96, Planilha 80, genérico 45%); extração do relatório (10 meses, R$1.611.223,96) e XML já validadas antes. Commit `ed67bc4`. ⚠️ **Google Vision/Azure/AWS Textract NÃO rodam em app estático** (precisam backend + chave paga) — ficam preparados p/ futuro via Supabase Edge Function. "IA" aqui = classificação por padrões (robusta p/ fiscais BR), não LLM no navegador. Auto-aprendizado ainda básico (logs); evoluir salvando mapeamentos emitente→cliente.

v6.54/6.55: **REFATORAÇÃO DA VERSÃO MOBILE (desktop 100% intocado).** Cliente pediu experiência de app nativo no celular, mantendo UMA lógica só. Feito como **camada de apresentação** (CSS `@media(max-width:860px)` + comportamentos pós-render no `pexMobileInit(rota)` chamado no `pexAfterRender`) — nada de views/lógica alterado, então toda tela nova entra no padrão mobile sozinha. **v6.54:** (1) **Barra superior enxuta** = ☰ · marca(logo+nome) · 🔍 (cockpit `cock-cluster`/relógio/clima/notif/relatório/page-title escondidos `display:none!important` no mobile; `.cock-search` vira botão redondo que abre `pexCmdOpen`). (2) **Barra de contexto** `#mobCtx` (injetada por `pexMobileCtx`): seta **Voltar** (`navVoltar`, sempre no mesmo lugar) + título da tela (lido do `#pageTitle`), some na home (`body.pex-home`). (3) **Drawer premium**: `.sidebar` mobile com vidro fosco/blur, glow, radius, e **fechar por gesto** (swipe p/ esquerda em `pexMobileGlobals`); o menu já lista tudo via `renderSidebar` (nova tela aparece sozinha). (4) **Spotlight**: `#pexCmd.cmdk` vira tela cheia no mobile (a busca `pexSearch` já acha veículos/motoristas/CT-e/notas/viagens/pneus/etc). (5) **Tabelas→cards** (`pexMobileTables`): injeta `data-th` (do `<thead>`) em cada `<td>`; CSS `table.tbl.mob-cards` transforma cada `<tr>` em card (label:valor via `::before content:attr(data-th)`). (6) **Modal vira bottom sheet**; campos **16px** (sem zoom iOS) + `min-height:50px`; botões grandes. (7) **Ripple** nos `.btn` (pointerdown global), **safe-area** (`env(safe-area-inset-*)`, o index já tem `viewport-fit=cover`), **IA** com safe-area, transição `mobViewIn`. Bloco CSS "APP MOBILE (v6.54)" no fim do estilo2. **v6.55:** **barra de navegação INFERIOR** (`pexMobileBottomNav` + `#mobNav`): Início · Painel · Buscar · Menu, fixa no rodapé, vidro fosco, item ativo com glow (muda por rota), safe-area; IA sobe (`bottom:84px+safe`) e `.content` reserva o rodapé. **Validado ao vivo (v6.54, viewport 375px):** cockpit oculto, busca=ícone, barra de contexto "Seguros"+Voltar (some na home), tabelas=cards com data-th; e a barra inferior (injeção) = 4 itens, ativo por rota, fixa no rodapé. Commits `12d71b8` (v6.54) + `85b16eb` (v6.55). ⚠️ Cliente aprovou ("mobile já está muito bom, só faça melhorias") — próximas são melhorias incrementais. **Regra confirmada: mobile é só CSS+pós-render; NUNCA duplicar lógica.**

v6.53: **Faturamento — Importar do contador (PDF/Excel/XML).** Cliente quis anexar arquivos do contador p/ preencher o faturamento automaticamente. Arquivo real recebido: `Empresa 201 - PLANETA EXPRESS TRAN.pdf` = **Relatório de Faturamento MENSAL** (colunas Mês/Ano/Saídas/Serviços/Outros/**Total**; set/2025→jun/2026; total geral R$ 1.611.223,96). Botão **"Importar do contador"** no card Faturamento → `modalImportarFatur` (drop de `.pdf,.xlsx,.xls,.csv,.xml,imagem`, prévia com dedup, `FAT_FILA`). Leitores: **`_faturParseRelatorio(txt)`** (PDF via `pexLerApoliceTexto` pdf.js/OCR; regex `mês ano val val val val` → 1 lançamento por mês, valor=Total, obs detalhada, `competencia`); **`_faturParseXml(txt)`** (NF-e `vNF`+dest xNome+chNFe / CT-e `vTPrest`+rem / NFS-e `ValorServicos`, 1 por nota, dedup por chave); **`_faturDetectExcel(grid)`** (via `PEXImport.lerArquivo`; acha cabeçalho e mapeia Mês/Ano/Data/Cliente/Saídas/Serviços/Total; mensal ou por nota; datas BR via `_impISO`). Dedup: mensal por competência (mês/ano), nota por chave/data+valor. `_faturImportConfirmar` grava em `DB.faturamento` (tira campos `_*`). **Validado ao vivo (v6.53) com o TEXTO REAL do relatório:** extraiu os **10 meses**, soma = **R$ 1.611.223,96** (bate exato com "Totais"), XML NF-e→BRF/5000/data/nº, importar inseriu e reimportar deu 0 novos (dedup ok); zero erro. Commit `40be0f3`. ⚠️ PDF escaneado depende do OCR (online). Os valores mensais são o **Total** (Saídas+Serviços) — detalhe fica na obs.

v6.52: **Financeiro — Planilha de Pagamentos (separada dos vales).** Cliente quis lançar os pagamentos que ELE fez, sem misturar com os vales dos motoristas. Nova coleção `DB.pagamentos` (backfill em ensureCollections; `clone(SEED['pagamentos']||[])`). Em `viewFinConteudo`: 4º KPI **"Pagamentos no mês"** (`i-red`), e card full-width **"Planilha de Pagamentos"** (`pagCard`) com filtro por mês (`pagMes`), tabela Data/Descrição/Categoria/Forma/Valor + `<tfoot>` com TOTAL, botão "Novo pagamento". `modalPagamento`/`salvarPagamento`/`excluirPagamento` (categorias e formas via `<datalist>`: Combustível/Manutenção/Pedágio/Fornecedor/Salário/Imposto…; Pix/Dinheiro/Boleto/Cartão…; valor via `fldR$`/`parseBRL`). **Validado ao vivo (v6.52):** 4 KPIs, card presente, criar pagamento de teste (valor 1.234,56→1234.56) apareceu na tabela+total, excluir removeu, contagem voltou; zero erro. Commit `3e319ac`. ⚠️ Possível próximo passo: importar planilha Excel de pagamentos + donut por categoria.

v6.51: **Excluir motorista apaga TUDO dele + modal de Alarme legível.** (1) `excluirMotorista` virou async e faz **cascata**: remove `DB.vencimentos` (CNH/ASO/tox/Opentech/direção), arquivos **anexados** (`todosArquivos` filtrado por motorista → `_removerAnexoSilencioso` cada, cobre IndexedDB+nuvem) e arquivos **registrados da pasta** (`DB.arquivos`), além do motorista; confirm mais explícito. (2) **Modal do Alarme Thermo King** (`modalAlarme`): as caixas "O que significa / O que fazer" ficavam **brancas com texto claro = ilegíveis** — causa: o override `#view.cyber .alarme-block` (dark) NÃO alcança o modal, que fica em `#overlay`. Refeito com marcação nova (`.alarme-hero` + badge do código com ícone/cor por categoria via `_alarmeCat`, `.alarme-cat` chip, `.alarme-aviso` âmbar) + bloco CSS **"ALARME THERMO KING (v6.51)"** escopado em `#overlay .modal .alarme-*` (fundo escuro `rgba(0,20,38,.55)`, texto `#e8f2fb`, acento neon lateral, verde no "O que fazer"). **Validado ao vivo (v6.51):** alarme com fundo escuro+texto claro (legível) + hero + categoria; exclusão de motorista de teste removeu motorista+2 vencimentos+1 arquivo (comprovante) e a contagem voltou; zero erro. Commit `6a073f9`. ⚠️ **Cliente pediu narrar em PORTUGUÊS o que estou fazendo** (ver [[narrar-em-portugues]]). A última frase dele veio cortada ("EM TODOS O...") — perguntar o que faltou.

v6.50: **Painel — "Seguros a vencer" bate exato com o que abre.** Cliente reclamou que clicar mostrava "todos". Investigado AO VIVO (v6.49): já funcionava — o KPI `iniKpiTile(...,'seguros/avencer',...)`→`#seguros/avencer`→`segFiltro='avencer'`→mostra só os 3 a vencer (clique real testado: 3 linhas, filtro 'avencer', renovBox oculto). Era **cache** do app do cliente (SW versão antiga). Mesmo assim, alinhei a contagem do KPI do Painel (`segAv`/`segCrit` agora `0<=dias<=90`, sem incluir vencidos) com o filtro do módulo Seguros (`aVencer` = `>=0 && <=90`), p/ o número do card ser SEMPRE = o que abre (evita divergência se surgir uma apólice vencida). Bump de versão força o SW a atualizar. Commit `aac484e`. **Lição:** quando o cliente diz "não funciona" e o teste ao vivo mostra que funciona, é cache — bump de versão + pedir F5.

v6.49: **Viagens — KPIs clicáveis (filtram direto o que está pendente).** Cliente: clicar em "Pendentes", "Transportes a baixar" e "Termos pallet pendentes" deve abrir SÓ o pendente correspondente. Os 3 KPIs usavam `kpi()` sem clique. Feito: novo `kpiV` (anchor `.kpi.link` com onclick que seta `viagemFiltro` + `viagemMes='todos'` p/ mostrar todos os meses, batendo com a contagem) — "Pendentes"→`emviagem` (status Pendente), "Transportes a baixar"→`pendentes` (baixado≠SIM/TSP), "Termos pallet pendentes"→**novo filtro `termo`** (`termoBaixado≠SIM`); "Viagens no mês"→filtra o mês atual. Botão "Termo pendente" add na barra `seg`. **Validado ao vivo (v6.49):** os 4 KPIs clicáveis; "A baixar"→3 linhas (=count), "Termo pendente"→13 linhas (=count, todas com Termo "Pendente"); zero erro. Commit `f6e8f37`.

v6.47/6.48: **NOVO MÓDULO PEDÁGIOS — Centro de Inteligência (tema Cyber).** O cliente mandou a fatura Sem Parar (PDF `26152227636`, R$ 1.415,55, jun–jul/2026) + 6 `.xls` (binário OLE2 — **não** legível: nem Read nem PEXImport abrem `.xls` antigo; só a PDF). Extraí do PDF **62 passagens reais** → `SEED.pedagios` (dados.js) + backfill em ensureCollections: 31 **"Pedágio"** (pagas pela empresa, débito) com totais por veículo CONFERIDOS (93,48 BBJ / 138,57 BCG / 96,91 EJZ / 47,03 IRU / 49,88 JSX / 393,30 QIO = 819,17) + 31 **"Vale-pedágio"** (débito da concessionária reembolsado pelo embarcador BRF, com nº da viagem). Campos: data/hora/placa/conc/praca/cat/valor/tipo/emb/viagem/tag; rodovia/km/sentido/cidade derivados por `_pedInfo(praca)`. **`viewPedagios`** (app.js, seção PEDÁGIOS): banner + toolbar (período `pedFiltro` Todos/Mês/Ano/Personalizado via `.seg2/.seg-b`; tipo `pedTipo`); **8 KPIs com count-up** (`pedCountUp` no pexAfterRender p/ rota pedagios; helper `kp` com `data-count`/`data-money`) — Total, Pago empresa, Vale/BRF, Passagens, Ticket médio, Custo/veículo, Praças, Concessionárias; **gráficos** (lineChart mês, donut pago×vale, barChart por veículo clicável→`pedBuscaPlaca`, donut por concessionária, barChart top praças); **mapa estilizado** `_pedMapa` (base Londrina + 10 praças como nós clicáveis→`pedPraca` drawer, rotas neon `.pdm-*`); **alertas inteligentes** `_pedAlertas` (duplicadas, TAG divergente, categoria divergente); **lista** `table.tbl` (62 linhas, `pexEnhanceTables` dá busca/ordenação/paginação) com clique→**drawer** `#pedPanel` (`.pn-panel`, reusa CSS dos pneus) `pedAbrir` (todos os campos + comprovante `badgeAnexo('pedagio',id,/./,'Pedágio')` + alertas + editar/excluir). CRUD `pedModal/pedSalvar/pedExcluir`. **Importar Extrato** (`pedImportarModal`): lê PDF Sem Parar via `pexLerApoliceTexto` (pdf.js/OCR da v6.45!) + `_pedParseSemParar` (regex acha placa "Descritivo:" + linhas `data hora conc praça cat valor D`, ignora crédito C), prévia com dedup (placa+data+hora+valor), confirma→insere. **IA Planeta** (assistente.js, branch PEDÁGIOS antes do financeiro pois "vale" cairia no guard): total, por veículo (placa frota OU do extrato), "qual veículo gastou mais", por rodovia (`BR-376`), por concessionária (PRVIAS/EPR/Araucária), por viagem, pago×vale; roteado via `_iaConsultaCurta` + trigger de rodovia. Ícones novos `toll`/`clients`/`map`; CSS "PEDÁGIOS (v6.47)" (.seg2/.ped-tag/.pdm-*/.pn-item/.pn-grid2). **Validado AO VIVO (GitHub Pages v6.47/48):** 62 passagens, KPIs [Total R$ 2.726,55 / Pago 819,17 / Vale 1.907,38 / 62], 5 gráficos, mapa 10 nós/9 rotas, tabela 62, drawer OK, parser Sem Parar extraiu 2/2 de amostra, IA respondeu total/maior(QIO R$1.500,11)/QIO/rodovia; zero erro. Commits `590a73b` (v6.47) + `102bb38` (v6.48 = IA rodovia). ⚠️ **`.xls` binário não importa** (avisar cliente: exportar como PDF ou CSV). Integração com **Viagens/Financeiro** é leve por ora (nº da viagem do vale casa por texto; aprofundar é próximo passo). Mapa é **estilizado** (posições aprox., não geo-exatas). Celular regenerado.

v6.46: **Apólice anexada aparece NA LISTA (verde "Anexado") com ver/baixar/trocar/remover.** O cliente quis o arquivo visível direto na aba (como o selo padrão do sistema), sempre disponível p/ baixar, remover ou alterar — não só dentro do modal. Novo `_segAnexo(s)`: se existe arquivo (`anexoTipo('seguro',s.id,/./)`) mostra selo verde `st ok` **"📎 Anexado"** (clica → `verArquivo`) + botões **baixar** (`baixarArquivo`) + **trocar** (`_segTrocarAnexo`) + **remover** (`excluirArquivo`); senão, botão **"Anexar apólice"** (`uploadPara('seguro',id,'Apólice')`). Injetado na célula "Apólice" de cada linha da tabela (`.seg-anexo-cell`) E no `modalSeguro` (substituiu o `badgeAnexo`). `_segTrocarAnexo(ref,oldId)` faz substituição limpa: `subirUm` do novo + `_removerAnexoSilencioso(oldId)` (remove do IndexedDB + Storage + `DB.anexos` sem confirm/router) + reload/save. Todos os botões com `event.stopPropagation()` (a linha inteira abre o `modalSeguro` no clique). CSS `.seg-anexo`/`.seg-anexo-cell` no bloco Seguros do estilo2. **Validado ao vivo (GitHub Pages v6.46):** sem anexo → 9 botões "Anexar apólice"; injetando um anexo fake p/ a apólice s2 (deslogado, nuvem intacta) → selo verde "Anexado" com `.st.ok` + ver/baixar/trocar/remover corretos, e o modal idem; zero erro de console. Publicada (commit `1803971`); celular regenerado.

v6.58: **NOVO MÓDULO "LICENÇAS E ALVARÁS" (Centro de Gestão de Conformidade) + "Conformidade ANTT" renomeada para "ANTT - RNTRC" e movida para Cadastros.** Pedido do cliente (briefing longo). **(1) Renome/mudança de lugar:** `ROTAS.antt.t` virou **"ANTT - RNTRC"**; no `renderSidebar` o item saiu do grupo *Empresa* e foi para **Cadastros**, embaixo dos outros (frota, motoristas, exames, direção, **antt**, **licencas**). **(2) Módulo novo** (rota `licencas`, ícone novo `stamp`; ícone `clock` também add ao `IC`): coleção **`DB.licencas`** + `DB.licencasRemovidas` (marca semente apagada de propósito). Cada licença tem nome, categoria, número, órgão, município, estado, emissão, validade, responsável, situação, obs, protocolo, hash, escopo (empresa/filial/veículo/parceiro), `historico[]` e `versoes[]`. **8 categorias** (`LIC_CATS`, dá p/ crescer só acrescentando linha): Alvará de Funcionamento, Vigilância Sanitária, Inscrição Estadual, Inscrição Municipal, Certidões, Cadastro, **Licença Ambiental**, **Bombeiros (AVCB)**. **Situação automática** (`licSit`) pela validade — 🟢 Vigente / 🟡 Vence em breve / 🔴 Vencida — com estados manuais 🟠 Renovação em andamento, 🔵 Protocolada, ⚪ Arquivada (`LIC_MANUAIS`); limite em `DB.config.alertaLicenca` (60 dias). **6 KPIs clicáveis com count-up** (`licCountUp` no `pexAfterRender`): Alvarás ativos, Vigilâncias Sanitárias, Licenças vencendo, Vencidas, Protocolos em andamento, Documentos anexados — cada um vira filtro (`licFiltro`) e **a contagem bate exatamente com o que abre** (lição da v6.30). Cards por titular (empresa → filiais → veículos → parceiros), chips de categoria, **pesquisa global** sem acento por qualquer campo (`_licCasa`/`_licNorm`, redesenha só a lista p/ não perder o foco). **Painel lateral** (`licAbrir`, reusa `.pn-panel`) com todos os campos + **linha do tempo** (`licTimeline` sobre `historico`). **(3) Painel de Renovação Inteligente** (o diferencial que o cliente pediu): `licAutoRenovacao()` (roda no `ensureCollections`) **abre a tarefa sozinha** quando a licença entra na faixa de 60 dias; etapas **Em preparação → Protocolado → Em análise → Aprovado → Renovado** (`LIC_ETAPAS`), checklist de documentos exigidos por categoria (`LIC_DOCS_NEC`), campo de protocolo, anexo de protocolo, e `licConcluirRenovacao` **guarda a versão anterior em `versoes[]`** e grava a nova validade/número. **(4) Upload inteligente:** `licEnviarModal` + `_licExtrair(txt,nome)` — usa o mesmo motor das apólices (`pexLerApoliceTexto` = pdf.js + OCR Tesseract) e descobre categoria, número, órgão, município, UF, emissão, validade e responsável; prévia conferível com selo Detectado/Confira/Preencher e `<select>` p/ cadastrar novo OU atualizar existente; anexa o arquivo (`subirUm`). **(5) Relatórios** (`LIC_RELS`/`licDataset`/`licExportar`): vigentes, vencendo, vencidas, por empresa, filial, estado, município, histórico de renovações e todas — exporta **PDF** (folha branca via `body.lic-printing` + `#licPrintArea`), **Excel** (.xls) e **CSV** (BOM + `;`). **(6) Integração automática:** `todosVencimentos()` passou a mesclar as licenças (`id='lic_'+id`, entidade `licenca`) → aparecem sozinhas em **Vencimentos**, **notificações**, **Painel de Controle** (KPI novo "Licenças a vencer" → `#licencas/vencendo`) e **calendário**; `modalVencimento` com id `lic_*` abre a ficha da licença; `nomeEntidade`/`tipoIcone`/`pexSearch` (busca do Ctrl+K, sem acento) tratam licenças. **(7) IA Planeta** (`assistente.js`): branch de licenças responde as perguntas do briefing — "quais alvarás vencem este mês", "existe alguma vigilância sanitária vencida", "quando vence o alvará da matriz", "mostre todas as licenças ambientais", "quais licenças não possuem arquivo anexado", protocolos em andamento e listagens por categoria. ⚠️ Precisei rotear licenças direto p/ `iaConsulta` **antes** do `_iaEhPergunta`, porque o detector antigo não reconhece "mostre" (o `\b` corta prefixos como `mostr`/`quant`). **(8) DADOS REAIS:** o cliente mandou `Documentos Jurídicos/Alvará e Vigilâcias Sanitárias 2026.pdf` (7 páginas, **100% digitalizado, sem camada de texto**) — extraí por **OCR** (pdf.js + Tesseract 'por' rodando no mcp Browser) e cadastrei **8 licenças** no SEED (`dados.js`) + `importarLicencasSeed()` (id fixo lic1..lic8, não duplica, chega mesmo em aparelho que já tinha a coleção vazia): Alvará de Funcionamento **26/2026** (Carambeí/PR, val. 19/01/2027), Alvará Sanitário da matriz **13/2026** (emissão 27/01/2026), **5 Licenças Sanitárias de veículo** (SESA 465/2013) para as carretas **IOW-1141, EOF-5A47, MDD-5C62, AMB-2928, NTY-8B66** (chassis conferem com a frota) e a **Inscrição Municipal 32885**. CSS: bloco "LICENÇAS E ALVARÁS (v6.58)" no fim do `estilo2.css` (cyber premium: vidro, glow, cards em cascata, trilha de etapas, timeline, chips, folha de impressão). **Validado AO VIVO (file://, ?v=123):** 8 licenças/8 cards, KPIs [1 alvará · 6 vigilâncias · 0 vencendo · 0 vencidas], agrupamento empresa+5 carretas, 7 licenças entrando em `todosVencimentos`, fluxo de renovação completo testado (abre → marca doc → etapa → conclui → versão anterior guardada → volta a Vigente), relatórios com 11 colunas, IA respondendo as 5 perguntas do briefing, extrator acertando os documentos REAIS do cliente (categoria/número/órgão/município/UF/emissão/validade), 14 telas antigas OK, mobile 375px sem overflow, **zero erro de console**. ⚠️ O OCR levou vários minutos por página em escala alta — o truque é medir a página e usar escala ~1700px de largura (`ocr2.html` do scratchpad); abas em segundo plano são suspensas e travam o OCR. ⚠️ As licenças sanitárias de veículo **não têm número** no documento (o "465/2013" é a resolução — o extrator agora ignora citações de lei/resolução/portaria ao procurar o número).

v6.59: **CENTRAL DE PROCESSAMENTO INTELIGENTE DE DOCUMENTOS — um pipeline único para TODO documento (`assets/central.js`, arquivo novo, ~1.020 linhas).** O cliente apontou o problema-raiz certo: o sistema tinha **um leitor por tela** (`modalImportarFatur`, `modalImportarDescarga`, `modalImportarViagem`, `modalEnviarApolice`, `licEnviarModal`, `abastecNfUpload`, `pedImportarModal`, `automacaoProcessar`), cada um com seu parser. Agora existe **uma porta de entrada só**. **Rota `#central`** (menu grupo Principal, logo após o Painel, com badge de pendências; ícone `spark`). **Pipeline fixo** (`cpidProcessar`, 7 etapas animadas em `CPID_ETAPAS`): Identificar formato → Ler → Classificar → Extrair → Relacionar → Conferir → (confirmação) → Atualizar módulos → Arquivar → Log. **(1) Formatos** (`cpidFormato`, por extensão **E** magic bytes): PDF, XML, XLSX/XLS/CSV, JPG/PNG, **DOCX**, **E-mail .eml**, **ZIP**. **(2) Leitura** (`cpidLer`) com as regras que o cliente exigiu: **XML nunca passa por OCR** (lê estruturado com DOMParser); **PDF** usa pdf.js e só cai no **Tesseract** quando não há texto (via `pexLerApoliceTexto`); **Excel** usa **SheetJS** quando há internet (lê inclusive **.xls binário antigo**, que o `PEXImport` não abria) e degrada para o `PEXImport` offline; **DOCX** e **ZIP** são lidos pelo unzip próprio (expus `unzip`/`inflateRaw`/`decode` no `PEXImport` — sem biblioteca externa); **.eml** faz parse MIME e extrai os anexos base64. **ZIP e e-mail devolvem os arquivos de dentro, que voltam ao INÍCIO do mesmo pipeline** (`cpidRodar`). **(3) Classificação** (`cpidClassificar`) **semântica com % de confiança**, na ordem: primeiro o **banco de modelos aprendido**, depois as tags do XML (99%), depois os padrões fiscais BR (pedágio 96, relatório 97, abastecimento 94, apólice 93, licença 93, CT-e 92, NFS-e/NF-e 90, boleto 88, fatura 70) — e mostra na tela **por que** classificou assim. **(4) BANCO DE MODELOS que APRENDE** (`DB.docModelos`, `cpidAprender`/`cpidModeloPor`): ao confirmar um documento, guarda o emitente (CNPJ + nome) e o tipo; **na próxima vez reconhece pelo emitente**, mesmo com nome de arquivo genérico — exatamente o "Posto Cupim → aprende → Shell → aprende" que o cliente pediu. Se o usuário **corrigir o tipo** no `<select>`, o pipeline **reprocessa a extração** (`cpidCorrigirTipo`) e o modelo passa a valer com a correção. Aba "Modelos" lista tudo com acertos/último uso e permite esquecer. **(5) Extração** (`cpidExtrair`) por significado, reaproveitando os extratores JÁ VALIDADOS como plugins (`parseCteXml`, `extrairAbastecimento`, `_faturParseRelatorio`, `_faturParseXml`, `_faturDetectExcel`, `_faturParseNotaTexto`, `_pedParseSemParar`, `_apoliceMatch`, `_licExtrair`, `_descDetectar`, `_viagemDetectar`, `PEXImport.detectarVencimentos`) + extratores novos de XML (NF-e/CT-e/NFS-e: chave, emitente, CNPJ, destinatário, data, produtos, ICMS, peso, cidade, UF, CFOP). **Detalhe importante:** uma **NF-e cujo produto é diesel/arla vira ABASTECIMENTO, não faturamento** (`_tipoReal`). **(6) Planilha com cabeçalho diferente**: `CPID_SINONIMOS` entende PLACA=VEÍCULO=CARRO=FROTA, VALOR=R$=TOTAL, LITROS=QTDE=VOLUME, DATA=ABAST.=COMPETÊNCIA, KM=HODÔMETRO etc.; `cpidMapearColunas` acha a linha de cabeçalho e **deduz o destino** pelo conjunto de colunas. **(7) Relacionamento** (`cpidRelacionar`): placa→veículo da frota (`veiculoByPlaca`), motorista (`_iaMotorista`), avisa quando a placa não é da frota. **(8) Conferência** (`cpidConferir`): pendências por tipo + **dedup** (não deixa lançar duas vezes). **(9) Aplicação** (`cpidAplicar`): grava em Abastecimentos, CT-e, Faturamento, Pagamentos, Pedágios, Licenças, Descargas, Viagens ou anexa em Seguros — e **diz na tela quais módulos foram atualizados**; depois **arquiva** o arquivo (`subirUm`, na entidade certa: licença/seguro/veículo/empresa) e grava **log auditável** (`DB.docLogs`: quando, usuário, arquivo, formato, tipo, confiança, OCR sim/não, lançamentos, módulos, pendências, tempo). Aba "Logs" com histórico. **(10) Ponte com as telas** (`cpidEnviar`/`cpidEscolher`): os botões de Seguros ("Enviar apólice"), Licenças ("Enviar documento"), Pedágios ("Importar extrato") e Financeiro ("Importar do contador") **agora abrem a Central** — a entrada é uma só. Os modais antigos continuam no código como conferência específica (não foram removidos). CSS "CENTRAL DE PROCESSAMENTO (v6.59)" no fim do `estilo2.css`. `central.js` entrou no `index.html` (depois do app.js) e no `build_celular.sh`. **VALIDADO AO VIVO (file://, ?v=126)** com documentos construídos na hora: **XML de NF-e do "AUTO POSTO CUPIM"** → 99% confiança, sem OCR, virou **Abastecimento** (posto, 520 L, R$3.540, ICMS 425,30, Londrina/PR, chave) e pendência "placa" correta; **CSV com cabeçalhos VEICULO/ABAST./R$/QTDE/HODOMETRO** → mapeou placa/data/valor/litros/km, converteu "3.540,00"→3540 e data BR→ISO, e **relacionou com o veículo v1 (IRU-4G62)**; **ZIP montado na hora** → descompactou 2 arquivos e cada um voltou ao pipeline; **.eml com anexo XML** → extraiu o anexo e classificou como abastecimento do POSTO SHELL; **aplicação real** gravou em `DB.abastecimentos`, arquivou, e listou "Abastecimentos · Financeiro · Médias/Custo por km · Painel · Documentos"; **aprendizado comprovado** — na 2ª vez, arquivo chamado "documento_qualquer.xml" do mesmo posto foi reconhecido com "modelo aprendido de AUTO POSTO CUPIM LTDA"; 18 telas antigas OK; **zero erro de console** (dados de teste removidos ao final). ⚠️ **Honestidade sobre "IA":** não há LLM no navegador (app estático, sem backend/chave paga) — o que existe é **classificação por padrões + leitura semântica por rótulos + banco de modelos que aprende**, que na prática entrega o comportamento pedido. Google Document AI / Azure / Textract continuam sendo o próximo passo **quando houver backend** (Supabase Edge Function) — o ponto de extensão está isolado em `cpidClassificar`/`cpidExtrair`. ⚠️ **SheetJS, pdf.js e Tesseract vêm de CDN** → só com internet; offline o sistema cai nos leitores internos (e `.xls` antigo avisa que precisa de internet ou de salvar como .xlsx).

v6.60: **MONITORAMENTO REESTRUTURADO — componente de verdade (`assets/monitoramento.js`, arquivo novo).** Briefing longo do cliente: transformar o mapa da Início num centro de monitoramento operacional, **sem mudar tamanho, posição nem o resto do dashboard**. Mantidos: altura **452px (desktop) / 380px (mobile)** — exatamente as de antes —, o cabeçalho (título "Monitoramento", "● AO VIVO", botão "Viagens" → `#viagens`), os 4 locais (Londrina + Maringá, Paiçandu, Cambé) e o painel lateral `iniAbrirVeic`. **Ibiporã saiu** (o cliente listou só 4 destinos e citou Ibiporã como cidade *futura*) — voltar é descomentar 1 linha. **Arquitetura separada, para o GPS entrar sem reescrever nada:** `MON_LOCAIS` (lat/lon **reais** dos municípios) → `MON_ROTAS` (com espaço para `polyline` real) → `monProjetar` (lat/lon→x,y; trocar por mapa real mexe só aqui) → `MonProvider` = **`OfflineSimulationProvider`** (um `GPSProvider` com a mesma interface entra no lugar) → `MonSim` (simula, não desenha) → `monRender` (desenha, não simula). **Visual:** Londrina virou base sóbria (marcador hexagonal + anel tracejado girando devagar + rótulos **LONDRINA / BASE OPERACIONAL** abaixo) — **saíram o radar girando, as ondas e o brilho exagerado** da v6.42. Camadas: fundo, grid, textura hexagonal, rotas (linha-base + principal + partículas), cidades, veículos, status, informações — tudo em opacidade baixa. **Veículos:** cavalos e motoristas **reais** do cadastro; deslocamento por `requestAnimationFrame` + `getPointAtLength` (posição **e** orientação seguem a rota). Ao chegar na ponta o caminhão **para no pátio 4-7s** e só então inverte — corrigiu o giro de 180° em movimento (salto máximo de ângulo medido caiu de **180° para ~20°/quadro**, com interpolação angular pelo caminho mais curto). **Tooltips** (mouse e toque): veículo (modelo, placa, motorista, destino, velocidade, previsão, status) e cidade (destino operacional, veículos em trânsito, atualização, status). **Extras:** legenda discreta, contadores (ativos / em trânsito / destinos / rotas ativas), **sincronização com o relógio real**, controles redesenhados (+ / − / centralizar na base / camadas) e o selo honesto **"Simulação local"** — nada é apresentado como GPS real. **Performance:** um único rAF, desenho só por `transform`, pausa ao sair da Início e quando a aba está oculta, respeita `prefers-reduced-motion`. **Validado ao vivo:** 17 telas OK, altura 452/380 preservada, zero erro de console, mobile 375px sem overflow com toque de 38px e nada sobreposto, ciclo completo de ida/pátio/volta, **zero colisão de rótulos** (medida pelas caixas de texto — Cambé fica a 13 km de Londrina e os nomes se sobrepunham; agora o rótulo vai sempre para o lado oposto à base). Commits `f7daf63` + `1dab6f1`. ⚠️ **Não consigo ver a tela aqui** (o painel não tira screenshot) — a validação é estrutural e por medição; **o julgamento visual final é do cliente**. ⚠️ `iniMapZoom`/`iniBaseWx`/`iniCavalos` continuam no app.js: as duas primeiras viraram código morto inofensivo, `iniCavalos` ainda é usada pela simulação.

v6.61: **Monitoramento — mapa geográfico de verdade, com muito mais detalhe (e SEM motorista).** O cliente achou a v6.60 "extremamente simples" e apontou um erro sério: **eu associava motorista à placa por adivinhação** (`motoristas[i % n]`). ⚠️ **REGRA:** o sistema **não sabe** quem dirige qual veículo (só saberia com apontamento de viagem ou rastreador) — então o mapa mostra **somente a PLACA do cavalo**, agora inclusive escrita ao lado do caminhão. Nunca voltar a inferir motorista. **Precisão:** `monCalcProjecao` passou a usar **escala uniforme** (longitude corrigida por `cos(lat)`), então distâncias e direções ficam proporcionais às reais — não é mais um esquema esticado. **Distâncias reais por Haversine** (`monDistKm`/`monRotaKm`), conferidas: Londrina→Cambé **12 km**, Londrina→Maringá **80 km em linha reta** e **108 km pela BR-369** (bate com a rodovia real). Aparecem sob o nome do destino, no rótulo da rota e alimentam o ETA. **Mapa muito mais completo:** **11 cidades de referência** do corredor da BR-369 (`tipo:'referencia'` — contexto, não destino: Rolândia, Arapongas, Apucarana, Jandaia do Sul, Mandaguari, Marialva, Sarandi, Ibiporã, Jataizinho, Sertanópolis, Bela Vista do Paraíso); **`MON_VIAS`** = malha viária onde **cada rodovia é a sequência REAL de cidades por onde passa**, e as rotas seguem esse traçado (curva Catmull-Rom por `monCaminhoDe`) em vez de uma curva inventada; **anéis de 50 e 100 km** da base; **barra de escala em km**, **bússola** e **grid de coordenadas** reais. **Animações novas:** varredura de dados atravessando o mapa (`mon-varre`), entrada em cascata dos destinos, partículas nas rotas, pulso das cidades. **Detalhe técnico:** o `<g>` do veículo só translada e **apenas o ícone gira** (`.mon-v-ico`) — assim a placa fica sempre legível. Tooltip do veículo ganhou barra de progresso, km que faltam e rodovia; o da cidade mostra coordenadas. Botão de camadas virou 3 estados (tudo → sem textura → só rotas e destinos). **Validado:** zero colisão de rótulo (medido entre grupos distintos; o rótulo da rodovia caía sobre Cambé e foi movido para o meio da linha), nenhum ponto fora da área, 11 telas OK, mobile 375px sem overflow (referências/bússola/km escondidos no celular), zero erro de console. Commit `5322579`. ⚠️ As coordenadas são do **centro urbano** dos municípios e a malha viária é o **encadeamento real de cidades**, não a geometria fina da pista — quando entrar o GPS, troca-se `MON_ROTAS[].caminho` por polyline real e `monProjetar` pela projeção do mapa.

v6.62: **BUG DE NAVEGAÇÃO CORRIGIDO — filtro travava ao chegar por um card do Painel.** Relato do cliente: clicar em "Documentos vencidos" no Painel abria Vencimentos certo, mas **clicar em outra faixa ali dentro não mudava nada** — só funcionava saindo e entrando em Vencimentos. **Causa-raiz:** o KPI faz `vencFiltro='d10'; router()` e, logo depois, o router executava `if(arg) vencFiltro=arg` lendo o hash antigo (`#vencimentos/venc`) e **desfazendo o clique**. Sem argumento no hash o mesmo código funcionava — por isso "sair e entrar" resolvia. **Correção no router:** nas rotas em que o argumento é um **filtro da própria tela** (`_ROTA_FILTRO` = vencimentos, km, documentos, pedagios, seguros, licencas, central — mais `pneus/limite`), o argumento **sai do endereço** via `history.replaceState(null,'','#'+rota)` logo depois de aplicado. **Deep-links por ID continuam intactos** (`#frota/v1`, `#motoristas/m1`, `#manutencao/v1`, `#pneus/v1`, `#viagens/<id>` — conferidos). Também corrigidos **dois casos piores**, em que o filtro era zerado a CADA render e os botões da própria tela nunca funcionavam: `km` (`kmFiltro=arg||'todos'` → `if(arg) kmFiltro=arg`) e `pneus` (`pneusFiltro=(arg==='limite')?'limite':'todos'` → só define quando o arg vem). **Validado no caminho exato do cliente** (local e no ar): Painel → Documentos vencidos = 2 vencidos; clicar em "≤10 dias" passa a 1 ("Vence em até 10 dias"); "11 a 20 dias" = 2; Seguros 4→9 linhas; Licenças 0→8 cards; 19 telas OK; zero erro de console. Commit `9cc7e67`. ⚠️ **Padrão a seguir:** se criar uma tela nova cujo `arg` do hash seja filtro, **acrescente a rota em `_ROTA_FILTRO`** — senão o mesmo bug volta.

v6.63: **ANTT sem o banner do topo — ações junto do selo ATIVO.** A pedido do cliente, o `<div class="banner">` "Conformidade ANTT — RNTRC" (com o texto "Registro Nacional de Transportadores…") foi **removido**, e os botões **"Editar dados"** e **"+ Veículo"** passaram para o **lado direito do próprio card de situação**, na mesma linha do selo **ATIVO** (`.antt-acoes` com `margin-left:auto`; card ganhou a classe `.antt-topo`). Fica mais limpo e sem repetir a informação — o título da aba já diz "ANTT - RNTRC". Nada mais mudou: KPIs, "Dados do registro", "Base legal e comprovante" (com o anexo do extrato) e a tabela dos 12 veículos seguem iguais. No **celular** (≤860px) os botões descem para uma linha própria dentro do card e dividem a largura (`width:100%` + `flex:1`), sem estourar a tela. **Validado:** desktop 1360px com badge e botões no mesmo centro vertical (Y=189) e botões a 23px da borda direita; celular 375px sem overflow e com os dois botões dentro do card; zero erro de console. Commit `d0da766`.

v6.64: **Monitoramento com MAPA CHEIO, no estilo da imagem-referência do cliente.** Ele mandou um print de um centro de monitoramento (fundo de carta escura, base com anéis, cidades com temperatura e "N VEÍCULOS", rotas pontilhadas, controles à esquerda) e pediu para chegar o mais perto possível **mantendo os mesmos destinos**. **(1) O mapa preenche o card:** `monMedir()` calcula o **viewBox conforme o formato REAL do card** (altura fixa 580, largura = 580 × proporção do card) e o SVG é injetado por `monDesenhar()` **depois** de medir — daí preenche 100% sem faixa vazia e **sem cortar nada** (com `slice` de viewBox fixo, Paiçandu ficava de fora). Redesenha sozinho ao redimensionar a janela (listener com debounce de 250 ms). **(2) Fundo gerado por código**, determinístico (PRNG com semente fixa) e **sem internet**: relevo desfocado (16 manchas), **86 vias secundárias** irregulares, **manchas urbanas** (grade inclinada) junto de cada cidade, textura hexagonal e vinheta. **(3) Base** com 4 anéis concêntricos + halo + núcleo, rótulo acima com LONDRINA / BASE OPERACIONAL e um anel que pulsa. **(4) Destinos** com anéis concêntricos, nome, temperatura e **selo "N VEÍCULOS"** que se atualiza sozinho (`monPintarBadges`, a cada 5 s). **(5) TEMPERATURA REAL por cidade** — `monClimaCarregar()` usa **Open-Meteo** (mesma fonte do cockpit, sem chave) numa **única chamada** com as coordenadas reais que já tínhamos; cache de 30 min em `localStorage['pex_mon_wx']`. **Sem internet o mapa funciona igual e a temperatura simplesmente não aparece** — nada é inventado. **(6) Rotas** pontilhadas com brilho e partículas. **(7) Controles à esquerda**, como na referência. **⚠️ Rótulos:** cada cidade escolhe o lado que **realmente cabe** (mede o texto contra a borda do viewBox — no celular o card é estreito e Paiçandu escapava), e cidades vizinhas (**Maringá e Paiçandu ficam a 12 km**) afastam o rótulo 70px na vertical para não colidir. **Validado em 3 formatos** (1440px, card estreito 1.11 e celular 375px): mapa preenche, **nenhuma cidade cortada, nenhuma colisão de rótulo**, 5 veículos com placa, selos corretos, zero erro de console; e no ar a temperatura real chegou (19° nas 4 cidades). Commit `d130139`. ⚠️ O fundo é uma **representação** (relevo/vias secundárias são geradas, não cartografia real) — a malha principal e as posições das cidades é que são reais.

v6.65: **Monitoramento virou CARTA TOPOGRÁFICA (a v6.64 tinha ficado apagada).** O cliente disse "achei muito fraco". Causa: **opacidades baixas demais** (0,10) e fundo ralo — some num card de 540×450. Fundo refeito, muito mais denso (tudo gerado por código, semente fixa, sem internet): **50 curvas de nível** (é o que dá cara de carta e não existia), **7 rios**, **150 vias secundárias** (eram 86), **manchas urbanas com ÁREA preenchida + quadras** (antes só riscos) e 20 manchas de relevo. Contraste reforçado em tudo: rotas 2,6→3,4 com glow bem mais forte, anéis das cidades .30→.52, anéis da base até .50, núcleo branco, nomes maiores com **contorno escuro** (`paint-order:stroke`) p/ ficarem legíveis sobre o mapa, veículos com glow ciano. Rótulos: além de Maringá×Paiçandu, **a BASE entrou na conta** (Cambé fica a 12 km de Londrina e batia no "BASE OPERACIONAL"). Commit `8322ac7`.

v6.66: **Mais cidades, rotas melhores, veículos maiores e mais lentos** (pedido do cliente). **(1) 23 cidades** (eram 15): entraram **Astorga, Jaguapitã, Mandaguaçu, Florestópolis, Primeiro de Maio, Assaí, Tamarana e Califórnia**, com coordenadas reais e `tipo:'referencia'` — **os destinos operacionais continuam sendo Cambé, Maringá e Paiçandu**. **(2) Malha viária de 5 → 11 rodovias** (PR-457, PR-090, PR-444, BR-376, PR-445, PR-218 leste…), com **15 placas** espalhadas. **(3) Rotas melhores:** rodovia não é reta entre duas cidades — **`_monSinuoso()`** acrescenta pontos intermediários (1 a cada ~70px) com desvio lateral suave e **determinístico** (semente vinda da própria posição, então não treme a cada quadro), e o traçado passou a serpentear como via de verdade. **(4) Veículos maiores:** **28×15** (eram 18×8), agora com carreta, cabine, vidro, farol, **3 rodas** e sombra; placa maior. **(5) Bem mais lentos:** `escala` da simulação 0,0009 → **0,00011** — a rota inteira leva **~2 min** (era ~15 s). **(6) Mais informação no painel:** velocidade média, **próxima chegada** (hora + destino) e o tamanho da malha (rotas · cidades). **Validado** em 1440px e 375px: nada cortado, nenhuma colisão de rótulo, 823 elementos SVG (estáticos), zero erro. Commit `2b153db`.

v6.67: **48 cidades, 23 rodovias e veículos de volta ao ritmo ágil.** **(1) Cidades 23 → 48**, todas com coordenadas reais e `tipo:'referencia'` (os destinos operacionais seguem só Cambé, Maringá e Paiçandu): vale do Paranapanema (Porecatu, Alvorada do Sul, Centenário do Sul, Lupionópolis, Prado Ferreira, Miraselva, Guaraci), região de Maringá (Colorado, Iguaraçu, Ângulo, Flórida, Munhoz de Melo, Nova Esperança), leste (Uraí, Leópolis, Sertaneja, Rancho Alegre, Cornélio Procópio, Santa Mariana) e sul (Sabáudia, Pitangueiras, Cambira, Marumbi, Rio Bom, Bom Sucesso). **(2) Rodovias 11 → 23** (PR-160, PR-436, PR-538, PR-340, PR-317, PR-082, PR-466, BR-369 leste e sul…), com **33 placas** no mapa. **(3) Velocidade de volta ao original** (`escala` 0,00011 → **0,0009**): a rota inteira leva **~14 s** — o cliente pediu para voltar a ser mais rápido. **(4) Anti-encavalamento dos nomes:** com 44 pontos de referência os rótulos se sobreporiam; quem está perto de outro já colocado **joga o nome para baixo** (`_refPost`), e **no celular ficam só os pontos** (`.mon-r-nome{display:none}` + placas de rodovia ocultas). **Validado:** 1264 elementos SVG (estáticos), **5,4 ms por quadro** (limite 16,7 para 60 fps), nada cortado, nenhuma colisão nos rótulos principais, zero erro. Commit `896d6bc`.

### 🟡 ESTADO EM 30/08/2026 — v8.0 (sinal verde no Painel + a marca sai de cena na abertura) — **LOGO AINDA PENDENTE**

Três pedidos numa mensagem só. **Dois entregues, um recusado pelo cliente** — leia o terceiro antes de mexer na marca.

**(1) Cartão "Documentos em dia" no Painel, com sinal verde.** Entrou como **3º cartão** da tira, logo depois de *Conjuntos ativos* e *Motoristas ativos*, exatamente onde ele pediu. Fonte: `fEmDia` (`diasAte > 30`), a mesma fatia verde do donut. Abre `#vencimentos/emdia`. A classe nova é **`.ini-kpi.ok`** — o espelho do `.crit`: barra e ícone verdes **mais um ponto verde pulsante no canto** (`::after`), que é o "sinal" que se enxerga sem ler o número. Tem par no acabamento neon (`.ini-cmd .ini-kpi.ok`) e o stagger foi estendido até `nth-child(11)`, senão os 3 cartões novos entrariam sem cascata.

> **Regra nº 3 do projeto conferida de verdade:** cartão = **50**, tela que ele abre = **50 linhas** e rodapé **"50 documento(s) em dia"**. Medido pelo caminho real: cliquei no `href` do próprio cartão e li o `#view` (não montei o objeto à mão).

**(2) A escrita "PLANETA EXPRESS" saiu da abertura.** O cliente mandou print com um X por cima do letreiro dourado: **a logo já traz o nome**, então o texto repetia a marca. Saíram o `<div class="sp-title">` do `index.html` e a regra `.sp-title` do `estilo2.css` (inclusive a do `@media 700px`). Ficam **logo, linha, "Sistema de Gestão Operacional" e a barra carregando**. Sem o texto no meio, os tempos de entrada encurtaram (linha `1.1s → .55s`, tag `1.3s → .75s`, barra `1.5s → .95s`) e a linha herdou o respiro do texto (`margin-top 22 → 44px`).
**O mesmo defeito estava na tela de LOGIN** (`<h2>PLANETA EXPRESS</h2>` embaixo da logo, em `mostrarLogin()`): saiu junto, e a `.login-logo` cresceu `78 → 96px` para ocupar o lugar. A regra `.login-box h2` foi removida do CSS.

**(3) 🚫 A LOGO NOVA — NÃO REFIZ, E NÃO TENTE REDESENHAR.** O cliente anexou uma arte **preta e dourada** (monograma "P" em ouro + PLANETA EXPRESS em serifa dourada sobre fundo quase preto) e pediu para trocar em todo o sistema. **O anexo do chat não vira arquivo em disco.** Eu reconstruí a arte em vetor (SVG: "P" geométrico com bisel + Cinzel do Google Fonts, renderizado a PNG pelo Chrome headless) e **o cliente reprovou na hora: "não mude para essa logo, ficou horrível"**. Nenhum arquivo de marca foi tocado — a reprodução morreu no rascunho.
**O caminho certo:** pedir para ele **salvar o PNG na pasta** (ex.: `Sistema Planeta Express\assets\logo-original.png`) e gerar as **três** variantes a partir do arquivo dele — `logo-claro.png` (interface escura), `logo-escuro.png` (relatório A4, papel branco) e `logo-marca.png` (só o "P", selos de 36–44px) — mais o ícone quadrado (`logo.png`/`logo-sm.png`). Os pontos de troca são 8: `index.html` (ícone ×2, splash, `relLogoDark`, sidebar, topbar) e `app.js` (`ini-brand .mk` ~1039, `etica-logo` ~5312, `login-logo` ~7183).
⚠️ A arte é **ouro**, mas o tema do sistema é **ciano** desde a v7.7 (o ouro foi reprovado na v7.6). Ele quer o ouro **na marca**, não no tema — não repita a v7.6.

**Versão:** assets `?v=203`, cache `planeta-express-v8-0`, rodapé `v8.0`, celular reconstruído (3,90 MB). **Publicada** no commit `919fbc4` — os itens (1) e (2) foram ao ar; o item (3), a logo, segue pendente do arquivo do cliente.

### ✅ ESTADO EM 30/08/2026 — v7.9 (agora o tom é MESMO quase preto)

O cliente cobrou: *"você não usou o tom mais escuro no sistema, corrija, eu quero um tom quase preto como esse"*. **Ele tinha razão, e o diagnóstico interessa:** a base escura da v7.6 estava lá (`--void #050609` etc.) — mas **coberta por gradientes azuis** que pintavam por cima dela. O sistema continuava parecendo azul-petróleo.

**Os culpados eram lavados de tela inteira** (não os detalhes): `.main`, `#view.cyber`, `.ini-cmd` (duas regras) e `.sidebar::after` tinham `radial-gradient` do acento em **.08 a .22 de opacidade**, mais fundos sólidos azuis (`#132433`, `#0b1522`, `#102a46`, `#020a14`). Agora o brilho do acento é **.025–.05** e o sólido é `var(--bg)`/`var(--void)`.

> **A lição:** trocar o token da base não basta se houver camada por cima. Ao mudar o tom, **procure os `radial-gradient` de escala de página** — são eles que decidem a cor que o olho vê.

**O acento ciano continua** nos botões, ícones, bordas, itens ativos e gráficos — só saiu do *fundo*.

**Validado no Chrome headless — 18 asserções, zero falha:** `body`, `.main` e `.sidebar` medidos por **luminância < 0,08**; cada token da base conferido para o **azul não superar o vermelho em mais de 14** (é o que separa "quase preto" de "azul escuro"); os gradientes de página sem azul forte; o botão ainda ciano; contraste de texto nas 26 rotas; 26 rotas, 21 relatórios e a folha A4 branca.

### ✅ ESTADO EM 30/08/2026 — v7.8 (a logo preenche o selo; acabou o quadrado branco)

O cliente mandou print do splash e cobrou, com razão: *"essa logo tem que preencher esses quadros, olha que coisa amadora"*.

**Eram DUAS causas somadas** (medidas, não chutadas — desenhei os PNG num canvas e li os pixels):
1. **O arquivo era um quadrado branco OPACO.** `logo.png` e `logo-sm.png`: **0% de pixels transparentes**, cantos `#ffffff`. Não era o CSS.
2. **A arte tinha margem enorme:** ocupava **828×627 dentro de 1024×1024** — ~50% da imagem era espaço vazio. Daí "não preencher".
   E ainda: os contêineres (`.sp-logo`, `.logo-badge`, `.tb-logo`, `.mk`, `.login-logo`, `.etica-logo`) tinham `background:#fff`.

**TRÊS ARQUIVOS NOVOS**, gerados da arte original por canvas (alpha = escuridão do pixel, então a antialiasing fica suave) e **recortados na caixa da tinta**:
- **`logo-claro.png`** — arte BRANCA, fundo transparente → interface escura (splash, login, menu, Ética).
- **`logo-escuro.png`** — arte ESCURA, fundo transparente → **relatório A4, que é papel BRANCO**.
- **`logo-marca.png`** — só o monograma "P" → selos pequenos (topbar 36px, Painel 44px), onde o texto "PLANETA EXPRESS" saía ilegível.

⚠️ **A ARMADILHA DO RELATÓRIO:** o `relLogoSrc()` pegava o `src` de uma `<img>` já carregada da interface. Com a logo da interface agora BRANCA, a folha A4 sairia com **logo invisível no papel**. Resolvido com uma `<img id="relLogoDark">` escondida no `index.html` (a primeira da lista do seletor), que o build também converte em base64 — funciona na pasta e no arquivo único.

⚠️ **O ícone do app continua sendo o `logo.png` quadrado opaco** (`__LOGO__` no build, `uri_icon`): ícone de celular fica melhor opaco, e branco-sobre-transparente sumiria na tela inicial.

**Se um dia trocar a arte da marca: gere as TRÊS versões**, sempre com fundo transparente e recortadas.

**Validado no Chrome headless — 13 asserções, zero falha:** cada PNG conferido **por pixel** (transparência ≥ 25% e brilho médio da arte provando que a clara é clara e a escura é escura); o Painel usando o monograma; o Ética usando a clara; `relLogoSrc()` apontando para a escura e a folha trazendo a logo; 26 rotas e 21 relatórios limpos. Celular: 3,90 MB (subiu ~0,5 MB porque agora vão três marcas embutidas).

### ✅ ESTADO EM 30/08/2026 — v7.7 (o ouro saiu; fica a base escura com as cores originais)

O cliente viu a v7.6 e foi direto: *"não gostei do dourado, utilize somente o fundo mais escuro como fez e volte a cor dos ícones e botões como estava antes"*.

**A volta custou 3 linhas de CSS** — e é a prova de que a tokenização da v7.6 valeu:
```css
--au:#00e5ff;  --au2:#00e5ff;  --au3:#0077ff;
--au-rgb:0,229,255;  --au3-rgb:0,119,255;
```
Nenhum hex foi caçado no meio do arquivo. **Trocar o tema de novo é mexer só aqui.**

**O que FICOU da v7.6** (o cliente pediu para manter): a base escura inteira (`--void #050609` … `--surf3 #161C27`), o `body` escuro, a `theme-color` do celular, o brilho de vidro dos cards em `.05` (em `.55` virava faixa cinza) e os tokens `--text/--card/--content` apontando para a base escura.

**O que VOLTOU ao original:** o acento ciano/azul em botões, ícones, gráficos e bordas; os gradientes dos gráficos no JS; e as **barrinhas coloridas do topo dos KPIs** (azul/verde/laranja/vermelho por posição) — eu as tinha unificado em ouro por conta própria na v7.6 e ele pediu as cores de antes.

**Como o JS voltou sem risco:** o backup pré-v7.6 do `app.js`/`contabilidade.js` foi conferido com um `diff` que **apaga todas as cores dos dois lados** — o resultado veio vazio, provando que a v7.6 só tinha mexido em cor. Aí deu para restaurar os arquivos inteiros sem perder nada da v7.5.

**⚠️ O dourado que sobra é da LOGO** (5 ocorrências, `#c9a227` e vizinhas no splash/login): já existia antes da v7.6, não é resto da repintura.

**Validado no Chrome headless — 20 asserções, zero falha:** os 5 tokens da base escura no valor exato e `body` escuro por luminância; os 4 tokens de acento de volta no ciano/azul; botão e ícone em ciano e **sem ouro**; a barrinha do 1º KPI de volta ao azul; varredura de contraste nas 26 rotas (zero texto escuro no escuro); 26 rotas e 21 relatórios limpos.

### ✅ ESTADO EM 30/08/2026 — v7.6 (novo tom do sistema: base quase preta + acento OURO)

O cliente pediu troca estética com um conceito — *precisão, tecnologia, exclusividade, requinte* — e passou a **base escura** pronta (`--void #050609` … `--surf3 #161C27`) mais a **alternativa violeta**. Faltava o acento principal; ele escolheu **ouro** (`--au #C9A227`, `--au2 #E8C766`, `--au3 #8F7318`) e **sem botão de troca** — o violeta fica escrito no CSS, pronto, mas desligado.

**Como foi feito (importa para as próximas):** o sistema tinha **~1.000 cores fixas** espalhadas (ciano `#00e5ff`, azuis-petróleo de superfície, azuis-claros de texto). Elas foram trocadas **de uma vez** por tokens, por famílias: acento → `--au/--au2/--au3`, texto → `--tx/--tx2/--tx3`, superfície → `--surf/--surf2/--surf3`, e os `rgba()` correspondentes por `rgba(var(--*-rgb), …)`. Foram **924 trocas no CSS** e as de acento no JS (gráficos, KPIs, donuts).

> **Daqui em diante, mudar a cara do sistema é mexer no `:root` do `estilo2.css`** — não caçar hex no meio do arquivo.

**⚠️ O RELATÓRIO A4 FICOU DE FORA de propósito** (linhas do bloco `.rel-*`): é documento em papel branco e não acompanha o tema. O teste confirma que a folha continua clara com texto escuro.

**🚨 TRÊS COISAS QUE A TROCA EM MASSA QUEBROU — e é a lição:**
1. **`--text` virou superfície.** `#0e1a2c` estava no mapa de superfícies e `--text` usava esse valor → o token de TEXTO passou a apontar para uma cor escura; texto sumiria no escuro em qualquer canto que ainda leia `--text`. Corrigido e comentado no código.
2. **Faixa cinza no topo de todo card.** `.card::before` tinha um brilho de vidro em `rgba(255,255,255,.55)` — discreto no tema claro/azul, mas sobre quase-preto virava uma **barra cinza**. Baixado para `.05`.
3. **O fundo da aplicação continuava CLARO.** O `body` era `--content:#e9edf3` e só não aparecia porque o `.main` cobria — bastou um card translúcido para vazar. Agora o `body` é o `--void` da paleta (e a `theme-color` do celular também).

**Também tirei o arco-íris decorativo:** `.kpi:nth-child(1..4)::after` pintava azul/verde/laranja/vermelho **por posição**, sem significado — competia com o vermelho/verde que de fato querem dizer algo. Viraram variações do ouro. **As cores de status (verde/vermelho/âmbar) foram MANTIDAS** em todo o sistema: elas são dado, não decoração.

**Validado no Chrome headless — 22 asserções, zero falha:** os 9 tokens da paleta com o valor exato; `body` escuro medido por luminância; **varredura de contraste nas 26 rotas** procurando texto escuro sobre fundo escuro (zero); o botão principal saindo em ouro; zero `#00e5ff` na tela; a folha A4 clara com texto escuro; 26 rotas e 21 relatórios limpos.

### ✅ ESTADO EM 30/08/2026 — v7.5 (Direção Defensiva e Tacógrafos viram parte das fichas; Frota separada)

Três pedidos: tirar a aba **Direção Defensiva** (só dentro do cadastro do motorista), tirar a aba **Tacógrafos** (só dentro do cadastro do cavalo), e na **Frota → Todos** separar em cavalos/carretas.

**Como as anteriores, nenhuma das duas telas tinha dado próprio** — liam `DB.vencimentos`. Removê-las não perdeu nada.

**1. Direção Defensiva → ficha do motorista.** Entrou como seção **"Certificados"** dentro da aba de exames, que passou a se chamar **"Exames e Certificados"**. Lista separada `CERT_TIPOS` **de propósito**: o bloco do Painel usa `EXAMES_TIPOS`, e misturar mudaria aquele resumo sem o cliente pedir (teste garante que o Painel segue com 3 cartões e sem "Defensiva").

**2. 🔑 DESCOBERTA IMPORTANTE — `badgeAnexo` não enxerga os arquivos da pasta.** Direção Defensiva tem **ZERO registro de validade** na base: os certificados existem só como **arquivo registrado** (`DB.arquivos`/`ARQUIVOS_EMPRESA`, abertos com `abrirReal`). Mas `badgeAnexo`/`anexoTipo` só olham `todosArquivos()` = nuvem + IndexedDB. Resultado: o cartão pediria "Anexar" um documento que a empresa **já tem**. Corrigido — os cartões de exame/certificado agora mostram **primeiro os arquivos da pasta** (`motDocsCat`, clicáveis) e depois o selo de enviar. Ganho colateral: ASO e Toxicológico também passaram a mostrar o arquivo real na ficha.

**3. Tacógrafo → ficha do cavalo** (`_veicTacografo`, card próprio com validade, emissão, situação, certificado, Modificar/Remover/Anexar). **Só em `tipo==='Cavalo'`** — carreta não tem cronotacógrafo, era assim na tela antiga. Teste confirma que a ficha da carreta **não** mostra o bloco.

**4. Frota → "Todos" em duas seções** (`.frt-sec`): **Cavalos** e **Carretas**, cada uma com contador. Os outros filtros seguem grade simples. Rótulos alinhados ao vocabulário do cliente: o filtro e a etiqueta do cartão passaram de "Reboque(s)" para **"Carreta(s)"**, e o donut do Painel também.

⚠️ **O que NÃO foi renomeado, de propósito:** `v.tipo` continua `"Reboque Frigorífico"` no banco, `isReb()` continua casando por `"Reboque"`, e o **cadastro de veículo ainda oferece "Reboque Frigorífico" no tipo**. Trocar isso mexeria em dado e exigiria migração — só rótulo de tela mudou. **Se um dia trocar o tipo no cadastro, `isReb()` para de reconhecer e a Frota inteira quebra a separação.**

**Validado no Chrome headless — 47 asserções, zero falha:** as rotas antigas `#direcao`/`#tacografos` não travam; o certificado da pasta aparece na ficha; lançar validade → aparece → remover → banco volta; o Painel intacto; cavalo com bloco de tacógrafo e carreta sem; Frota com 5+5 = os 10 veículos ativos, contadores certos e nada fora de seção; 26 rotas, **as 10 fichas de veículo**, as 5 abas das 6 fichas de motorista e os 21 relatórios.

### ✅ ESTADO EM 30/08/2026 — v7.4 (resumo de exames no Painel, com regra própria de alerta)

O cliente mandou o print do resumo que existia na aba Exames (removida na v7.3) e pediu que voltasse **no Painel de Controle** — com uma regra explícita: *"nada que **irá** vencer deve gerar crítico nesses, aparece 'tudo em dia'; o crítico somente em Vencimentos"*.

**O bloco "Exames dos colaboradores"** (`_dashExames`) entra no Painel logo abaixo da faixa de KPIs: um cartão por tipo (**ASO · Toxicológico · Opentech**) com a quantidade de registros e o selo de situação.

> **⚠️ REGRA QUE VALE SÓ AQUI:** o cartão olha **`situacao().ord===0`** — ou seja, **apenas o que JÁ venceu**. Documento que *vai* vencer não pinta nada: o cartão continua dizendo "Tudo em dia". O aviso de proximidade não sumiu do sistema, ele mora nos KPIs "Documentos vencidos"/"Vencem em ≤10 dias" e na tela Vencimentos. A própria tela explica isso numa linha abaixo dos cartões, para ninguém achar que o Painel está escondendo alerta.

Isso muda o que o cliente reclamou: no print dele o Opentech dizia **"1 crítico(s)"** por causa de documentos que ainda não venceram. Com os dados reais de hoje o cartão passa a mostrar **"1 vencido(s)"** (o do Renato, vencido há 10 dias) e ignora os dois que vencem em 5 e 10 dias.

Acrescentei um selo neutro **"N sem lançar"** quando há colaborador ativo sem aquele exame — informação, não alarme (o Opentech tem 5 registros para 6 motoristas).

⚠️ **Clique leva à faixa certa** (`vencVerTipo(tipo, faixa)`): a tela Vencimentos, no filtro padrão, **só lista vencidos + próximos 30 dias**. Sem cuidado, clicar num cartão "Tudo em dia" abriria uma tela **vazia** e o número não bateria com o que aparece. Então o cartão abre em `venc` quando há vencido e em `emdia` quando não há. Faixa desconhecida cai em `todos` sem quebrar.

**Validado no Chrome headless — 29 asserções, zero falha:** a contagem de cada cartão bate com `todosVencimentos()`; os três estados testados um a um (**longe de vencer → "Tudo em dia"**; **prestes a vencer, com `ord===1` confirmado → continua "Tudo em dia" e sem vermelho**; **já vencido → "1 vencido(s)" e vermelho**); com os dados reais de volta, o ASO mostra o vencido e ignora o que vence em 20 dias; o clique abre Vencimentos filtrado e **com registros na tela**; 28 rotas limpas e a aba Exames da ficha intacta.

### ✅ ESTADO EM 29/08/2026 — v7.3 (fora clima e sino; Exames virou aba de cada motorista)

Três pedidos do cliente numa mensagem: tirar a **temperatura de Londrina** e o **sino de alertas** do topo, e tirar a **aba Exames** — mas com os exames indo para o cadastro de cada motorista, com os mesmos dados, mais anexo e inserir/modificar/remover.

**1. Topo limpo.** Saíram o chip do clima (`cockWeather` + `pexWeather`/`pexWeatherRender`/`_wxInfo`, que buscava na open-meteo) e o sino (`cockBell`/`cockBadge` + `pexNotifData`/`pexNotifBadge`/`pexNotifToggle`/`pexNotifOutside`/`pexNotifClose`). Relógio, status e Ações rápidas continuam. ⚠️ **`_vencNome()` ficou**: morava no bloco do sino mas quem usa é a **linha do tempo do Painel** — o comentário acima dela foi corrigido para não enganar a próxima leitura.

**2. 🔑 A aba Exames NÃO tinha dado próprio.** `viewExames` era só uma *matriz* lendo `DB.vencimentos` (entidade `motorista`, tipos ASO/Toxicológico/Opentech). Por isso removê-la **não perdeu nada** — os 17 exames continuam onde sempre estiveram, aparecendo em Vencimentos, no Painel e nos alertas.

**3. Aba "Exames" na ficha do motorista** (`viewMotExames`, entre Resumo e Contrato, com contador). Três cartões — **ASO, Toxicológico, Opentech** — cada um com situação colorida, validade, emissão, observação, **anexo** (`badgeAnexo`) e os botões **Modificar** / **Remover**. Quem não tem o exame mostra "Não lançado" + **Lançar exame**. Embaixo, "Outro exame" e "Anexar arquivo". Tudo reaproveitando `modalVencimento`, `excluirVencimento` e `uploadPara` — **nenhuma coleção nova, nenhum CRUD novo**: o que se lança aqui é o mesmo registro que aparece em Vencimentos, e a tela diz isso ao usuário.

⚠️ **Detalhe que confundiu o teste (e é comportamento ANTIGO, não defeito):** a tela **Vencimentos só lista vencidos + próximos 30 dias**; o que está "Em dia" fica na faixa própria (`vencFiltro='emdia'`). Exame com validade em 2027 não aparece na lista padrão — o que torna a aba nova ainda mais útil, porque ela mostra os três sempre.

**Validado no Chrome headless — 49 asserções, zero falha:** as 7 funções do clima/sino sumiram e o cockpit inicia sem erro (relógio segue); a rota `exames` saiu e o endereço antigo `#exames` não trava; **os 17 exames continuam na fonte única** e na faixa Em dia; a ficha mostra validade e situação de cada exame; e o ciclo completo **inserir → aparecer na ficha → aparecer em Vencimentos → modificar → remover** rodou de verdade (Opentech do Wesley, o único faltando), com o banco voltando ao tamanho original. Mais 28 rotas limpas, **as 5 abas das 6 fichas** e os 21 relatórios.

**Celular: 3,39 MB → 3,38 MB.**

### ✅ ESTADO EM 28/08/2026 — v7.2 (a aba Início saiu; o Painel virou a entrada)

O cliente: *"remova a aba início e o que tem nela"*. Um dia depois da v7.1, que tinha acabado de refazer essa tela — ela era o resto do monitoramento e ele decidiu não querer nenhuma das duas.

**Removidos:** `viewInicio()`, a entrada `inicio` em `ROTAS`, o item do menu lateral, o **botão "Início" da barra do celular** (sobraram Painel · Buscar · Menu) e o bloco CSS `.ini-painel` criado na v7.1.

**O Painel de Controle (`#dashboard`) virou a tela de entrada.** Todo `'#inicio'` padrão virou `'#dashboard'`: o `router()`, o `renderSidebar` da abertura, o `navVoltar`, o link da logo na topbar (`index.html`) e o `pex-home` que esconde o Voltar no celular.

**⚠️ NÃO confundir: o Painel usa as MESMAS peças da Início** — `iniKpiTile()`, `iniCountUp()`, `#iniClock` e todo o CSS `.ini-cmd`/`.ini-kpi`/`.ini-spark`. **Nada disso pode ser apagado** achando que é "coisa da Início"; só o `.ini-painel` (exclusivo da v7.1) saiu.

**🚨 DEFEITO QUE O TESTE PEGOU:** o `router()` fazia `ROTAS[rota] || ROTAS.inicio`. Com a `inicio` fora do catálogo, **qualquer endereço desconhecido travava o sistema** (`Cannot read properties of undefined`) — e o mais provável de acontecer é justo o cliente ter `#inicio` nos favoritos, ou o navegador restaurar essa aba. Virou `|| ROTAS.dashboard`. Conferidos os outros 3 pontos que leem `ROTAS[...]`: já eram guardados.

**Validado no Chrome headless — 19 asserções, zero falha:** a rota e a função sumiram; menu lateral e barra do celular sem Início; **abrir sem endereço cai no Painel**; **`#inicio` antigo abre o Painel em vez de travar**; o Painel intacto (10 cartões, números fora do zero, relógio, CSS `.ini-kpi` vivo); 29 rotas limpas; 21 relatórios abrindo; zero erro. Medida a barra do celular: os 3 botões cabem (Menu termina em 488 de 504).

**Celular: 3,44 MB → 3,39 MB.**

### ✅ ESTADO EM 28/08/2026 — v7.1 (saíram o chatbot e o monitoramento)

O cliente: *"chatbot não está tendo uso, pode removê-lo, 'monitoramento' também"*.

**Apagados:** `assets/assistente.js` (~55 KB) e `assets/monitoramento.js` (~52 KB), os `<script>` do `index.html`, as entradas do `build_celular.sh` (e a checagem `iaMontarFab` de lá), e **797 linhas de CSS morto** em 6 blocos (`ASSISTENTE INTELIGENTE`, `IA PLANETA` v6.40/6.69/6.70, `MONITORAMENTO` v6.60/6.64/6.65/6.76). **Celular: 3,60 MB → 3,44 MB.**

**⚠️ Duas funções do assistente NÃO eram do chatbot** — eram utilitário da leitura de documentos: `_iaMotorista` (acha o motorista pelo nome escrito de qualquer jeito) e `_iaVeiculo` (acha o veículo pela placa no texto). O `_impRef` e a Central (`cpidRelacionar`) dependiam delas. Viraram **`motoristaPorNome()`** e **`veiculoPorTexto()`** no `app.js`, com o **mesmo casamento frouxo de antes** (de propósito: é o que já foi validado com os documentos reais). O limite está escrito no código: um sobrenome comum sozinho ("da Silva") casa com quem tiver Silva. Apertar isso um dia exige revalidar a importação com os arquivos do cliente.

**A tela Início foi refeita.** O mapa era o elemento principal; sem ele sobrariam **dois números**. Os valores que a `viewInicio` já calculava e não mostrava viraram **8 cartões clicáveis** (`.ini-painel`, classe nova — `.ini-kpis` já existia com outro comportamento e reaproveitar quebraria as duas): conjuntos, motoristas, viagens no mês, viagens pendentes, CT-e emitidos, total em CT-e, vencimentos a vigiar e reboques.

**⚠️ Duas coisas quase foram junto no CSS — e é a lição desta versão:**
1. **`.antt-acoes`** (tela ANTT, viva) morava **no meio** do CSS do monitoramento e foi apagada. Restaurada.
2. Dois cortes saíram **uma linha adiantados** e levaram `.cp-previa pre` e `.cb-pconta.off`. Restauradas.

> **Como cortar CSS com segurança (o que funcionou):** guardar cópia antes; `diff` do antes/depois; extrair **toda classe** das linhas removidas; e cruzar cada uma com `class="..."` do JS. Foi assim que `.antt-acoes` apareceu. Só confiar em "o bloco está marcado com comentário" **não basta** — havia regra viva dentro do bloco.

**Mobile:** com 2 colunas o cartão da direita era cortado (o `.ini-cmd` é full-bleed, mais largo que a janela, e o `.content` tem `overflow-x:clip` — o corte da v6.72). Passou a **1 coluna a partir de 620px**. Medido em 375/390/414/768/1024/1440: nada escapa da tela.

**Validado no Chrome headless:** 38 asserções na remoção (as funções sumiram, os utilitários seguem achando motorista/veículo, o Início com 8 cartões todos apontando para rota existente, o 1º cartão batendo com a frota) + 8 na varredura final depois da limpeza de CSS — **30 rotas limpas, 21 relatórios abrindo, CSS do ANTT vivo, zero erro de console.**

### ✅ ESTADO EM 27/08/2026 — v7.0 (saiu o gráfico de barras do saldo, que virou repetição)

O cliente riscou no print o bloco **"Saldo por motorista"** (as barras azuis no topo) e pediu para tirar. Com os cartões com foto embaixo, era **o mesmo número duas vezes na mesma folha** — e a versão com foto é a que ele pediu.

Removido o `graficos` do `fin-vales`. O `comSaldo` continua (é dele que saem os cartões **e** o KPI "Saldo em aberto"), agora somado em `emAberto`. Nada mais mudou na folha: KPIs, tabela, totais e cartões seguem iguais.

**Validado pelo caminho real — 17 asserções, zero falha:** as barras sumiram (nem `rel-graf` nem `rel-gb` na folha), os cartões e as 3 fotos ficaram, o KPI "Saldo em aberto" continua em R$ 4.400,00, a tabela e os totais intactos, a folha segue com 1 página, **os 21 relatórios abrem sem erro** e os outros 4 que têm gráfico **continuam com o deles** (a remoção foi só neste relatório).

### 🚨 ESTADO EM 27/08/2026 — v6.99 (a foto da v6.98 não chegava na folha — erro meu de teste)

O cliente mandou o print: *"NÃO ESTÁ APARECENDO COMO EU PEDI A FOTO IGUAL NO SISTEMA"*. Estava certo — a v6.98 saiu **sem o bloco funcionando** para quem usa o sistema.

**A CAUSA:** `PEXRelExecutar()` chama `r.gerar(f)` e depois monta o spec copiando uma **LISTA FIXA de campos** (`kpis, resumo, graficos, tituloTabela, colunas, linhas, totais, analise, orientacao`). O que o `gerar()` devolve e não está nessa lista é **jogado fora em silêncio**. Meus `cartoes`/`tituloCartoes` caíam ali. O renderizador (`relCartoesHTML`) e a paginação funcionavam — a folha é que nunca recebia os dados.

**POR QUE OS 51 TESTES DA v6.98 NÃO PEGARAM:** eu montei o spec **na mão** no teste (`Object.assign({...}, spec, dados)`) e chamei `relMontarPaginas` direto. Isso **pula exatamente a função que descartava os campos**. Testei o renderizador, não o caminho do cliente.

> **REGRA QUE FICA:** relatório se testa **pelo caminho real** — `PEXRelAbrirId(id)` + `PEXRelExecutar()` — e conferindo o HTML de `#pexRelOv`. Nunca montando o spec à mão. O mesmo vale para qualquer tela: se o teste constrói o objeto que o sistema construiria, ele não está testando o sistema.
>
> E **campo novo no `gerar()` tem que ser acrescentado na lista de `PEXRelExecutar`** — hoje há um comentário em maiúsculas lá avisando disso.

**Correção:** duas linhas (`cartoes: ds.cartoes, tituloCartoes: ds.tituloCartoes`) + o aviso no código.

**Revalidado pelo caminho real — 19 asserções, zero falha:** com os dados do print do cliente (12 vales, 3 motoristas em 1.800/1.800/800), a folha sai com o bloco, o título, **as 3 fotos como `<img>`**, os três nomes, os valores, e os cartões **depois** da tabela; alternar retrato/paisagem não perde o bloco; sem ninguém devendo o bloco simplesmente não aparece; e **os 21 relatórios do catálogo abrem pelo caminho real** sem erro.

### ✅ ESTADO EM 27/08/2026 — v6.98 (saldo com foto no relatório + gasto de descarga entra em Descargas)

**1. "Saldo por motorista" com FOTO no relatório de Vales.** O cliente pediu que o bloco que existe na tela saísse igual no papel. O relatório já tinha o saldo, mas só como **gráfico de barras** — agora tem também os **cartões com foto**, embaixo, depois da tabela de lançamentos.

Feito na **engine** (`relatorios.js`), não na tela — vale a regra da v6.77: *nenhuma tela monta a própria folha*. Bloco novo e genérico: `spec.cartoes` = `{foto, iniciais, nome, valor, sub}` + `spec.tituloCartoes`, renderizado por **`relCartoesHTML()`**. Qualquer relatório pode usar. Sem foto, cai nas iniciais — a mesma degradação do `avatarFoto()` da tela. A foto sai de `m.foto`, então funciona na pasta **e** no arquivo único do celular (lá vira base64 no build).

⚠️ **Os cartões PAGINAM medindo o acumulado**, igual à tabela — um grid solto estouraria a folha. Testado com 60 cartões: quebra em mais de uma página, sem vazar.

O saldo sai da **mesma `valeSaldo()`** da tela, com o mesmo filtro "só quem tem saldo ≠ 0" — se o papel tivesse conta própria, um dia discordaria da tela. No papel o valor sai **sem sinal**, e quem explica é o rótulo: *saldo devedor* ou *a favor do motorista*.

**2. Gasto de categoria "Descarga" entra sozinho na aba Descargas.** O formulário de gasto ganhou **Veículo (opcional)** e a categoria **"Descarga"** na lista. Lançou gasto de Descarga com veículo → nasce a descarga com **data, placa e valor** (`_pagSincronizarDescarga`).

O **gasto é o dono**; a descarga é o espelho, marcado com **`origemPagamento`**. Os dois andam juntos: editar o gasto atualiza a descarga; **editar a descarga escreve de volta no gasto** (senão os dois números discordariam do mesmo fato); tirar a categoria ou o veículo remove o espelho; apagar o gasto (inclusive em lote) apaga a descarga; e apagar a descarga espelhada avisa que **apaga o gasto também**. Na aba, o espelho leva o selo **"do Financeiro"**.

🚨 **A ARMADILHA — e a trava:** `pagamentos` e `descargas` são **as duas fontes da Contabilidade**. Sem cuidado o mesmo dinheiro entraria duas vezes. Por isso a fonte `descarga` **ignora quem tem `origemPagamento`** (o dinheiro já entra pelo gasto). E `_contabContaPorCategoria` aprendeu `descarga` → **`c.descarga`**, senão o gasto cairia em "Outras despesas".

**3. Defeito antigo corrigido de passagem:** o formulário de gasto mostrava **"Valor (R197609"** no lugar de "Valor (R$)". Conferido que estava igual na v6.97 publicada — não foi desta sessão.

**Validado no Chrome headless — 51 asserções, zero falha:** cartões batendo com `valeSaldo()`, quem quitou fora da lista, saldo negativo como "a favor", os cartões saindo **depois** da tabela no documento impresso, 60 cartões paginando; e no lado do gasto: descarga criada com data/placa/valor, **1 lançamento contábil e não 2**, custo de descargas subindo **450 e não 900**, conta `c.descarga`, edição nos dois sentidos, remoção em cascata, descarga digitada na aba **continuando** a contar normalmente, e o banco voltando ao estado original no fim.

### ✅ ESTADO EM 22/08/2026 — v6.97 (escolher o mês nos Pedágios + contador que ficava em ZERO)

Pedido do cliente: *"em pedágios, opção de selecionar por mês o que quer ver"*.

**1. Lista de meses na barra de Pedágios.** Ao lado dos atalhos (Todos / Este mês / Este ano / Personalizado) entrou um `<select>` com **"Todos os meses" + um item por mês que tem passagem**, igual ao dos Gastos. Novo modo `pedFiltro='mesEsp'` + variável `pedMes`. Escolher um mês manda no período; clicar em qualquer atalho **limpa** a escolha (`pedMes='todos'`), para os dois controles nunca discordarem.

⚠️ **A lista de meses é montada de `DB.pedagios` INTEIRO, nunca de `_pedFiltradas()`** — se saísse do filtrado, escolher julho apagaria os outros meses da lista e não teria como voltar. Teste guarda isso a cada mês.

**2. O período agora está escrito na tela** (`_pedPeriodoLabel()`): aparece no KPI "Total em pedágios" e no cabeçalho da lista de Passagens. Como TODO número dessa tela sai do mesmo filtro, ele precisa dizer de que período está falando.

**3. 🚨 DEFEITO ANTIGO ENCONTRADO NO CAMINHO — os 8 KPIs de Pedágios ficavam em "R$ 0,00".** A v6.72 já tinha aprendido que **`requestAnimationFrame` não serve para ESCREVER conteúdo**, mas só tapou o caso da **aba escondida** (`document.hidden`). Faltava o caso de a aba estar **visível e os quadros não virem assim mesmo**. Aí o contador nunca saía do zero — número errado na cara do cliente, que é pior do que não animar.

**Conferido que era anterior:** rodei o mesmo teste contra o `app.js` da **v6.96 já publicada** e o defeito aparece igual. Não foi o filtro de mês que causou.

**Correção — `_pexRedeContadores()`:** um `setTimeout` de 1,5 s (maior que os 900 ms da animação mais longa) que chama `pexFinalizarContadores()`. `setTimeout` dispara mesmo sem quadro nenhum, e reescrever um contador que já terminou não muda nada — o texto final é o mesmo. Ligado nos **três** count-ups (`iniCountUp`, `pedCountUp`, `licCountUp`), então Início/Painel, Pedágios e Licenças estão cobertos.

> **Regra que fica:** `requestAnimationFrame` **anima**; quem **garante** o número é o `setTimeout`. Contador novo tem que chamar `_pexRedeContadores()`.

**Validado no Chrome headless — 41 asserções, zero falha:** a lista traz os 3 meses reais (**jun/2026 = 47, jul/2026 = 47, ago/2026 = 7 = as 101 passagens**), do mais novo para o mais velho; escolher cada mês faz tabela, filtro e KPI baterem com o banco; **a soma dos meses = o total de todo o período**; a lista de meses não encolhe; o atalho "Todos" limpa a escolha e volta às 101; mês + tipo funcionam juntos; mês sem passagem não quebra a tela; e **os KPIs mostram o valor em vez de zero**.

⚠️ **O KPI arredonda de propósito** (R$ 2.726,00 no cartão × R$ 2.725,84 na lista) — é assim em todo o sistema, não só aqui. Se o cliente quiser centavos nos cartões, é uma decisão visual do sistema inteiro, não desta tela.

⚠️ **Nota de teste:** `--screenshot` do Chrome headless captura **antes** do JS assentar, então KPI animado sai zerado na imagem; `--dump-dom` espera e mostra o valor certo. Para conferir número, use `--dump-dom`; a imagem serve para layout.

### ✅ ESTADO EM 22/08/2026 — v6.96 (acabou a quebra de página nas tabelas)

Relato do cliente: *"na aba financeira, em Gastos, não fazer a quebra para segunda página, fica ruim de visualização... isso também em outros arquivos do sistema que quebrar a página"*.

**Era uma paginação só, global.** `pexEnhanceTables` (v6.6) pendurava busca + ordenação + **paginação de 12 em 12** em toda `#view table.tbl` com 6 linhas ou mais. Ou seja: não era defeito da tela de Gastos, era do sistema inteiro — arrumar num lugar arrumou todas as telas de uma vez.

**Além de chato, escondia dinheiro.** A tabela de Gastos tem `<tfoot>` com o TOTAL do filtro, mas só 12 linhas apareciam: o rodapé somava 31 lançamentos e a tela mostrava 12. O número não batia com o que dava para conferir — exatamente o que a regra de "número derivado tem que bater com a tela" existe para impedir.

**O que mudou:** `pexPaginate` virou **`pexFiltrarTabela`** — mostra/esconde linha só pela busca, sem fatiar em páginas. Sumiram `st.page`/`st.per` e os botões ‹ ›. **Busca e ordenação continuam** (com a lista inteira à vista elas ficaram até mais úteis), e o contador passou a dizer `"7 registro(s) de 101"` quando há filtro. `pexSort` não volta mais para "página 1" — não existe página. O nome `pexPaginate` ficou como atalho para o novo, para não quebrar chamada esquecida.

⚠️ **O que NÃO mudou, de propósito:** o **relatório em A4 (`PEXRel`) continua paginando** — folha de papel tem página mesmo. Só a tela deixou de quebrar. Teste cobre isso: 400 linhas → mais de uma página no PDF.

**Validado no Chrome headless — 56 asserções, zero falha:** 17 rotas varridas, 9 tabelas com a melhoria (4 delas passavam de 12 linhas: **Pedágios 101, CT-e 36, Gastos 31, Documentos 13**), todas mostrando 100% das linhas; **zero** botão de página e zero "1 / 3" em todo o sistema; no caso exato do cliente, o **TOTAL do rodapé passou a bater com a soma do que está na tela**; busca acha 1 entre 31 e volta para 31 ao limpar; ordenar duas vezes mantém as 31 e põe o maior valor em cima.

⚠️ **O arquivo do celular NÃO entra no repositório.** Ele voltou por engano no commit `b42886b` — e voltou **velho** (build v6.95, ainda com a paginação). O lugar dele é a **pasta da empresa**, um nível acima; dentro do repo são 3,5 MB que o GitHub Pages não serve e que envelhecem a cada versão. Uma limpeza antiga (`3c10cbe`) já o tinha removido pelo mesmo motivo, então agora ele está no **`.gitignore`** para não haver uma terceira vez (`30f9d68`).

⚠️ **Armadilha nova registrada — `build_celular.sh` truncava o celular.** Os passos de `perl` leem os data URIs por `$ENV{T}`, mas o `TMP` do topo cai num caminho antigo quando `T` não está exportado: rodar sem `export T` matava o perl **depois** de o arquivo do celular já ter sido zerado (aconteceu aqui: 0 byte). O script agora faz `export T="$TMP"` e confere a pasta **antes** de escrever qualquer coisa — testado rodando com `env -u T`, sai o arquivo completo de 3,58 MB.

### ✅ ESTADO EM 21/08/2026 — v6.95 (a parte da fatura que não é pedágio, sem contar duas vezes)

Logo depois da v6.94 o cliente respondeu **"lança"** ao item que eu tinha deixado em aberto: os **R$ 274,62** da mesma fatura Sem Parar que **não** são passagem.

**O lançamento:** um único registro em `DB.pagamentos`, id fixo `pg_semparar_26176725165`, data **15/08/2026** (o dia do débito em conta), R$ 274,62, categoria **"Fornecedor"**, forma "Débito automático", com `faturaPed:"26176725165"` ligando à fatura. A memória de cálculo ficou na observação: 397,20 (plano) + 54,00 (estacionamento) + 19,50 (gestor de débitos) − 196,08 (créditos).

**A ARMADILHA QUE ISSO EVITA — e que precisa continuar evitada.** O extrato do banco mostra **R$ 874,81 numa linha só**. A tentação é lançar os R$ 874,81 em Pagamentos, e aí a Contabilidade contaria os **R$ 600,19 de pedágio duas vezes** (uma pelo módulo Pedágios, outra pelo pagamento). Por isso:

> **Da fatura Sem Parar, em Pagamentos entra SÓ o que não é passagem.** Pedágio e sobra do vale já são custo pelo módulo Pedágios. A conta que fecha é `600,19 (Pedágios) + 274,62 (Pagamentos) = 874,81 (extrato)`.

E a **categoria não pode ser "Pedágio"** — `_contabContaPorCategoria` casa `/pedagio/` e jogaria os R$ 274,62 na conta `c.pedagio`, inflando o custo de pedágio. "Fornecedor" cai em `a.outros` (Outras despesas); se o contador quiser outra conta, reclassifica na Contabilidade — o que **não** duplica o registro.

**Ficou na tela, não só no commit:** o drawer da fatura ganhou a seção **"Onde este dinheiro está no sistema"**, que mostra os dois pedaços, soma e diz **"✓ Os R$ 874,81 do extrato estão todos lançados"** — ou, se o cliente apagar o pagamento, **"Faltam R$ 274,62 sem lançamento"**. O aviso é derivado de `DB.pagamentos` por `faturaPed`, então ele reage à realidade em vez de repetir um número fixo.

Também: `excluirPagamento` passou a chamar `marcarRemovido('pagamentosRemovidos', id)` (era mais um CRUD sem a segunda trava da v6.92), e `importarCadastroSeed` aprendeu a coleção `pagamentos`. Entrega em `SEED_ENTREGAS` v**6.95**.

**Validado no Chrome headless — 76 asserções em 3 fases, zero falha:** base nova e base na v6.94 (28 cada) conferem valor, data, vínculo, categoria, o lançamento único na Contabilidade **fora** da conta de pedágio, o custo de pedágio **intacto em R$ 1.419,36** e a conciliação 600,19 + 274,62 = 874,81; a 3ª fase (20) apaga o pagamento e confirma que **ele não ressuscita**, que nada sobra dele na Contabilidade e que o drawer passa a avisar o que falta.

### ✅ ESTADO EM 21/08/2026 — v6.94 (fatura Sem Parar 26176725165 lançada + o vale que NÃO volta)

O cliente mandou a fatura **26176725165** (Sem Parar, emissão 08/08/2026, vencimento 15/08/2026, período **10/07 a 07/08/2026**, total **R$ 874,81**) e pediu: *"inserir no sistema esse relatório de pedágios, retire dados e anexe"*. O PDF **já estava** na pasta `Documentos Internos\Faturas Sem Parar\` e registrado em `arquivos.js` (`sp1`) desde 13/08 — o que faltava era o **dado**.

**Como o PDF foi lido (vale para as próximas faturas):** o `pdftotext` deste ambiente é o do **xpdf**, não o do poppler — **não tem `-bbox`**, e com `-layout` a coluna `Valor(R$)` sai deslocada das linhas (o valor da linha 1 aparece 20 linhas abaixo). **A opção que resolve é `-table`**: aí cada passagem sai numa linha só, com o valor no fim. Duas armadilhas do formato: o **número da viagem quebra em duas linhas** (`10611144` + `3` = `106111443`) e a **praça pode quebrar** em duas linhas quando é longa — o parser precisa costurar as continuações.

**O que a fatura tinha (39 passagens):**
- **6 pedágios pagos pela empresa** — R$ 369,38 (BDP-1B55 214,32 · IRU-4G62 105,18 · QIO-9J07 49,88).
- **33 vale-pedágio** do embarcador BRF — R$ 1.822,60 de débito.
- Itens que **não são passagem**: plano contratado R$ 397,20, estacionamento R$ 54,00 (3 estadias no Shopping Catuaí, BCG-4D41), gestor de débitos R$ 19,50, créditos R$ 196,08.

**A DESCOBERTA QUE MUDOU O MODELO — vale-pedágio nem sempre volta inteiro.** Até a v6.93 o sistema tratava vale-pedágio como **sempre reembolsado** (a Contabilidade descartava 100% dele). Nesta fatura isso é **falso**: em 8 passagens o crédito do embarcador veio **menor que o pedágio** (ele credita a categoria contratada; se o veículo passou numa categoria maior, a diferença fica para a empresa). Foram **R$ 230,81** que a empresa pagou e o sistema não enxergaria.

> **A prova de que a extração está completa:** 369,38 + 230,81 + 397,20 + 54,00 + 19,50 − 196,08 = **R$ 874,81**, exatamente o total impresso na nota. Essa conta virou **tela**, não comentário — está no drawer da fatura, com o ✓/✗ na frente do cliente.

**O que mudou no código:**
- **`dados.js`** — 39 registros novos (`pd32`–`pd37`, `pv32`–`pv64`) com dois campos novos: **`fatura`** (nº da nota que originou) e **`credito`** (quanto o embarcador devolveu). As 62 passagens antigas ganharam `fatura:"26152227636"` na semente **e** por migração (`carimbarFaturaPedagio`, uma vez por base, só em quem ainda não tem o campo). Coleção nova **`SEED.pedFaturas`** = a capa da nota (só o que **não** é passagem; pedágio e vale são **somados** das passagens, para o mesmo dinheiro não existir escrito em dois lugares).
- **`_pedSobra(p)`** (app.js) — a regra única de "quanto sobrou para a empresa": pedágio comum = tudo; vale = débito − crédito, **nunca negativo**, e **registro sem `credito` = reembolso integral** (é o que valia até a v6.93, então nenhuma base antiga muda de número). Quem precisa do custo real usa `_pedSobra`/`_pedCustoReal` — não recalcula por conta própria.
- **`contabilidade.js`** — a fonte `pedagio` deixou de descartar todo vale e passou a lançar **só a sobra**. Vale integralmente reembolsado continua não virando custo (sobra 0 → sem lançamento). Custo de pedágio total foi de R$ 819,17 para **R$ 1.419,36** (819,17 + 369,38 + 230,81).
- **Tela de Pedágios** — card **"Faturas Sem Parar"** (nº, período, vencimento, passagens, custo, total da nota e **conferência Confere/Difere**; fatura que aparece nas passagens mas ainda não tem capa cadastrada é listada como "Só passagens", em vez de sumir). Drawer da fatura com a memória de cálculo e os botões de abrir/baixar o PDF. Drawer da passagem mostra débito × crédito × sobra. KPI do vale agora diz **"R$ 230,81 NÃO reembolsado"** em vez de "reembolsado", e o KPI Total traz o **custo real** no subtítulo.
- **Relatório `pedagio-periodo`** — coluna **Custo real** e total **CUSTO REAL DA EMPRESA**; a IA também passou a responder o custo real.
- **`pedExcluir`** agora chama `marcarRemovido('pedagiosRemovidos', id)` (era o único CRUD de pedágio sem a segunda trava da v6.92).
- Mapa ganhou **FLORESTA** e a concessionária **VIA CAMPO**; e o `_pedMapa` passou a **dizer embaixo** quais praças ficaram fora do desenho, em vez de descartá-las em silêncio.
- Entrega pela regra da v6.92: `SEED_ENTREGAS` v**6.94** (39 pedágios + `spf1` + o arquivo `sp1`), e `importarCadastroSeed` aprendeu as coleções `pedagios` e `pedFaturas`.

**Validado no Chrome headless com o sistema real — 119 asserções em 3 fases, zero falha:**
1. **Base nova** (54): 101 passagens, somas idênticas às do PDF (369,38 / 1.822,60 / 230,81 e o total de cada uma das 5 placas), capa fecha em 874,81, sem id duplicado, 14 rotas renderizam, relatório com toda linha no nº certo de colunas, zero erro de console.
2. **Base de um cliente na v6.93** (56): recebe as 39 novas, **passagem apagada antes da virada não volta**, edição do cliente (valor trocado) preservada, migração carimba a fatura antiga sem sobrescrever nada.
3. **Depois de receber a entrega** (9): cliente apaga 2 passagens e a capa da fatura, recarrega — **nada ressuscita**, carimbo não duplica, tela continua renderizando.

✅ **Resolvido na v6.95 (acima):** os R$ 274,62 que não são pedágio foram lançados em Pagamentos — **só eles**, nunca os R$ 874,81, senão o pedágio contaria duas vezes.

⚠️ **Para a próxima fatura:** o caminho oficial é a **Central de Documentos** (`docEnviar('Pedágios')` → `_pedParseSemParar`). Este lançamento foi por semente porque o parser da Central lê o texto do pdf.js (ordem de stream), que sofre do mesmo embaralhamento do `-layout` — **ele ainda não sabe ler o par crédito/débito do vale nem o nº da fatura**. Enquanto isso não for feito, fatura nova importada pela Central entra com o vale como se fosse 100% reembolsado.

### ✅ ESTADO EM 17/08/2026 — v6.93 (a regra da v6.92 aplicada nos 4 importadores)

O cliente mandou "corrija o que for necessário". Auditei **todos** os importadores de semente do sistema. Eram 4, e **3 ainda tinham o mesmo defeito** da v6.91:

| importador | como estava | risco real |
|---|---|---|
| `importarCadastroSeed` | corrigido na v6.92 | — |
| `importarLicencasSeed` | varria as 8 licenças a cada carregamento; respeitava `licencasRemovidas` | licença apagada **antes** da trava existir voltava |
| `importarCtesSeed` | varria os 36 CT-e a cada carregamento, **sem trava nenhuma** | **CT-e excluído voltava na abertura seguinte, sempre** |
| `importarManutencaoPlanilhas` | varria os 111 serviços do MANUT_SEED a cada carregamento, sem trava | **serviço de manutenção excluído voltava sempre** |

**Todos agora usam a mesma regra:** entrega **uma vez** e carimba em `DB.seedAplicado` (`legado-licencas`, `legado-ctes`, `legado-manutencao`). Helper novo: `importarSeedLegado(col, fonte, rem, tag, prep)` — o `prep` preserva o `cteDerivaPlaca` dos CT-e; a manutenção ficou inline porque além de inserir ela **conserta** campos de quem foi importado em versão antiga.

**Como a virada decide, já que não existe histórico de quando cada id foi entregue:**
- base **já tem** algum registro daquela semente → já recebeu → **só carimba, não repõe nada** (o que falta, o cliente apagou);
- base **não tem nenhum** → nunca recebeu → entrega.

Depois do carimbo, apagar é definitivo. Segunda trava ligada em todas as exclusões: `excluirCte` → `ctesRemovidos`, `excluirServico` → `servicosRemovidos`, `excluirLicenca` passou a usar `marcarRemovido('licencasRemovidas')` (antes só marcava se o id fosse da semente).

**Validado com 24 asserções no Chrome headless:** base nova recebe tudo (8 licenças, 36 CT-e, 111 serviços, 6 motoristas, 4 carimbos); apagar licença/CT-e/serviço e recarregar **duas vezes** não repõe nenhum; base do cliente na virada não ganha de volta o que apagou; base com a coleção vazia recebe a entrega; CT-e excluído pela tela não volta nem zerando o carimbo; e as 5 telas afetadas continuam renderizando.

**Não há mais varredura de semente no sistema.** `VIAGENS_SEED` e as demais coleções só são clonadas quando a coleção **não existe** (base nova), o que não ressuscita nada.

### 🚨 ESTADO EM 17/08/2026 — v6.92 (conserta a ressurreição de motorista da v6.91)

**O DEFEITO (meu, na v6.91):** o `importarCadastroSeed` varria a **semente inteira** e repunha na base tudo que estivesse faltando. O cliente já tinha **desligado e apagado o motorista Odecio Delatorre Fernandes (`m6`)** — e ele voltou sozinho na atualização, com vencimentos e documentos. O cliente viu na hora e cobrou: *"por que você puxou ele novamente, corrija isso para que nunca mais se repita"*.

**POR QUE ACONTECEU:** as listas `DB.*Removidos` só registram exclusões feitas **a partir da v6.91**. Exclusão anterior a isso não deixou rastro nenhum, então o backfill não tinha como saber que aquele sumiço era proposital. Varrer a coleção toda transforma **"o cliente apagou"** em **"está faltando, vou repor"**.

**A REGRA QUE FICA (não voltar atrás nisso):**
> **O backfill NUNCA percorre a semente inteira.** Ele aplica só a lista da versão, declarada em `SEED_ENTREGAS` (`app.js`), e **uma única vez por base** — controlado por `DB.seedAplicado`, que guarda as etiquetas de versão já entregues.

Ao entregar dado novo numa versão, some o id em `SEED_ENTREGAS` com a etiqueta:
```js
const SEED_ENTREGAS = [
  { v:'6.91', motoristas:['m7'], vencimentos:['c7','t7','a7'],
    arquivos:['a41',…,'a49'], processos:['pj1','pj2'] },
];
```
Assim: registro **novo** chega em quem já tem base; registro que **sumiu da base continua sumido**, porque nunca esteve na lista da versão; e reaplicar não duplica. As listas `*Removidos` continuam valendo como segunda trava.

**Conserto pontual:** `corrigirRessurreicaoV691()` remove o `m6` de novo (motorista + vencimentos + arquivos + processos), marca em `motoristasRemovidos` e grava `DB.correcoes.odecioV691` — roda uma vez só, então **se o Odecio for recontratado e recadastrado, não apaga de novo**. O `m6` também saiu da semente (`dados.js`, `arquivos.js`, `assets/fotos/m6.png`) e dos exemplos do Assistente; a foto continua recuperável pelo git e pela pasta de backup de 13/08.

**Validado com 23 asserções no Chrome headless** (ver [[validar-no-chrome-headless]] na memória), cobrindo: base nova sem Odecio, base do cliente com ele voltando → correção o remove, recadastro manual **não** é apagado, motorista/vencimento/arquivo apagado **não** ressuscita mais (nem o próprio Wesley), base que ainda não recebeu a 6.91 ganha o Wesley, e entrega de versão não arrasta o resto da semente.

**Resolvido na v6.93:** os outros 3 importadores (licenças, CT-e e manutenção) tinham o mesmo defeito e foram migrados para a mesma regra.

### ✅ ESTADO EM 17/08/2026 — v6.91 (motorista novo + Contrato e Ficha Criminal)

**Entregue nesta sessão:**

**1. Wesley Luiz da Silva Pereira cadastrado (`m7`)** — 7º colaborador. Dados extraídos dos documentos reais que o cliente mandou (CNH digital, ASO, laudo toxicológico Sodré + laudo do revisor, sentença do processo, declaração da advogada, contrato de trabalho assinado, integração BRF, foto). Nada foi inventado: o que o documento não traz ficou em branco (PIS, matrícula, e-mail).
- CNH `05396948335`, cat. **AE**, 1ª hab. 06/01/2012, emissão 10/07/2026, **validade 08/07/2036**, RENACH `PR929170076`, espelho `5160834370`, EAR sim, Curitiba/PR.
- CPF `068.256.969-04`, RG `97574090 SESP-PR`, nasc. 10/03/1988 em Sertanópolis/PR, filiação Valdeir Luiz Pereira e Marlene Silva de Oliveira.
- Endereço Av. Anália Franco, 151 — Jardim Brasília, Londrina/PR, CEP 86039-560. Celular (66) 98127-2639.
- Admissão **18/08/2026**, Motorista de Carreta, CTPS `068256` série `96904`.
- Vencimentos criados: **CNH** (08/07/2036), **ASO** (12/08/2026 → 12/08/2027, apto) e **Toxicológico** (coleta 11/08/2026 → 11/02/2029).
- **A validade do toxicológico é decisão a confirmar com o cliente:** o laudo Sodré imprime "Validade do exame: 10/10/2026" (prazo para uso na admissão, 60 dias). O sistema gravou **2 anos e 6 meses** (periodicidade legal da CLT, mesmo critério dos outros motoristas) e deixou os dois prazos escritos na observação do vencimento.
- 9 arquivos registrados em `arquivos.js` (`a41`–`a49`), foto em `assets/fotos/m7.png`.

**2. Ficha do motorista agora tem ABAS** (`viewMotorista`): **Resumo · Contrato de Trabalho · Ficha Criminal · Documentos**, com contador em cada aba. Estado na variável `motAba` — **não** no endereço, porque em `#motoristas/<id>` o argumento já é o id do registro (por isso `motoristas` **não** entra em `_ROTA_FILTRO`). Aba desconhecida cai no Resumo; trocar de motorista volta para o Resumo (`_motAbaId`).

**3. Aba Contrato de Trabalho — em TODAS as fichas**, não só na do Wesley. Mostra tipo, vigência, duração da experiência, **término previsto calculado** (`contratoFim`/`contratoFimISO`: início + dias, contando o primeiro dia — com a memória de cálculo escrita na tela, sem guardar a data pronta em lugar nenhum), prorrogação, remuneração, CTPS/série, PIS e local. Avisa quando a experiência está terminando usando a **mesma régua dos vencimentos** (`DB.config.alertaCritico/alertaAtencao`), para não existir um segundo critério de "está perto" no sistema. Ao lado, o cartão de anexos com `uploadPara(...,'Contrato de Trabalho')`. O contrato assinado do Wesley já aparece anexado.

**4. Aba Ficha Criminal.** Situação (`Nada consta` / `Possui processo(s)` / `Em análise` / `Não informado`), data da conferência, fonte da informação e observação — tudo editável pelo cadastro. Tabela de **processos judiciais** (nova coleção `DB.processos`, com `modalProcesso`/`salvarProcesso`/`excluirProcesso`) e cartão de certidões/sentenças. Regra que ficou escrita na própria tela: **processo em andamento não é condenação**; registrar só o que o documento diz. Os 2 processos do Wesley entraram como o papel descreve — o de furto **encerrado com absolvição** (art. 386, VII do CPP, com o próprio MP pedindo a absolvição) e o segundo **em fase inicial, sem julgamento**.

**5. Cadastro (`modalMotorista`) ganhou 3 seções:** filiação (mãe/pai) e série da CTPS em Documentos/Trabalhistas, **Contrato de Trabalho** e **Situação Criminal / Antecedentes**.

**6. Backfill que não apaga e não ressuscita** (`importarCadastroSeed`): motoristas, vencimentos, arquivos e processos novos da semente chegam em quem **já tinha base salva**, comparando por id fixo. Não duplica ao rodar de novo, **não sobrescreve edição do cliente**, e o que foi excluído de propósito não volta — `excluirMotorista`, `excluirVencimento`, `excluirArquivoReg` e `excluirProcesso` marcam o id em `DB.*Removidos` via `marcarRemovido`.

**⚠️ COMO VALIDAR NESTE AMBIENTE (descoberta desta sessão — vale mais que a receita antiga):**
O `preview_start` continua caindo em `data:` URI (scripts relativos não resolvem). **Mas o Chrome do cliente roda headless e executa o sistema de verdade:**
```
"/c/Program Files/Google/Chrome/Application/chrome.exe" --headless=new --disable-gpu \
  --allow-file-access-from-files --virtual-time-budget=15000 \
  --user-data-dir="$T/chromeprofile" --dump-dom "file:///C:/.../__teste.html"
```
Precisa da **URL `file://` completa e percent-encoded** (caminho relativo abre a página de erro do Chromium). Com `--screenshot=` sai a imagem da tela. Duas armadilhas: o `init()` do app roda `router()` e **sobrescreve** o que você injetou (renderize num `setInterval` que só age se o alvo sumiu), e sem `pexAfterRender` o `#view` não recebe a classe `.cyber` — aí o nome no cabeçalho sai escuro (é artefato do teste, não defeito). Foi assim que rodei 60 asserções nos arquivos reais e as 4 abas das 7 fichas antes de publicar.

### ✅ ESTADO EM 14/08/2026 — TUDO PUBLICADO E SINCRONIZADO (v6.90)

**Nada pendente de publicação.** Árvore do git limpa, `main` igual ao `origin/main`, GitHub Pages no ar em **v6.90**, pasta offline e celular (`Planeta Express - CELULAR.html`, ~3,5 MB) na mesma versão. Assets em `?v=183`, cache SW `planeta-express-v6-90`.

#### O que esta sessão (v6.72 → v6.90) entregou, em uma linha cada
| ver | entrega |
|---|---|
| 6.72 | revisão geral automatizada: 6 defeitos achados e corrigidos (abas da Contabilidade travadas, KPIs em zero, IA criando registro a partir de pergunta, 5 telas cortando no celular) |
| 6.73 | **contagem de pneus com fonte única** (`pneusResumo()`) — Painel dizia 38, tela dizia 80 |
| 6.74 | "Em dia" verde no card Situação dos vencimentos, clicável e batendo com a tela |
| 6.75→6.81 | módulo Aniversários criado e depois **reduzido a 82 linhas sem aba** (só data no cadastro do motorista + aviso 20/10 dias no sino) |
| 6.76 | monitoramento com cidades 28% mais afastadas + **medidor de percurso** (Dijkstra na malha viária) |
| 6.77 | **CENTRAL DE RELATÓRIOS** (`relatorios.js`): engine única A4, paginação medida no DOM, 21 relatórios |
| 6.78 | Financeiro: "Vales Motoristas", "Gastos", faturamento sob demanda, relatórios separados |
| 6.79 | relatório ganhou **4 saídas** (Voltar, ✕, ESC, voltar do navegador) + ordem por data crescente |
| 6.80 | fatura Sem Parar 26176725165 arquivada em Documentos Internos |
| 6.82→6.86 | **importar planilha no Financeiro**: separa vales de gastos, entende formato americano de data e valor, detecta duplicata |
| 6.87 | **seleção múltipla para excluir** (Gastos e Vales) + 8 listas invertidas para ordem crescente |
| 6.88 | faturamento saiu do Financeiro e virou **aba na Contabilidade** |
| 6.89 | **aba Central de Documentos REMOVIDA**: `docEnviar('<modulo>')` leva o mesmo motor para 18 abas |
| 6.90 | "Soma de Notas Fiscais": cartões do **mês vigente**, TOTAL GERAL legível, gráfico removido |
| — | **limpeza técnica**: 4,7 MB de arquivos soltos + 22 funções mortas removidas (com auditoria e backup) |

#### ⚠️ PENDÊNCIAS REAIS (ler antes de continuar)
1. **O ambiente de validação por navegador está degradado.** O `preview_start` passou a carregar o `index.html` como `data:` — nesse modo os `<script src="assets/...">` relativos **não resolvem** e o sistema não executa; arquivos grandes (o do celular, 3,5 MB) o navegador recusa abrir; e o site publicado ficou bloqueado por política em parte da sessão. **O que funciona:** um HTML pequeno (até ~100 KB) com o código embutido inline — foi assim que validei tudo desde a v6.81. Receita: `sed -n '/^function X/,/^}/p' assets/app.js` para extrair as funções, montar stubs dos helpers e abrir. **Tente o navegador normal primeiro; se vier `data:`, use o teste isolado.**
2. **Limpeza técnica inacabada** (auditoria pronta, remoção não feita): **97 classes CSS órfãs** (~11%, o grosso é `.cc-*` do Command Center substituído na v6.60) e **10 funções auxiliares em ciclo fechado** (`_apoliceFilaHTML`, `_apoliceRefresh`, `_apoliceRemove`, `_faturImpFilaHTML`, `_faturImpRefresh`, `_pedImpFilaHTML`, `_pedImpRefresh`, `_licFilaRender`, `_licRemover`, `_licSet`). ⚠️ **NÃO remover `_licExtrair` nem `_licTitulo`** — parecem mortas mas a leitura de licença as usa (`central.js:539`). Não removi porque não dava para testar depois.
3. **Possível defeito irmão do da v6.90.** Ao inverter 8 listas para ordem crescente (v6.87), qualquer código que pegava `lista[0]` como "o mais recente" passou a pegar o **mais antigo** — foi exatamente isso que fez a tela de notas mostrar julho em agosto. Corrigi em `viewNotas`; **conferir as outras 7** (estoque de baterias, estoque de pneus, Check-list, Viagens, Abastecimentos, Faturamento, CT-e) procurando por `[0]` logo depois do `sort`.
4. **Backup da limpeza** em `Área de Trabalho/Backup Planeta Express 2026-08-13_1630` (19 MB, fora do projeto, ignorado pelo git). Pode apagar quando o cliente confirmar que está tudo certo.
5. O cliente ainda não confirmou o funcionamento da **leitura de documento nas 18 abas** (v6.89) num arquivo real. Não há deploy travado (o problema de fila da v6.55 foi resolvido há muito tempo — `.nojekyll` no repo).

**Como retomar:** abra o sistema em https://planetaexpress02-sys.github.io/planeta-express/ (ou dê duplo clique no `index.html` da pasta) e siga pedindo. Se o cliente disser que "não mudou nada", é **cache** — bump de `?v=` + F5 (ver lição da v6.50).

**Arquivos do sistema (todos em `Sistema Planeta Express/assets/`):**
| arquivo | o que é |
|---|---|
| `app.js` (~6.200 linhas) | motor: rotas, views, modais, componentes |
| `dados.js` | SEED com os dados reais da empresa |
| `central.js` | MOTOR de leitura de documentos — sem aba própria desde a v6.89; use docEnviar() |
| `aniversarios.js` | avisos de aniversário (20 e 10 dias) — sem aba desde a v6.81 |
| `relatorios.js` | Central de Relatórios — engine única de PDF/impressão (v6.77) |
| `monitoramento.js` | mapa da tela Início (v6.60→6.68) |
| `contabilidade.js` | Contabilidade (v6.71) |
| `assistente.js` | IA Planeta |
| `importar.js` | leitor de xlsx/csv + unzip |
| `estilo.css` + `estilo2.css` | visual (o tema cyber vive no fim do estilo2) |

**Ao lançar versão nova SEMPRE:** subir o `?v=NN` de TODOS os assets no `index.html`, trocar o rodapé `<b>vX.YZ</b>`, trocar o nome do cache no `service-worker.js`, rodar `bash build_celular.sh` (com `export T="<scratchpad>"`) e `git add/commit/push`.

**⚠️ REGRAS QUE O CLIENTE COBROU (não quebrar):**
1. **Nunca inventar dado.** Se não dá para saber, mostrar o que falta e pedir. (Ex.: o mapa não associa motorista a placa; a IA avisa quando falta lançamento; tributos são lançados pelo contador, o sistema não apura.)
2. **Nunca duplicar informação.** Cadastro numa tela só; os outros módulos leem. A Contabilidade e a Central seguem isso.
3. **Não criar leitor de documento por tela** — registrar na Central (`central.js`).
4. **Falar em português**, linguagem simples, passo a passo.
5. **Funcionar offline.** O que depende de internet (OCR, SheetJS, clima) degrada sem quebrar.
6. **Mobile é só CSS + pós-render**, nunca duplicar lógica.

**Próximos passos possíveis (o cliente decide):**
- Conciliação bancária (OFX) — só a estrutura está preparada, falta implementar
- Registrar KM por competência, para o custo/km da Contabilidade ficar exato (hoje usa o odômetro atual, e a tela avisa)
- Cadastrar ativo imobilizado e financiamentos (as telas existem, estão vazias)
- Automação real de e-mail/OCR entrando sozinho — precisa de backend (Supabase Edge Function)
- Rastreador GPS de verdade no mapa (a camada `MonProvider` já está pronta para trocar)

**Versão atual: v6.90 (assets em `?v=183`, cache SW `planeta-express-v6-90`).** As entregas v6.72→v6.90 estão resumidas na tabela do bloco "ESTADO EM 14/08/2026", acima; o detalhe de cada uma está nas mensagens de commit (`git log`). **Quatro coisas mudaram o jeito de mexer no sistema — leia antes de codar:**

**(a) RELATÓRIOS têm engine única.** `assets/relatorios.js` → `PEXRelGerar(spec)`. Para acrescentar um relatório basta uma entrada em `PEX_RELATORIOS` com `{id, modulo, nome, desc, filtros, orientacao, gerar(f)}`, onde `gerar` devolve `{colunas, linhas, kpis, totais, graficos, analise}`. **Nunca montar folha de impressão dentro de uma tela.** A paginação é medida no DOM (régua invisível com `zoom:1!important`, senão o PDF sairia diferente no celular); medir bloco isolado e somar NÃO funciona — margens fazem o conjunto ser maior que a soma.

**(b) LEITURA DE DOCUMENTO tem motor único, mas entrada em cada aba.** `assets/central.js` continua sendo o motor (15 tipos; PDF, XML, Excel, CSV, imagem, DOCX, e-mail e ZIP; OCR quando o arquivo é digitalizado). A **aba** Central foi removida na v6.89: cada tela chama **`docEnviar('<modulo>')`** e o painel de leitura abre ali mesmo; o botão padrão é **`docBtn('<modulo>')`**, já presente em 18 abas. `cpidEscolher`/`cpidEnviar` continuam funcionando, redirecionando para o novo fluxo. **Não crie leitor próprio numa tela.**

**(c) IMPORTAR PLANILHA no Financeiro** (`finImportar`): separa Vales Motoristas de Gastos sozinho, detecta as colunas por cabeçalho **ou pelo conteúdo** (a planilha real do cliente não tem cabeçalho), entende data e valor em **formato americano** — `_finData` decide dia/mês olhando a **coluna inteira** (`8/1/26` é ambíguo, mas `8/13/26` na mesma coluna prova que é mês/dia) e `_finValor` decide pelo separador que aparece por último — e marca o que **já existe** no sistema, deixando desmarcado.

**(d) `FIN_APELIDOS` resolve nome ambíguo.** A frota tem três "Marcelo"; o dono confirmou que na planilha de vales "Marcelo" é sempre o **Marcelo Setsuo Goto**. A tabela casa por trecho do nome (não por id). Fora dela, quando o nome serve para mais de uma pessoa o sistema **não adivinha**: deixa em branco e a prévia pergunta.

Abaixo, o histórico da v6.81 para trás.

**v6.81** = **ABA DE ANIVERSÁRIOS REMOVIDA** — o cliente disse "o sistema está ficando muito complexo". O módulo v6.75 foi de **300 para 82 linhas** e não tem mais tela, coleção própria na interface, filtros nem configuração de prazos. Ficou só o que ele quer: **a data de nascimento no CADASTRO DO MOTORISTA** (onde já era digitada) e **aviso com 20 e 10 dias de antecedência** no sino de notificações (`ANIV_AVISOS=[20,10]`, fixo). Removidos: rota, item de menu, `_ROTA_FILTRO`, `contadores().aniv`, `viewAniversarios`, `anivLista`, `anivResumo`, os modais, o CSS do bloco v6.75 e a configuração `DB.config.anivAvisos`. **A coleção `DB.aniversarios` continua no `ensureCollections` de propósito** — a aba saiu, mas quem tiver cadastrado alguém não pode perder o registro (regra: trocar de versão nunca apaga dado). Sobraram 5 funções: `anivDiasAte`, `anivIdadeQueFaz`, `anivProximo`, `anivSelo` e `anivNotificacoes`. Na **ficha do motorista** o campo Nascimento ganhou um selo com quantos dias faltam e a idade que vai completar (verde no dia, vermelho ≤10, âmbar ≤20). O relatório mudou de módulo: virou **`mot-aniversarios` dentro de Motoristas** (sem aba, o dado é do motorista). **VALIDADO em teste isolado:** limites exatos (20 dias entra, 21 fica fora), motorista inativo e sem data não geram aviso, 29/02 comemora 28/02 em ano comum, e a **ordem dos avisos foi corrigida** — eu ordenava por `when`, que é texto dd/mm/aaaa, e "02/09" vinha antes de "13/08"; agora ordena pelos dias que faltam. ⚠️ **A varredura completa no navegador não pôde ser feita nesta versão**: o preview passou a carregar o `index.html` como `data:` (os `<script src>` relativos não resolvem) e o site publicado ficou bloqueado por política. A conferência foi estática: zero referências órfãs às funções removidas, rota/menu/router/filtro zerados. **Conferir no navegador na próxima oportunidade.** Abaixo, a v6.79.

**v6.79 (validada ao vivo e publicada; celular regenerado; assets em `?v=172`, cache SW `planeta-express-v6-79`).** v6.79 = **CORREÇÃO URGENTE: o relatório não tinha saída visível + ordem por data.** ⚠️ **O defeito (reclamado pelo cliente):** a prévia do relatório tinha só um `btn ghost` escrito "Fechar", **último de cinco botões** na barra — e no celular os cinco dividiam a largura, ficando ilegíveis. Sem achar a saída, ele usou o **"voltar" do navegador e caiu para FORA do sistema**. **Lição: tela cheia sobreposta precisa de saída ÓBVIA e precisa responder ao "voltar" do aparelho.** Agora há **quatro saídas**: (1) botão **"← Voltar"** grande, colorido, **primeiro item à esquerda** da barra (é onde se procura voltar); (2) **✕ fixo** no canto superior direito (`position:fixed`, não depende de rolagem nem da barra); (3) tecla **ESC**; (4) o **"voltar" do navegador e o gesto de voltar do celular** — ao abrir a prévia é empilhado `history.pushState({pexRel:1})` e o `popstate` fecha o relatório em vez de sair; `PEXRelFechar(doPop)` só chama `history.back()` quando o fechamento **não** veio do popstate (senão desfaria duas entradas). No celular a barra reordena: Voltar ocupa a linha de cima inteira e o título desce. **Também:** **Vales Motoristas e Gastos agora listam SEMPRE do mais antigo para o mais novo** (era decrescente) — `(a.data).localeCompare(b.data)`; os relatórios dos dois já eram crescentes. **VALIDADO:** ordem conferida na tela (05/06 → 20/07 → 10/08 nos vales; 02/05 → 01/07 → 09/08 nos gastos); as 4 saídas testadas uma a uma, incluindo `history.back()` — o relatório fecha e **volta para o Financeiro, dentro do sistema**; desktop com Voltar a 16px da esquerda e ✕ no canto; celular com tudo visível; regressão de 913 cliques, 40 modais, 32 rotas, 21 geradores íntegros, banco intacto, zero erro. Abaixo, a v6.78.

**v6.78** = **FINANCEIRO reorganizado a pedido do cliente.** (1) **"Vales e Pagamentos" → "Vales Motoristas"**, e o **saldo por motorista saiu do fim da tela e passou a ficar DENTRO do próprio card**, logo abaixo da tabela (`.fin-saldos`, com faixa separadora) — era isso o "logo abaixo dessa janela". (2) **"Planilha de Pagamentos" → "Gastos"** (o KPI virou "Gastos no mês", o botão "Novo gasto", e o modal/toasts também: "Editar gasto", "Gasto salvo", "Excluir este gasto"). (3) **Faturamento não fica mais em lista o tempo todo** — os **dois cartões de faturamento do topo viraram botões**: clicar em "Faturamento no mês" abre a lista **só do mês corrente**, "Faturamento acumulado" abre **todos os lançamentos**, e clicar de novo fecha (estado `finFatVer` = `''|'mes'|'tudo'`; o cartão aberto fica aceso via `.kpi.link.ativo` e a tela rola até a lista). Sem clique, a tela mostra apenas Vales Motoristas + Gastos. (4) **Dois relatórios novos no catálogo, separados** — `fin-vales` (**Vales Motoristas**: filtros de período e motorista; KPIs de lançamentos, vales adiantados, pagamentos e saldo em aberto; totais Vales/Pagamentos/Diferença; gráfico de saldo por motorista — o saldo usa a **mesma função `valeSaldo` da tela**, não uma conta paralela) e `fin-gastos` (**Gastos**: filtros de período e categoria; KPIs de quantidade, total e gasto médio; gráfico por categoria; análise com a maior categoria). Cada card ganhou **seu próprio botão "Relatório"** (`PEXRelAbrirId('fin-vales')` / `('fin-gastos')`, função nova que abre o configurador já naquele relatório). O catálogo foi de 19 para **21 relatórios**. Filtros novos na engine: **motorista** e **categoria de gasto**. **VALIDADO** com dados de teste: renomeações conferidas na tela e no modal; faturamento oculto por padrão, abrindo 1 lançamento no mês e 2 no acumulado, fechando ao reclicar; saldo dentro do card com a conta certa (Reinaldo 1200−500 = R$ 700); relatório de Vales com vales R$ 3.500, pagamentos R$ 500 e saldo R$ 3.000 batendo; relatório de Gastos com 45 registros → 3 páginas e total R$ 142.364,25 exato; zero estouro de folha; regressão de 913 cliques, 40 modais, 32 rotas, os 21 geradores íntegros, banco intacto, zero erro; celular sem corte, saldos em uma coluna e relatório cabendo na tela. Abaixo, a v6.77.

**v6.77** = **CENTRAL DE RELATÓRIOS — engine única (`assets/relatorios.js`, arquivo novo).** ⚠️ **REGRA NOVA E PERMANENTE: nenhuma tela monta a própria folha de impressão.** O fluxo é **dados do módulo → filtros → PEXRel → prévia → PDF/impressão**; melhorar o padrão na engine melhora TODOS os relatórios de uma vez. **Antes havia 3 abordagens conflitantes:** `imprimirRelatorio()` imprimia a tela; `licExportar()` montava a folha das Licenças; e `contabExportar()` **reusava a folha das Licenças** (a Contabilidade dependia de `_licBaixar`). Agora tudo passa por `PEXRelGerar(spec)`. **A engine cuida de:** cabeçalho (logo + PLANETA EXPRESS TRANSPORTES + título + período + emissão + responsável), linha divisória, bloco de **filtros aplicados** + nº de registros, **KPIs**, **gráfico de barras sóbrio para papel**, tabela com **alinhamento por tipo de coluna** (texto à esquerda, número/dinheiro à direita com `tabular-nums`, data ao centro) e `overflow-wrap:anywhere` (texto não escapa da célula), **totais**, **análise**, rodapé com empresa/CNPJ e **"Página X de Y"**, **orientação retrato/paisagem alternável** (`@page` reescrito na hora), prévia em tela e exportação **PDF/impressão + Excel + CSV** com nome profissional (`PlanetaExpress_Relatorio_<Nome>_MM-AAAA`). ⚠️ **A PAGINAÇÃO É MEDIDA DE VERDADE NO DOM**, numa régua invisível com a largura útil da folha — e o defeito que isso corrigiu merece registro: **medir cada bloco isolado e somar NÃO funciona** (margens e colapso de margem fazem o conjunto ser maior que a soma), na 1ª versão **5 de 13 páginas estouravam a folha**; a versão final mede sempre o **acumulado da página**, ajusta o lote de linhas para baixo até caber e depois para cima enquanto couber (evita página quase vazia). A régua tem **`zoom:1!important`** porque no celular a prévia usa `zoom` — sem isso a paginação mudaria conforme o tamanho da tela, e o PDF tem que sair **idêntico em qualquer aparelho** (confirmado: 14/6/5/3 páginas iguais no desktop e a 375px). No celular a prévia usa **`zoom` e não `transform:scale`** (o scale encolhe o desenho mas o elemento continua ocupando 1123px e o painel passa a rolar de lado). **Logo:** `relLogoSrc()` pega o `src` de uma `<img>` já carregada — funciona na pasta e no arquivo único do celular (base64), sem caminho fixo. **CATÁLOGO: 19 relatórios em 16 módulos** — frota (cadastro, KM/horas), motoristas (cadastro, CNH), vencimentos, manutenção (serviços, próximas trocas), pneus, abastecimentos, pedágios, seguros, CT-e, viagens, descargas, baterias, licenças, financeiro, contabilidade e aniversários. **Acrescentar relatório novo = uma entrada em `PEX_RELATORIOS`** com `{id, modulo, nome, desc, filtros, orientacao, gerar(f)}`; o `gerar` devolve `{colunas, linhas, kpis, totais, graficos, analise}`. **Filtros implementados:** período (com atalhos Este mês / Mês passado / Este ano / Todo o período), veículo, tipo de veículo, situação do veículo, situação do motorista, tipo de manutenção, faixa de vencimento, tipo de documento, situação do CT-e e situação da viagem — **e todos saem impressos no documento**. O botão **Relatório** da barra superior abre o configurador do módulo atual (`imprimirRelatorio()` → `PEXRelAbrir(rota)`); os **9 botões "Imprimir" espalhados** passaram a apontar para ele; quem não tem relatório no catálogo cai em `imprimirTelaAtual()` (o comportamento antigo, preservado). **VALIDADO:** os 19 geradores rodam sem erro e **toda linha tem exatamente o número de células das colunas**; **zero estouro de folha** em 10 relatórios reais; casos extremos — **500 registros → 14 páginas**, textos de 170 caracteres **sem cortar nada**, relatório **vazio** com a mensagem certa, **1 linha**, 1 e várias páginas; folha exatamente A4 (794px = 210mm paisagem, 1123px = 297mm retrato); nada fora da folha; filtros, KPIs, totais e rodapé conferidos; exportação CSV/Excel com o nome certo; regressão de 911 cliques, 40 modais, 32 rotas, banco intacto, zero erro; celular sem rolagem lateral e com a mesma paginação. Abaixo, a v6.76.

**v6.76** = **MONITORAMENTO: cidades mais afastadas + MEDIDOR DE PERCURSO.** (1) **Espalhamento** — o cliente pediu Londrina "mais longe" e mais distância entre as cidades. Não se mexe em coordenada (elas são reais): quem manda no espalhamento é a **`MON_AREA`** em `monMedir()`, a área do viewBox onde as cidades são projetadas. Foi de **58% da largura e 280px de altura para 74% e 372px** → a escala subiu de ~2,34 para **2,99 px/km (+28%)**. Londrina–Cambé (12 km) saiu de ~31px para **41px** de separação; Londrina–Maringá foi para 267px. **Nenhuma cidade e nenhum rótulo saiu do quadro** (conferido nos 48 locais e nos 95 textos, no desktop e a 375px). Se ele pedir mais afastamento ainda, é **só nessa `MON_AREA`** que se mexe — nunca em `MON_LOCAIS`. (2) **MEDIDOR DE PERCURSO (a melhoria)** — botão de régua nos controles do mapa: liga o modo, toca na cidade de origem e na de destino, e o sistema mostra **quilometragem, tempo estimado a 60 km/h, as rodovias, as cidades do trajeto e a distância em linha reta**, com o caminho traçado em verde no mapa. Como funciona: `MON_VIAS` já era um **grafo** (cada rodovia é uma sequência real de cidades), então `monGrafo()` monta a lista de adjacência e **`monCaminhoEntre(a,b)` roda Dijkstra** por quilometragem (Haversine entre cidades vizinhas). **Dado real, não inventado** — coordenadas reais + sequência real das rodovias; o painel diz que é **estimativa** ("a quilometragem oficial pode variar"), seguindo a regra de nunca vender conta como certeza. Aferição: Londrina→Maringá **108 km** pela BR-369 (oficial ~100), contra 80 km em linha reta; Londrina→Cornélio 55 km; Paiçandu→Tamarana 166 km por PR-323/BR-369/PR-090. As cidades de **referência** ganharam `data-local` e uma área de clique invisível (`.mon-r-hit`, r=11) para entrarem no medidor. **Regras do cliente mantidas:** card **exatamente 452px no desktop e 380px no celular** (medido antes e depois de abrir o painel), o painel **flutua por cima** e some ao desligar; no desktop fica à direita (os controles ocupam a coluna esquerda) e no celular vai para baixo, entre os controles e o rodapé "SINCRONIZADO", com rolagem interna — **sem tapar nada**; funciona **offline**; nada apresentado como GPS real. **VALIDADO:** os **2.304 pares** de cidades (48×48) têm percurso calculável e nenhuma cidade ficou isolada da base; fluxo completo testado (ligar, escolher origem, destino, resultado, traçado com 10 cidades destacadas, "medir outro", clicar duas vezes na mesma cidade, desligar limpando tudo); regressão de 901 cliques, 40 modais, 32 rotas, banco intacto, zero erro. Abaixo, a v6.75.

**v6.75** = **NOVO MÓDULO "ANIVERSÁRIOS DO PESSOAL"** (`assets/aniversarios.js`, arquivo novo — registrar em `index.html` E na linha do `cat` do `build_celular.sh`). Pedido do cliente: tabela de aniversários que avise 30 e 10 dias antes, totalmente editável, **inclusive o prazo de aviso**. **PRINCÍPIO SEGUIDO (o mesmo da Contabilidade):** quem já é **motorista/sócio NÃO é recadastrado** — a data vem de `DB.motoristas[].nascimento` (os 6 já tinham no seed), e **editar pela tela de Aniversários grava na ficha do motorista** (`salvarAnivMotorista`), sem cópia. A coleção nova **`DB.aniversarios`** guarda só quem não existe em outro módulo (escritório, parceiros, família). **Prazos editáveis:** `DB.config.anivAvisos` (padrão `[30,10]`, vive no `pex_config` e sobrevive à troca de versão) — `modalAnivAvisos()` deixa **mudar, apagar e acrescentar quantos prazos quiser**, com prévia em português ("vai avisar 30 dias antes, 10 dias antes") e botão de voltar ao padrão. **Fonte única:** `anivLista()` (motoristas + pessoas, já com dias/idade/dia da semana/faixa) e `anivResumo()` — toda tela, KPI e notificação lê daí ([[numeros-derivados-fonte-unica]]). Tela: 4 KPIs (hoje · ≤menor prazo · ≤maior prazo · no mês, os rótulos acompanham os prazos configurados), filtros Todos/Hoje/Avisando/Este mês/Sem data, e tabela com Pessoa · Cadastro (selo azul "Motorista" que abre a ficha, ou verde "Pessoa") · Dia · Nascimento · Faz (idade que completa) · Próximo aniversário com dia da semana · Situação · Contato. Linha de quem faz hoje fica verde. **Integrações:** sino de notificações (`pexNotifData` → `anivNotificacoes()`; quem faz hoje vira Aviso, os próximos Informativo) e **badge no menu** (`contadores().aniv`), item em **Empresa**, entre Quadro Societário e Código de Ética. Ícones novos `cake` e `users`; CSS "ANIVERSÁRIOS (v6.75)" no fim do estilo2.css. **Detalhe de cálculo:** `anivDiasAte` trata **29/02** (em ano comum comemora 28/02, senão o JS empurraria para 01/03). **VALIDADO com datas injetadas nos limites:** 0d→"É hoje", 5d e 10d→faixa de 10, 11d e 30d→faixa de 30, 31d→fora, sem data→"sem data"; KPI × lista bate nos 4 filtros; trocar os prazos para 60/20/3 recalculou KPIs, notificações (6) e badge (6); CRUD real testado (cadastrar com máscara de telefone, editar, excluir, e editar nascimento de motorista **gravando na ficha dele sem duplicar**). Regressão: 32 rotas, 1.030 cliques, 40 modais, banco intacto, zero erro; celular sem corte, tabela vira cartões e os modais viram folha. Abaixo, a v6.74.

**v6.74** = **"Em dia" (verde) no card "Situação dos vencimentos"** — pedido do cliente: *"deve ter o primeiro escrito 'em dia' e verde de tudo o que está em dia no sistema, para dar um ar de organização"*. No Painel, o card virou **situação geral** em vez de só a faixa de atenção: a fatia **verde `#22c55e` "Em dia"** entrou como **primeira** do donut e **primeira** da legenda, o centro passou a mostrar o **total de documentos** (era "na faixa") e o subtítulo virou "situação geral · clique para abrir". Com os dados atuais o verde ocupa **87,9% do anel** (51 de 58) — é esse o "ar de organização" que ele queria. **A linha verde é clicável** (`#vencimentos/emdia`) e **abre exatamente o que ela conta** (regra de [[numeros-derivados-fonte-unica]]): na tela de Vencimentos entrou a faixa `emdia` em `VENC_SEC` (verde, ícone `check`, sub "mais de 30 dias — nada a fazer agora") + **5º KPI verde "Em dia"** (a grade foi de `repeat(4,1fr)` para `repeat(5,1fr)`; no celular continua 2 colunas pela regra `.grid.kpis`). ⚠️ **A regra da v6.27 foi preservada:** a tela de Vencimentos **continua mostrando só a faixa de atenção por padrão** — `section('emdia')` tem guarda `if(k==='emdia' && !vendoEmDia) return ''`, então os "em dia" só aparecem quando o usuário pede (clicando no verde do Painel ou no KPI verde). Quando está nesse modo, o banner, o contador do rodapé e o estado vazio mudam de texto. **VALIDADO:** Painel 51 → tela lista 51 linhas e o rodapé diz "51 documento(s) em dia"; donut confere (verde 375,7 de 427,3 da circunferência = 51/58; vermelho 2, laranja 3, azul 2, amarelo omitido por ser 0); alternar os 6 filtros não dá erro; regressão de 878 cliques, 38 modais, 31 rotas, banco intacto, zero erro; celular sem corte, legenda inteira e 5 KPIs em 2 colunas. Abaixo, a v6.73.

**v6.73** = **CONTAGEM DE PNEUS BATENDO EM TODA PARTE — achado PELO CLIENTE, não pela varredura.** Ele viu no Painel "Pneus cadastrados **38**" e na tela de Pneus "Pneus no total **80**". Causa: o Painel usava `DB.pneus.length` (conta **linhas cadastradas**) e a tela de Pneus usava `pneuTotal()` (**soma o campo `qtd`** — uma linha pode valer 4 pneus). **Por que a varredura da v6.72 não pegou:** ela rodou sobre a base semente, onde `DB.pneus` está **VAZIO** — 0 = 0 batia, e a divergência era invisível. Os pneus reais só existem na nuvem do cliente. **Lição de método: número derivado só se testa com dado que force a diferença** (quantidade > 1), nunca com a coleção vazia. **Correção estrutural:** nova função **`pneusResumo()`** em `app.js` (logo depois de `pneuTotal`/`_pneuNoLimite`) = **fonte única** de toda contagem de pneu — devolve `{total, registros, noLimite, estoque, veiculos}`. **TODA tela nova que mostrar número de pneu tem que ler daí; nunca de `DB.pneus.length`.** Ligados nela: Painel (card "Composição da frota", rótulo virou **"Pneus no total"** e ficou **clicável** → `#pneus`), KPI "Pneus no limite" do Painel (agora **soma as quantidades**, antes contava linhas), tela de Pneus (os 3 KPIs) e a tela `#pneus/limite` (o KPI virou `pneuTotal` dos alertas, e cada linha mostra `2×` quando a quantidade é maior que 1, para o usuário entender por que 3 linhas = 6 pneus). A IA já estava certa (diz "80 pneus no total (34 registros)"). **VALIDADO com dado injetado de propósito:** cenário 34 registros = 80 pneus + 27 em estoque → Painel 80 · tela de Pneus 80 · IA 80 · estoque 27 dos dois lados · limite 18 no Painel e 18 na tela. E **8 movimentações em sequência** (cadastrar registro de 4, registro de 1, mudar qtd 4→6, registro sem qtd, excluir, dar baixa no estoque e instalar, +4 no limite) — os três lugares bateram em **todas**. Também varri **todos os 10 KPIs do Painel contra a tela que cada um abre**: só o de pneus divergia (os outros batem; "Despesas" e "Conjuntos ativos" acusam falso positivo no teste — formatação em centavos e conceito cavalo=conjunto). Regressão: 884 cliques, 38 modais, 31 rotas, zero erro, banco intacto. Abaixo, a v6.72.

**v6.72** = **REVISÃO GERAL AUTOMATIZADA — 6 defeitos encontrados e corrigidos.** Nenhuma tela nova; é uma versão de qualidade. Como foi feito: o sistema inteiro foi executado no navegador (`file://`) e varrido por script — **31 rotas + 30 sub-rotas renderizadas, 902 botões clicados, 38 modais abertos**, conferindo erro de console, handler inexistente, texto sujo (`undefined`/`NaN`), id duplicado, data inválida e vazamento de layout, no desktop e a 375px. **O que estava errado:** **(1) Abas da Contabilidade travavam** — `contabilidade` não estava em `_ROTA_FILTRO`, então ao entrar por `#contabilidade/<aba>` o argumento ficava no endereço e o `router()` desfazia todo clique seguinte nas abas (é o mesmo defeito que a v6.62 corrigiu nas outras telas); **(2) aba desconhecida abria a Contabilidade em branco** — nenhum `if` casava e o corpo saía vazio; agora volta para o Painel. **(3) KPIs podiam ficar em ZERO para sempre** — os 4 contadores animados (Painel/Início, Pedágios, Licenças, Central) usam `requestAnimationFrame`, e o navegador **não entrega quadros para aba em segundo plano**: se a tela fosse montada assim (aba aberta atrás, sessão restaurada, recarga do service worker) o número nunca saía do zero. Agora, aba escondida = escreve o valor final direto (`_pexSemAnimacao`/`_pexEscreverContador`), e ao voltar para a aba tudo é reconferido (`pexFinalizarContadores` no `visibilitychange`). **(4) A IA criava registro a partir de PERGUNTA** — em `_iaEhPergunta` os radicais estavam fechados com `\b` no fim (`\b(quant|gast|…)\b`), então "quant" não casava com "quanto" nem "gast" com "gastei"; "quanto gastei com manutenção" caía no detector de comandos, batia em `/manuten/` e **gravava um serviço com a frase do usuário como descrição, sem veículo e valor 0** (fere a regra "nunca inventar dado"). Radicais viraram prefixo de verdade; perguntas de contagem/gasto/vencimento voltaram a responder. **(5) A IA travava no follow-up** — depois de "de qual veículo?", qualquer frase seguinte era engolida como resposta e ela repetia a mesma pergunta até o usuário digitar "cancelar"; agora, se a frase nova é claramente outra pergunta (sem placa, sem nome, não é número), ela abandona o pendente e responde. **(6) `iaCmdServico` gravava serviço sem veículo** — agora pergunta a placa, como os outros comandos. **(7) CELULAR: 5 telas cortavam conteúdo** — Exames, Documentos, Quadro Societário, Financeiro e Configurações traziam o número de colunas **no `style=` da própria tag** (`repeat(3,1fr)`, `minmax(460px,1fr)`…), e **estilo em linha ganha do CSS**: a coluna nascia mais larga que a tela e o `.content` (`overflow-x:clip`) cortava — no Quadro Societário sumiam 113px. Corrigido com `.grid:not(.kpis){grid-template-columns:1fr!important}` + `min-width:0` nos filhos (as faixas `.kpis` seguem com 2 colunas). Também: tabela que **não** vira cartão volta a rolar de lado (`.tbl-wrap.tbl-rola`, marcada no `pexMobileTables`), filtros em cápsula (`.seg`) rolam em vez de sumir, botões do cabeçalho do card e do Código de Ética quebram linha, e célula de tabela-cartão com conteúdo largo quebra em vez de empurrar. **Resultado da revalidação: zero corte no celular, zero erro em 902 cliques, banco de dados intacto, celular regenerado com tudo dentro.** ⚠️ **LIÇÃO para o futuro:** (a) rota nova com argumento **tem que entrar em `_ROTA_FILTRO`** se o argumento for filtro/aba; (b) **radical em regex não pode ter `\b` no fim**; (c) **nunca confie em `requestAnimationFrame` para escrever conteúdo** — só para animar; (d) grid com colunas no `style=` **não obedece** ao CSS do celular sem `!important`. Abaixo, a v6.71.

**v6.71** = **nova aba CONTABILIDADE** (`assets/contabilidade.js`, arquivo novo, ~1.230 linhas). **PRINCÍPIO: a informação é cadastrada UMA VEZ, no módulo dela — a Contabilidade LÊ e traduz em lançamento na hora da consulta; NADA é copiado.** `contabLancamentos()` deriva de ctes, faturamento, abastecimentos, pedagios, servicos, manutencoes(óleo), pneus, baterias, descargas, pagamentos, vales, notas, seguros (prêmio rateado em 12), tributos, depreciação e juros de financiamento. Cada lançamento tem **id estável** (`fonte:idDoRegistro`) e carrega a ORIGEM. **Vale-pedágio não entra como custo** (é reembolsado). `DB.contabClass` guarda **só a reclassificação** do usuário, sem duplicar o registro. Estruturas próprias: plano de contas editável (44 contas/6 grupos), centros de custo (áreas + um por veículo, automático), lançamentos manuais, ativo imobilizado com depreciação linear, financiamentos com parcelas e juros, tributos, fechamento por competência e auditoria. **11 abas**: Painel (10 KPIs + comparação com período anterior), DRE (% e variação), por veículo (custo/km, receita/km, margem), lançamentos (busca + reclassificação), ativos, financiamentos, tributos, fechamento com pendências, auditoria, relatórios (10 tipos em PDF/Excel/CSV) e plano de contas. IA responde faturamento/lucro/gastos/veículo de maior custo **e avisa quando falta dado**. Importação **reusa a Central de Documentos** (não foi recriada). **VALIDADO:** totais batem com as fontes (CT-e R$166.205,35 · manutenção R$350.357,65 · pedágio pago R$819,17 · descargas R$6.674,00), sem ids duplicados, chamar 2× não dobra, reclassificar não cria registro nem altera o CT-e; **teste ponta a ponta** (abastecimento+CT-e+manutenção+pedágio inseridos nos módulos) fez os 4 aparecerem na conta/centro certos com resultado do veículo calculado, e ao remover voltou ao original; mobile sem overflow; 7 telas antigas intactas; zero erro. Commit `3c6f7ba`. ⚠️ **Ainda dependem de configuração/uso:** conciliação bancária (OFX) é só estrutura preparada; km rodado por período não existe no sistema, então custo/km usa o odômetro atual (avisado na tela); tributos são lançados pelo contador (o sistema não apura).

**O MONITORAMENTO (card da tela Início) é o assunto mais quente — 5 versões seguidas.** Tudo vive em **`assets/monitoramento.js`** (arquivo novo desde a v6.60), com as camadas separadas: `MON_LOCAIS` (48 cidades, lat/lon reais) · `MON_VIAS` (23 rodovias = sequência real de cidades) · `MON_ROTAS` (3 rotas operacionais) · `monProjetar` (lat/lon → x,y, **escala uniforme**) · `MonProvider` = `OfflineSimulationProvider` (trocar por `GPSProvider` com a mesma interface) · `MonSim` (simula, não desenha) · `monRender` (desenha, não simula). **Regras que o cliente cobrou e NÃO devem ser quebradas:** (1) **nunca associar motorista a placa** — o sistema não sabe quem dirige o quê; mostrar **só a placa do cavalo**; (2) **nada apresentado como GPS real** enquanto for simulação (o rodapé diz "Simulação local"); (3) **mesmo tamanho e posição** do card (452px / 380px no celular); (4) **funciona offline** — o único extra online é a **temperatura real** das cidades (Open-Meteo, cache 30 min; sem internet some, nada é inventado). **Se acrescentar cidade:** uma linha em `MON_LOCAIS` (`tipo:'referencia'` para contexto, `'destino'` para virar destino + rota em `MON_ROTAS`). **Histórico do que ele reprovou:** v6.60 (achou "extremamente simples"), v6.64 (mapa cheio, mas "muito fraco" — opacidades baixas), v6.65 (carta topográfica densa), v6.66 (mais cidades, rotas sinuosas, veículos maiores e lentos). **Se ele pedir mais impacto de novo, mexa em densidade/contraste/tamanho — não em opacidade fina.**

v6.63 = **ANTT sem o banner do topo**, com "Editar dados" e "+ Veículo" ao lado do selo ATIVO. v6.62 = **corrige o filtro que travava** ao entrar por um card do Painel (o argumento do hash desfazia o clique seguinte) — ver `_ROTA_FILTRO` no router. v6.61 = **mapa geográfico preciso** (escala uniforme, distâncias Haversine reais, 11 cidades de referência, malha viária real, anéis de raio, escala, bússola) + **somente a placa do cavalo — nunca motorista associado por adivinhação**. v6.60 = **MONITORAMENTO reestruturado** (`assets/monitoramento.js`): dados/simulação/desenho separados, base sóbria, veículos reais percorrendo as rotas com pausa no pátio, tooltips, legenda, status e relógio real — mesmo tamanho e lugar de sempre, pronto para receber GPS trocando só o provider. v6.59.1 = **PDF de tabela lido pelas coordenadas** (corrige o faturamento que gravava 0). v6.59 = **CENTRAL DE PROCESSAMENTO INTELIGENTE DE DOCUMENTOS** (`assets/central.js`): um pipeline único para PDF/XML/Excel/CSV/JPG/PNG/DOCX/e-mail/ZIP — identifica, lê (XML sem OCR, PDF com OCR só se preciso, Excel com SheetJS), classifica com % de confiança, extrai, relaciona com a frota, confere, atualiza o módulo certo, arquiva e gera log; **com banco de modelos que aprende o emitente**. Acabou o "um leitor por tela". v6.58 = **módulo "Licenças e Alvarás"** com 8 licenças reais lidas por OCR + **"Conformidade ANTT" renomeada para "ANTT - RNTRC" e movida para Cadastros**. v6.58 = **NOVO MÓDULO "Licenças e Alvarás" (Centro de Gestão de Conformidade)** com 8 licenças REAIS lidas por OCR do PDF do cliente, situação automática, renovação inteligente com versionamento, upload que lê o PDF e preenche, relatórios PDF/Excel/CSV, integração automática com Vencimentos/Painel/notificações/IA — **+ "Conformidade ANTT" renomeada para "ANTT - RNTRC" e movida para o grupo Cadastros**. v6.57 = **aba Conformidade ANTT + "Conjuntos ativos" + leitor de relatório robusto**, commit `ef2415f`. v6.56 = **Faturamento: pipeline inteligente de documentos** (OCR pdf.js+Tesseract, classificação de tipo com % de confiança, fila multi-arquivo com etapas animadas, extração+relacionamento+conferência, painel de resumo, logs), commit `ed67bc4`. v6.54/6.55 = **REFATORAÇÃO MOBILE** (app nativo — barra superior enxuta, Voltar+título, drawer premium com swipe, Spotlight, tabelas→cards, bottom sheet, barra de navegação inferior; desktop intocado; uma lógica só), commits `12d71b8`/`85b16eb`. v6.53 = **Faturamento: Importar do contador** (PDF/Excel/XML → lançamentos por mês ou por nota, com prévia e dedup), commit `40be0f3`. v6.52 = **Financeiro: Planilha de Pagamentos** (coleção `DB.pagamentos` separada dos vales; KPI + card com filtro/total/CRUD), commit `3e319ac`. v6.51 = **excluir motorista apaga todos os documentos dele (cascata) + modal de Alarme Thermo King legível** (era branco/ilegível), commit `6a073f9`. v6.50 = **Painel "Seguros a vencer" bate exato com o que abre** (contagem alinhada ao filtro; era cache no cliente), commit `aac484e`. v6.49 = **Viagens: KPIs clicáveis** (Pendentes/A baixar/Termos pallet levam direto ao filtrado), commit `f6e8f37`. v6.47/6.48 = **NOVO MÓDULO PEDÁGIOS** (centro de inteligência Cyber; 62 passagens reais da fatura Sem Parar; KPIs/gráficos/mapa/alertas/drawer/importar extrato + IA), commits `590a73b`/`102bb38`. v6.46 = **apólice anexada aparece na lista (verde "Anexado") com ver/baixar/trocar/remover** (`_segAnexo`), commit `1803971`. v6.45 = **leitura profissional das apólices (pdf.js + OCR Tesseract)** — resolve os PDFs que "não liam" (object streams Tokio/Allianz) e escaneados; auto-detecção muito mais forte, commit `16cb10a`. v6.44 = **enviar apólice com detecção automática do seguro** (botão na aba Seguros; casa nº/placa/seguradora e anexa no seguro certo), commit `c2c966a`. v6.43 = **nova aba Seguros** (apólices/vigências/prêmios + avisos no Vencimentos e no Painel), commit `41c2219`. v6.42 = Monitoramento virou Command Center (mapa da Início redesenhado), publicada no commit anterior. v6.41 = topbar sem barra (desktop) — cápsulas de vidro flutuantes, publicada no commit `b343bce`. v6.40 = nova experiência da IA Planeta (esfera + chat holográfico; cérebro intacto), publicada no commit `685fc07`. v6.39 = ESC volta uma tela + Motorista "Problemas de saúde" + busca Termo Pallet/Transporte indo direto, publicada no commit `087e60a`. v6.38 = Viagens sem coluna Status + Frota com mais campos por tipo, publicada no commit `28b6000`. v6.37 = óleo com valor somado na manutenção + documentos ver/baixar/excluir + Viagens sem paginação, publicada no commit `9b853e2`. v6.34 = navegação mobile premium (menu ☰ ao lado da marca + botão Voltar no topbar), publicada no commit `bfc58e1` (v6.35/6.36 revertidas — ver acima). v6.33 = Viagens ganhou importação de Excel + cabeçalho sem descrição repetitiva + Alarmes modernizados, publicada no commit `b36d2d4`. v6.33 = Viagens ganhou importação de Excel + cabeçalho sem descrição repetitiva + Alarmes modernizados, publicada no commit `b36d2d4`. v6.32 = topbar/cabeçalho + modais viraram CYBER (fecha 100% o tema escuro), publicada no commit `228e0b0`. v6.31 = Viagens virou CYBER (removida do `_noCy`), publicada no commit `c49be68`. v6.30 = Painel de Controle com navegação inteligente (todo card abre o item pendente já filtrado), publicada no commit `1bc1eb8`. v6.29 = Descargas reformulado (cyber + acordeão por mês + importação de Excel), publicada no commit `25c1336`. v6.28 = KM/Horas: removido o ícone de calendário GIGANTE dos cards (svg sem tamanho) + cards menores; Painel "Trocas a vencer" leva a #km/avencer (visão filtrada só com trocas vencidas/próximas; kmFiltro + _kmAvencer; roteador aceita #km/<filtro>). v6.27 = Vencimentos reformulado: esconde "em dia" (>30d), mostra só a faixa (vencidos + próx. 30 dias) SETORIZADA em Vencidos/≤10/11-20/21-30 dias, com 4 KPIs clicáveis por faixa, seções coloridas e badge de dias. viewVencimentos reescrito + CSS "VENCIMENTOS (v6.27)". v6.26 = Painel de Controle enxugado: removidos "Viagens por mês", "Despesas por mês" e "Situação das viagens". Sobraram 4 cards (Situação dos vencimentos, Trocas a vencer, Composição da frota, Últimos check-lists) + 8 KPIs do topo. v6.25 = Painel de Controle: removidos "Vencimentos por mês", "Mapa de atividade", "Linha do tempo" e "Documentos por situação"; "Situação dos vencimentos" reordenado (Em dia>Atenção>Críticos>Vencidos) e pareado com "Trocas a vencer"; "Situação das viagens" em largura total. 7 cards. v6.24 = Início reorganizada: KPIs (Veículos/Motoristas ativos) à ESQUERDA + Monitoramento (meia tela) à DIREITA; mapa com mais vida (radar/anéis/caminhões glow, viewBox 640x520), Cambé abaixo-esq de Londrina, 5 caminhões = placas reais dos cavalos (iniCavalos), hover só placa, painel com dados reais do veículo (sem motorista fictício). v6.23 = ajustes nos Pneus: nomes das posições (Dianteira Esq/Dir, Tração/Truck/Eixo do Meio/Último Eixo Externo-Interno LE/LD), cavalo SEM estepe (10 pos), carreta com 2 estepes (14), condição editável manual (pneuSetCondicao/PNEU_COND), desenho refinado (chassi/para-brisa/cubo/sulcos). Versão ESCURA completa no histórico: v6.4 (commit `38d3479`). **Praticamente TODO o sistema é CYBER neon** (Início/Painel via `.ini-cmd`; Manutenção via `#view[data-route="manutencao"]`; demais telas via `#view.cyber` global; topbar cockpit + menu lateral + **modais**). **NÃO há mais superfície clara** (topbar/menu/conteúdo/modais todos CYBER desde a v6.32; só a impressão força papel branco). **Financeiro sem senha.** Gráficos "vivos" em todo o sistema. Pneus com diagrama inteligente.
```
